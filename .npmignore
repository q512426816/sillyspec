# 测试与文档（工具运行时不依赖；init 只读 templates/ 和 .claude/skills/）
test/
docs/

# sillyspec 仓库自身用 sillyspec 管理变更的 spec 目录（非工具运行时）
.sillyspec/

# 开发 / IDE
.idea/
.DS_Store
STATE.md
.playwright-mcp/

# 依赖
node_modules/
packages/dashboard/node_modules/

# 构建产物 / 敏感凭证
*.tgz
.npm-publish-token

# dashboard 纯开发期文件（运行时只依赖 dist/ + server/；src/ 是 Vite 构建源、public/ 与 dist/ 重复、
# 根 index.html/vite.config.js/package-lock.json 仅本地构建用 —— 排除瘦身，保留 dist + server 供
# `sillyspec dashboard` 运行时 import 与服务。实证：index.js:707 import server，server 服务 dist/）
packages/dashboard/src/
packages/dashboard/public/
packages/dashboard/index.html
packages/dashboard/vite.config.js
packages/dashboard/package-lock.json

# 平台模式本地调试残留（specRoot 指针，含本地绝对路径；git 不跟踪，若不排除就会进 npm 包）
.sillyspec-platform*

# ⚠️ 不要忽略 .claude/skills/ 和 templates/ ——
# sillyspec init 从这两个目录复制 skill 模板和 workflow 到用户项目（init.js:209,283）。

# .claude/CLAUDE.md 是项目级 Claude 指引（含开发者本地路径），不是工具运行时依赖，不进 npm 包。
# .claude/skills/ 仍保留（见上，init.js 复制 skill 模板用）。
.claude/CLAUDE.md

# 根 CLAUDE.md 同理：项目级 Claude 指引（含开发者本地绝对路径 C:\Users\...），非工具运行时依赖
# （sillyspec init 写「用户项目」的 CLAUDE.md，模板读包内 templates/claude-instruction.md，不读本文件），不进 npm 包。
# 锚定仓库根（/CLAUDE.md）避免误排子目录同名文件。
/CLAUDE.md
