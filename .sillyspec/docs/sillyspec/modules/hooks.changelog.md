ql-20260911-030-bad4 | guard 白名单收口：git branch 整类只读放行改按参数细化（-d/-D/-m/-M/-f 及长形进危险表——删改分支致 task review base/head 引用悬空）、git worktree 仅放行 list（remove/prune 进危险表）、dangerBlockHint 拦截原因带官方替代路径
- ql-20260912-001-ca16 | queryDbFirstCell 改进程内 createRequire 同步 node:sqlite（原每次 hook spawn node 子进程 100-300ms；实测 1.2ms），字符串/数值/null/fail-closed 语义全保持
