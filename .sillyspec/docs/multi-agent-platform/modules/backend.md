---
schema_version: 1
doc_type: module-card
module_id: backend
source_commit: ba87eec
author: qinyi
created_at: 2026-06-24T01:16:42
---
# 后端服务（backend）

## 定位

multi-agent-platform 的核心 API 服务，monorepo 的"大脑"。以 FastAPI 提供 REST/SSE/WebSocket 接口，承载多智能体协作平台全部领域逻辑：工作区管理、SillySpec 变更编排、Agent 运行时调度、PPM 项目管理、知识库、发布、权限治理等。是 frontend 与 sillyhub-daemon 的唯一数据后端，二者均通过 HTTP/WebSocket 调用它。

技术栈：Python 3.12+、FastAPI、SQLModel、Alembic（迁移）、PostgreSQL、Redis（缓存/会话）、httpx、uvicorn、uv（包管理）、pytest。代码组织为"核心 core + 按领域分 modules"两层结构。

## 契约摘要

对外契约是 `/api` 前缀的 HTTP 路由树。`app/main.py` 聚合 30+ 个领域路由，统一挂在 `/api` 下：

- 基础：health、auth、members、workspace、admin、settings、scan_docs
- SillySpec 编排：change、change_writer、task、workflow、archive、spec_workspace、release、knowledge、incident
- Agent/运行时：agent、runtime、daemon（守护进程接入）、lease（租约）、tool_gateway、policy（权限策略）。
- **AgentProfile 配置层**（2026-08-04-agent-profile-ui-redesign）：`agent` 模块 `profile/` 子域（agent_profiles 表 CRUD + 三级 visibility private/workspace/platform + 系统预置只读）；新增 `GET /api/agent-profiles?scope=mine` 跨工作区聚合只读端点（scope 省略=原 platform 级冻结 C8，前端选档下拉依赖），聚合视图逐档 `_can_read_async` 越权防护。**模型错误可见性**：daemon 回传的 `InteractiveRunResultRequest.error`（ModelErrorDTO）写入 `AgentRun.error_detail` JSON 列（与 `error_code` 正交）；`GET /api/daemon/sessions/{id}/runs` 返 SessionRunRead（含 error_detail）；会话 SSE 在 failed turn 推 `run_error` 事件。三端同构 ModelErrorDTO 由 gen:types 对齐（design §7.1）。
- Git：git_identity、git_gateway、worktree
- **工作区 Git 日志**（2026-08-25-workspace-git-log）：`git_log` 模块三 GET 端点——`GET /api/workspaces/{wid}/git-log/commits`（skip≤2000/limit≤200/branch/author 过滤）、`.../commits/{sha}`（详情+变更文件列表）、`.../commits/{sha}/diff?path=`（单文件 unified diff），统一 `require_permission(Permission.WORKSPACE_READ)` 门控；service 仿 explorer 链路——MemberBindingResolver 绑定解析 + `resolve_root_path_for_daemon` 路径改写 + `probe_workspace_git_mode` 三态映射（direct→`git_mode=no_git` 空态、unknown→按 offline 502），平名 RPC 直连 daemon `git_log`/`git_refs`/`git_show`/`git_diff_file`（显式超时，不走 `host_fs.` 前缀通道）；`graph_layout.compute_lanes` 确定性 lane 计算（每页对 skip+limit+lookahead=50 全前缀计算保跨页一致）；错误映射：未绑定/not_found→404、forbidden→403、offline→502、timeout→504、method_not_found（旧 daemon）→422；2026-08-26-workspace-git-status 增量：第 4 端点 `GET /api/workspaces/{wid}/git-log/status`（无 query 参数轻端点，`GitLogStatusResponse`）——分支/upstream/ahead-behind/未提交改动 ±/未跟踪计数 + 自动 fetch 结果（fetch 失败降级不阻断：`fetch.performed=false` + error 代号 + behind 为 stale 值，`synced_at` 为 backend 组装时刻），service 经平名 RPC 直连 daemon `git_status`（fetch 15s 超时降级 + `status --porcelain=v2 --branch` + `diff HEAD --numstat --no-renames` 改动计数单源）。
- LLM：llm_provider（LLM 供应商配置，含 `POST /api/llm-providers/{id}/usage` 用量/余额查询）
- **工作区自定义 Skills 完整文件编辑**（2026-08-26-workspace-skill-edit）：workspace 模块 skills 写路径 5 端点——`POST /workspaces/{wid}/skills`（名+描述→生成 `skills/<名>/SKILL.md` frontmatter，白名单 `^[A-Za-z0-9._-]+$` 拒 `..`，重名 409）/ `DELETE .../skills/{name}`（rmtree 经 to_thread，逐条目 symlink 拒绝防逃逸）/ `GET|PUT|DELETE .../skills/{name}/files/{path:path}`（文本限定 415/512KB 上限 413/SKILL.md 删除 409 入口保护/原子写 newline 固定 LF）；路径安全三重防线（段白名单+≤2 层+resolve/commonpath fail-closed，穿越变体 8+3 用例磁盘零接触）；每写操作手工 AuditLog（action=workspace_skill.*，details 只含 skill 名/路径）；daemon 零改动（skill-manager 经既有 spec sync 消费，页面提示「下次同步对新会话生效」）。
- **工作区 MCP 配置编辑与端到端注入**（2026-08-26-workspace-mcp-edit）：workspace 模块新增 `PUT /api/workspaces/{wid}/mcp-config`（WorkspaceWriter；仅 stdio 校验 `HTTP_422_MCP_TYPE_NOT_STDIO`、`<set>` 占位符服务端还原[磁盘同名 server 同名键取真值，不可还原 `HTTP_422_MCP_SECRET_UNRESOLVABLE`，绝不写盘]、同目录临时文件+os.replace 原子写 specDir/.mcp.json、手工 AuditLog+commit[纯文件写无 ORM 变更不触发 audit_hooks，action=workspace_mcp_config.update]、响应返回写后脱敏视图）；daemon 模块 `GET /api/daemon/mcp/config` 扩展可选 `workspace_id` query（响应追加 `workspace` 明文键，`_read_mcp_config_raw` 不脱敏直读，文件缺失/损坏返回空不抛；不带参响应与旧结构字节级同构）；lease/context.py 主控 ws 兜底（stage=='orchestrator' 且 metadata 无 workspace_id 时按 agent_run_id→AgentRun.mission_id→AgentMission.workspace_id NOT NULL 锚点解析补齐 execPayload.workspaceId 下发；分身 mission_worker/quick-chat 明确不补，O1-O4 守护测试）。
- **变更文件二进制读取端点**（2026-08-26-file-fullscreen-preview）：change 模块新增 `GET /api/workspaces/{wid}/changes/{cid}/files/raw?path=`——镜像文件原字节流（StreamingResponse + Content-Type=mimetypes.guess_type 未知回 octet-stream + Content-Disposition inline RFC5987 filename* + Content-Length 实际字节数），CHANGE_READ 门控与 files/content 一致；service 从 read_file 提取 `_resolve_change_file` 路径穿越守卫 helper（read_file/read_file_raw 共用，穿越/不存在 404 同款语义），`MAX_RAW_BYTES=50MB` 超限 413（先 stat 后 read 防超限整读进内存），读盘 asyncio.to_thread；read_file 对外契约零变更（文本 1MB 截断语义不动）。前端 fetchChangeFileRaw 消费（变更文件全屏预览/图片内联统一走此端点，D-009）。
- PPM 子树（统一前缀 `/api/ppm`）：project、plan、task、problem、kanban
- **SillySpec 进度同步层**（2026-08-10-sillyhub-platform-sync）：`platform_sync` 模块 3 端点——POST `/api/changes/{name}/progress`（上行 `serializeForSync` 六表裸 JSON + 读 3 个 `X-SillySpec-*` header[User/Base-Ts/Pushed-At，缺失/空均 None] + §4.2 base_ts 字典序乐观锁：无 base_ts 或 stored≤base_ts→接受 200 / stored>base_ts→409 body `{conflict,platform_progress,last_pushed_at}` 不 auto-merge）/ GET `/api/changes`（轻量列表裸数组 `[{name,current_stage,last_pushed_at,last_pusher}]`）/ GET `/api/changes/{name}/progress`（完整六表 JSON + 顶层 `last_pushed_at`，不存在 404）。Bearer=APIKey(`shk_live_`)/JWT 双路径鉴权（复用 `ApiKeyService.authenticate`+`get_current_user`）；`platform_change_progress` 表单行存 `latest_progress`(JSON)+`last_pushed_at`/`last_pusher`；裸 dict 透传（NG-6 不强类型校验）；铁律不碰派发层（D-004，与 `/api/workspaces/{wid}/changes/*` 正交，无路径/数据重叠）。跨仓契约见 `sillyspec/docs/sillyspec/sillyhub-progress-sync-contract.md`。2026-08-17-spec-file-incremental-sync 增 CLI 直跑文件树增量 2 端点（路由 `-` 占位段避开 `{name}` 贪婪匹配）：GET `/api/changes/-/spec-manifest`（`SpecFileManifest` 全表清单 `files: {path: {hash, version, exists}}`，含软删行 exists=false；读清单也要求 `require_platform_sync_write` 仅 shpsync_——清单是增量写协议基线，防非同步方探测布局；workspace 从 token 派生，无归属 403 fail-closed）/ POST `/api/changes/-/spec-sync`（body `ops: list[FileOp]` add/update/delete/rename + base_version 乐观锁，单事务透调 `spec_workspace` `apply_ops`；冲突返回 `conflict=true` + `server_versions` 指向冲突文件，不 auto-merge；ql-20260818-002：`local.yaml`（机器本地连接配置，含 shpsync_/shmcp_ token）服务器侧三处统一过滤（`spec_workspace/service.py` `SERVER_EXCLUDED_FILENAMES`）——apply_ops 拒 add/update/rename-to 静默丢弃（delete 放行清存量行）/ tar 解包跳过该成员 / build_bundle 导出跳过；CLI `spec-sync.js` 侧 walk 同步排除（双侧对齐，服务器清单残留行由 delete op 清理），token 不落 landing 树、不随 bundle 跨机分发。
- **会话 reopen/resume 链路加固**（2026-08-21-session-reopen-resume）：daemon 模块 `confirm-reconnected` / `mark-recovery-failed` 请求体 `SessionRuntimeRequest` 新增**可选 lease_id**——与会话当前挂起 lease 不匹配时幂等跳过（防陈旧确认误翻，DS-4），未提供保持现状向后兼容（旧 daemon 不受影响）；active→failed 翻转保留。**reopen 窗口语义**：reconnecting 且 `last_active_at` 超 `RECONNECTING_RETRY_WINDOW_SEC=180`（session/service.py）放行重开，旧挂起 lease 置 cancelled、新建 lease 重发 SESSION_RESUME；cwd 空拒绝时返回 409 中文文案（DaemonSessionNoCwd）。**lifespan 常驻巡检协程**：`session_reconnect_sweeper`（sweep.py，60s 周期，main.py lifespan 挂载）把 reconnecting 超时会话收敛 failed + 挂起 lease 置 cancelled，同时覆盖旧 daemon 不发 confirm 的过渡期。配套迁移 `20260821120000_backfill_session_agent_session_id` 回填 AgentSession.agent_session_id。

- **机器 sillyspec 进度总览快照**（2026-09-02-changes-overview-card）：daemon_instances 加 sillyspec_status JSON 列（迁移 20260903090000）；心跳载荷 sillyspec_status（宽松全可选 DTO，宁宽勿断不 422）None=清除置 NULL（语义同 sillyspec_update）+ register 双分支恒清（快照随进程重启失效）；GET /api/daemon/machines 机器视图嵌套透出（MachineSillySpecStatusRead，NULL=CLI 能力缺失/版本过低）。落库形态=上报形态（32KB 预算与 changes N=50 截断在 daemon 侧执行）。

- **SillySpec 冲突对比端点**（2026-09-07-conflict-diff-compare）：daemon 模块新增 `GET /api/daemon/machines/{instance_id}/sillyspec-conflicts/{change}/compare?kind=spec-tree|progress&workspace_id=<uuid>`（router.py :1621 `compare_machine_sillyspec_conflict`）——权限同裁决端点（RuntimeAdminUser + `_get_owned_instance` 越权 404）另校验当前用户是 workspace_id 成员（compare 数据与裁决同一权限集合，Grill B1）；change 路径段白名单 `_validate_sillyspec_change_segment`（:1603，resolve 端点同款正则 + 显式拒 `..`，422 早于任何 RPC 外呼）；RPC 腿 `send_rpc('sillyspec_conflict_snapshot')` 显式 15s 超时（send_rpc 默认 10s 不够 183 文件级快照），机器离线/超时走既有 504 家族（DaemonRuntimeOffline / DaemonRpcTimeout 原样上抛），daemon 业务失败（no_spec_root 等）经 DaemonRpcRemoteError 映射 502（DaemonRpcRemoteGatewayError）。编排全在新文件 `sillyspec_compare.py`（SillySpecCompareService）：workspace 成员校验后 asyncio.gather 并行「RPC 快照 + 平台侧定位」；spec-tree 平台侧逐路径 containment 校验（拒绝对路径/盘符/`..` 段/resolve 落点越出 spec_root——daemon 是半可信端，spec_workspace 同款范式；越界按平台侧缺失处理不读取）+ difflib.SequenceMatcher（autojunk=False）服务端 diff 出对齐行（replace 段展开相邻 delete+insert）+ 四分类 modified/local_only/platform_only/identical + 双侧均缺失路径剔除计数 dropped_paths + 本地截断/二进制无 content 不出 diff_rows（防全 insert 方向信号失真）；截断护栏单文件 diff ≤5000 行（置 diff_truncated）+ 整响应 JSON ≤2MB 超限按文件倒序丢 diff_rows（置 response_truncated，_json_size 与 starlette JSONResponse 序列化逐字节一致）；progress 白名单六字段（当前阶段/阶段标签/步骤进度/最近活跃/ql_id/ghost）归一化对比行缺失显式「—」，platform_updated_at=last_pushed_at。心跳 `DaemonHeartbeatSillySpecConflict`（:313）加可选 `ql_id: str | None`（:329，QUICKLOG 编号，daemon 对 quick-* 名 best-effort 补报）——零改写透传语义不变，旧 daemon 不上报不影响心跳落库。测试 tests/test_sillyspec_compare.py。

启动入口 `uvicorn app.main:app`，带 `lifespan` 钩子（初始化/释放 DB 引擎、Redis、遥测）。`app = FastAPI(...)` 实例在 `main.py` 构建，装配 CORS 中间件与全局异常处理器（`core/errors.register_exception_handlers`）。

## 关键逻辑

- **分层结构**：`app/core/`（config/db/redis/security/crypto/logging/telemetry/audit_hooks/spec_paths 等横切关注）+ `app/models/base.py`（SQLModel 基类）+ `app/modules/<域>/`（每域含 `router.py` + 业务/service + tests）。
- **领域模块清单**：admin、agent、archive、auth、change、change_writer、daemon、git_gateway、git_identity、git_log（2026-08-25-workspace-git-log）、health、incident、knowledge、ppm(5 子域)、platform_sync、release、runtime、scan_docs、settings、spec_profile、spec_workspace、task、tool_gateway、workflow、workspace、worktree。
- **Daemon 接入**：daemon 模块与 lease 模块共同支撑本地守护进程注册、领租约、心跳、消息回传的在线交互模型。**模型错误回传**：daemon 在 turn 失败时归类出 ModelError（auth_failed/quota_exceeded[不可重试]/rate_limited[可重试]/timeout/model_not_found/network/provider_error/unknown），经 notifyRunResult → close_interactive_run 三层透传（router+facade+实体）写 AgentRun.error_detail，run→failed。**run 终态翻会话终态补发 SESSION_END**（ql-20260823-006）：`close_interactive_run` 经 `_apply_session_terminal_status` 把会话翻 ended/failed 时（change 绑定会话每轮 run 终态都翻），commit 后经模块级 `_send_session_end_best_effort`（session/service.py，复用 `_resolve_daemon_id_for_runtime` 路由）best-effort 补发 SESSION_END 清理 daemon 内存 SessionStore 副本——此前只翻 DB 不通知 daemon，副本残留成僵尸，后续 reopen 全撞 SESSION_ALREADY_EXISTS 死循环（2026-08-23 bdec91a4 事故）；多轮对话保持 active 不发（防误杀活会话）。**孤儿 dialog 作废（ql-20260815-003）**：run 终止后其 pending `session_dialog_requests` 卡片由恢复路径作废——`cancel_pending_dialogs_for_run`（permission_service 模块级 helper，pending→cancelled、不自带 commit）在 `_cleanup_stale_runs_impl`（后端重启清 stale run）与 `_converge_crashed_run`（daemon 重启收敛）中调用；读侧兜底：session 级 `list_pending_dialogs` 与 workspace 级 `list_pending_dialogs_for_workspace` 均 join AgentRun 过滤非 ACTIVE_TURN_STATUSES 的孤儿卡（`list_dialog_history` 历史全量不受影响）；`respond_permission` 把 dialog 行解析提前到 current_run 检查前，终态卡直接返回明确 409/404（不再报笼统 no active run）。
- **迁移与建表**：Alembic（`migrations/`）+ `create_tables.py` 兜底；`core/layout_migration.py` 处理 SillySpec Native Layout 演进。
- **测试**：`backend/tests/` + 各模块内 `tests/`；CI 要求 `--cov-fail-under=60`。
- ql-20260812-002-7ca3 | 后端测试提速：全量 pytest 12min→4min（711s→245s，3800+ 用例零回归）。①daemon session 集成测试（test_session_service / test_interactive_lifecycle_patch / test_change_session / test_session_router / test_session_user_log）每个正常路径用例都在 `session/service.py` create/inject 硬编码 `readiness.wait(timeout=30)` 真等 30s（create+inject 串 60s），全量 slowest15 全在此、共 ~570s；新建 `app/modules/daemon/tests/conftest.py` autouse 打桩（`wait` 即时返 True、保留 mark_ready/clear，排除专测 readiness 语义的 test_session_readiness）→ 该 5 文件 570s→14.8s。②pre-existing 10 ERROR 清零：member_runtimes 单独跑/xdist 部分 worker 因 selected-metadata 缺 llm_providers 抛 NoReferencedTableError —— `member_runtimes/tests/conftest.py` 补 import llm_provider + needed 集合加 llm_providers（自洽修复，不依赖根 conftest）。
- ql-20260812-003-0632 | 后端测试二轮提速：全量 pytest 203s→121s（4min→2min，3848 passed 0 errors）。①`test_session_readiness` 两个 30s（TestInjectWaitsForReady / TestInjectTimeoutFallback）：wait 打桩挪到 `create_session` 前（原 mark/patch 在 create 之后，create 已白等 30s）。②`test_session_permissions` 两个 30s：直接 `await _on_timeout`，其内部 `asyncio.sleep(_timeout_sec=30.0)`→`timeout_sec=0.01`（仅 2 处，其余 18 处 fire-and-forget 不真等、未动）。③`ws_hub.send_rpc` 默认参数 `timeout: float=RPC_DEFAULT_TIMEOUT`（import 时绑定旧值致 monkeypatch 无效，test_list_dir_504 等满 10s）→ `timeout: float|None=None` + 调用时动态读常量。④`test_lease_service` TestCompleteLeaseWebhook 两测试 mock `_trigger_stage_completion_callback`（HostFsDelegate RPC 无 daemon 超时）。全量 203s→121~128s 零回归；一次全量偶发 1 flaky（test_provider_switch test_batch_lease_sessions_not_pushed，单独过 + 重跑全量过，与本次改动无关）。
- ql-20260812-004-f361 | 后端测试三轮收尾：`TestCompleteLeaseWebhook` 两 completed run 测试（fires_deliver/dispatcher_raises）慢 3-6s，根因=mission converge 副作用（`converge_mission_for_completed_run` 内 FinalizerService 经 `new_host_fs_delegate` 调 `git_merge` RPC 无 daemon 真等 + `_glm_merge` httpx 120s timeout；`worker_failed` 是 failed 不走 finalize 故 0.02s）→ 两测试 monkeypatch converge 为 noop，4 测试 9s→1.6s。另修 `test_orchestrator` 同根偶发慢：`schedule_loop` 末尾真跑 converge（RPC/httpx 等待），单跑 8~34s 随机、拖垮全量墙钟——module 级 autouse patch `finalizer.converge_mission_for_completed_run`（返 done，断言分支不变），13 测试稳定 2.8s。`test_provider_switch` 偶发 flaky：代码审查无共享 DB/redis 泄漏（per-worker in-memory sqlite + 随机 user_id），并发跑 5 次未复现，不猜测改码。全量 3855 passed 0 errors 115.85s（首次 <120s）。
- ql-20260812-008-c860 | 测试质量审查 P0 修复（6 维度审查后）：①worktree `test_router.py` 补 7 个安全/完整性分支测试（revoked/expired git identity→503、cross-user extend→403、已释放 release/extend→409、no repo_url→503、文件系统失败 rollback+cleanup），原仅 happy path 删校验全量仍绿；顺修 `backend/app/modules/worktree/service.py` tz 不健壮（SQLite 读回 naive datetime 与 `now(UTC)` aware 比 TypeError，生产 Postgres 不触发——测试暴露的真实缺陷）。②e2e SC-5 `test_three_member_collaboration:367` except pass 空断言→spy `resolve_runtime_for_writeback` 断言 member-binding 路径被走（原即使回归读 deprecated daemon_runtime_id 只要抛 DaemonClientNoActiveSession 测试仍绿）。③`test_allowed_roots_policy_push` wall-clock flaky（`time.sleep(0.005)` 推单调 version，Windows 时钟粒度下偶发）→ patch `_derive_policy_version` 注入可控递增 version 源。④顺补预存债 `backend/app/modules/change_writer/tests/test_proxy.py:28` current_stage draft→brainstorm（proxy ql-006 已改 brainstorm，测试断言漏改）。全量 3867 passed；P0 五项各自单测通过零回归；另 1 failed（test_provider_switch xdist 偶发 flaky，单独过，前几轮已知）+ 1 error（test_bootstrap teardown，并发会话的 conftest 后台 task 隔离 fixture）均非本次引入。
- ql-20260812-009-976d | 测试质量审查 P1 批隔离/flaky 加固（ql-008 P0 的后续，纯测试基建零生产逻辑改动）：①根 `conftest.py` 加 `_isolate_background_tasks` autouse fixture——每测试 clear + teardown cancel/await 四处 fire-and-forget task 强引用集（`spec_workspace.bootstrap._BACKGROUND_BOOTSTRAP_TASKS` / `AgentService._background_tasks` / `ExecutionCoordinatorService._background_tasks` / `RunSyncService._background_tasks`，与既有 `_isolate_permission_timers` 同款坑）；teardown 用 `isinstance(t, asyncio.Task)` 守卫，跳过 `test_bootstrap_provider_model` 注入的 `_FakeTask`（无 cancel）——**即 ql-008 提到的 test_bootstrap teardown error 的根治**。②redis 跨 worker 分库：`PYTEST_XDIST_WORKER` gw0..gw15→db0..db15（`%16`，非 xdist 保持 db15），各 worker function 级 FLUSHDB 只清自己库，消共享 db15 互清 login:fail/captcha 的并发 flaky（redis 仍 best-effort 不可用降级）。③`_reset_lazy_singletons` autouse 每测试置 `storage.factory._backend`/`core.db._engine` 为 None（防懒加载单例泄漏，当前测试路径不触达，护栏性）。④`spec_data_root` 改会话级独立子目录（按 worker/pid）+ atexit rmtree，治跨运行 temp 堆积。⑤`mcp_gateway/tests/test_webhook.py` 补 `test_deliver_4xx_abandoned_no_retry`（4xx 只投 1 次不重试，防误改走重试触发 5×[1+4+16+64]s≈85s 退避占 worker 的回归）。全量 `pytest -n auto` 3868 passed/0 fail/5 skipped/5 xfail、ruff+mypy 干净。
- ql-20260813-001-f9af | 测试质量审查 P2（重构脆裂，纯测试风格零行为改动）：`permission_service` 加公共 `has_pending(request_id)`，`test_ws_hub_permission` 5 处断言/清理从私有 `_timers` 改用 `has_pending` + 去冗余手工 task cancel（依赖 conftest `_isolate_permission_timers` teardown）——重构 `_permission_timers` 数据结构（dict→对象）时测试不脆裂。P2 其余项评估后不改：①vitest clearMocks 已由 ql-009 P1 加（且 restoreMocks 被论证不加，会破坏 21 个 beforeAll 持久 spy）；②test_delegate_injection 是 DI 接线测试，isinstance/非None 是类型契约非脆裂；③test_ws_rpc 冲突/already-done 边缘 case 无公共 API，直接操作内部必要；④_max_conflict_attempts/_bucket_unit 是 deliberate test seam / 已有公共测试覆盖；⑤test_policy check / test_permission_cache 的 guard/silent 降级契约"不抛即过"合理；⑥password_hasher restore 良性不值得改 fixture 结构。全量 3868 passed 0 failed 0 error。
- ql-20260813-002-cf43 | 修复全量 pytest 9 failed+9 error（xdist 间歇 flaky）：①根因=xdist 默认 `load` 分发（按测试 round-robin）致跨文件状态污染，task/change/runtime/workspace 的 spec reparse/扫描间歇得 0（`created=0` → fixture `next()` StopIteration / 文件列表空）→ `pyproject` addopts 加 `-o dist=loadscope`（按模块/类分组到 worker），消除跨模块污染（9→0~2）。②残留 task reparse 间歇=Windows Defender 瞬时锁 copytree 新文件致首扫 `parsed=0` → `task/tests/test_router.py` 的 `workspace_with_tasks` fixture 加一次 reparse retry（锁瞬时，重扫恢复；正常路径 items 非空不触发）。连续两次全量 3868 passed 0 failed 0 error 稳定。
- ql-20260822-002-2dcb | 后端测试提速（Redis 停机防护 + verify 模块命令并行）：①根 `conftest.py` `_reset_redis_state` autouse 每测试 `flushdb`，Redis 停机时 localhost 连接失败不是立即拒绝而是等满超时（Windows 实测 ~2s，setup 实测 ~3.2s/用例）——agent 模块 632 用例裸串行 34.8min 中 CPU 仅 ~90s、slowest durations 清一色 setup；新增 `_probe_redis_once()`（进程内一次 0.5s 短超时 ping，xdist 每 worker 各探测一次，失败则本会话全部跳过 flushdb；redis 可用路径逐测试 FLUSHDB 行为不变）→ 串行 34.8min→2:39。②`.sillyspec/local.yaml` modules 块 12 个 backend 条目 test 命令加 `-n auto`（此前模块命令裸串行，07-23 xdist / 08-12 readiness 打桩等优化只覆盖手动全量跑，verify/子代理走的模块命令从未并行）→ agent 模块 632 passed / 44.4s 零 flaky（`dist=loadscope` 兜底跨文件污染）。
- ql-20260822-003-a265 | verify gate 全量命令并行收尾：`.sillyspec/local.yaml` 顶层 `commands.test` 的 backend 段加 `-n auto`（ql-20260822-002 只覆盖 modules 块 12 条子模块命令，顶层全量命令仍串行，08-21 verify 实测 936.69s）→ backend 全量 4771 passed / 6 skipped / 3 xfailed，356.97s（0:05:56）零 failed 零 flaky（Redis 停机状态下跑出，conftest 会话级探测在 xdist 每 worker 生效；Redis 起回后应更快）。文件头部坑2 过期注释同步改「已解」。



## 注意事项

- **CLI 上行 naive 时间戳解释时区（ql-20260822-006）**：sillyspec CLI 上行的 `steps.completed_at`/`stages.started_at` 是 CLI 宿主机墙钟（`toLocaleString('zh-CN')`，无时区标记），后端归一到 UTC 一律按 `settings.cli_progress_timezone`（默认 Asia/Shanghai，env `CLI_PROGRESS_TIMEZONE`）解释——**禁止用 `naive.astimezone()` 随进程时区走**（Docker 容器 UTC ≠ CLI 宿主机时区，历史上致步骤时间线整体偏 8 小时）。解析入口 `ChangeService._normalize_completed_at`，时区串解析 `core/config.resolve_cli_tzinfo`（IANA 名或 ±HH:MM）。同类 naive 串新消费点出现时应复用同一配置，不要再发明第二种解释。
- 改动 backend 必须实测 API（curl 打端点），不能只靠 tsc/mypy，历史上出现过运行时未导入符号导致 500 的案例。
- Docker 部署时 backend 容器跑镜像内代码、不热重载，改源码后需 rebuild 镜像再验。
- 路由前缀约定：绝大多数在 `/api`，PPM 走 `/api/ppm`；新增模块要在 `main.py` 显式 `include_router`。
- 提交前需跑 `backend/.venv/bin/ruff format` 处理 staged 文件，否则 pre-commit hook 拦截。
- **scan 命令路径加引号**：`build_scan_bundle` 生成 sillyspec 命令（init/scan start/scan done）时 `--dir` 路径必须双引号包裹，防 Windows 反斜杠路径在 Git Bash 无引号时被转义吃掉（`C:\Users` 的 `\U` 被吞 → 路径变形/目录不存在）。
- **daemon-client spec 同步契约（D-005@v1，2026-06-26-daemon-client-spec-sync-fix）**：`platform-managed` workspace（`spec_workspaces.strategy`）的 spec_root 是扁平 `.sillyspec` 内容根，reader 经 `SpecPathResolver.for_spec_workspace(spec_ws)` 选 mode；`apply_sync` 接收 daemon tar 含 `.runtime`（push 非对称，pull `build_bundle` 仍排除）+ 落 `last_synced_at`/`sync_status=clean`；change-write 经 `daemon_change_writes` 表 lease-polling（`change_writer.proxy_create_change` + daemon `pending-change-writes`/`claim`/`complete` 三端点 + 60s 超时 gc），无在线 daemon 抛 `DaemonClientNoActiveSession`(400 `DAEMON_CLIENT_NO_SESSION`)。
- **runtime 实时读取链路（2026-08-19-runtime-live-daemon-read）**：runtime 模块 5 端点（`/runtime`、`/runtime/user-inputs[/raw]`、`/runtime/artifacts[/{filename}]`）数据源从容器直读 `spec_ws.spec_root` 快照切换为经绑定 daemon WS RPC 实时读取——`RuntimeLiveService` 经 `MemberBindingResolver.resolve_member_binding_or_none`（用户门控，D-004@v1）解析 daemon_id 后 `ws_hub.send_rpc` 调 `runtime.*` 四方法（独立命名空间，D-005@v1），35s 显式超时；daemon 失败不回退快照直接报错（D-001@v1）。进度经 sillyspec CLI 只读命令（D-002@v1，daemon spawn `sillyspec progress dump --spec-dir <specCacheRoot> --json`，输出 snake_case+ISO 契约）；用户输入/产物由 daemon 直读 `.runtime/` 下文件（D-003@v1）。错误映射 9 个 `Runtime*` 子类（离线 502/在途断连 502/超时 504/forbidden 403/not_found 404/版本过旧 method_not_found 422/产物超 1MB 413/其余 502），filename 预检（空/控制字符/绝对路径/../子路径）先于 RPC。旧 `RuntimeService` 快照读法整体删除。
- **SillySpec 进度同步契约（2026-08-10-sillyhub-platform-sync）**：`platform_sync` router **不自带 prefix**（路径写全 `/changes/...`，main 挂 `/api` 落地 `/api/changes/...`）——避开 FastAPI 对 `GET /changes` 的尾斜杠 307 redirect（客户端 `sync.js` 打无尾斜杠 `/api/changes`）。POST 409 必用 `JSONResponse` 绕过 `response_model` 校验，确保 body 严格按契约 §4.4（`{conflict,platform_progress,last_pushed_at}`，`platform_progress` 是平台当前 `latest_progress` 原样回显，**不 auto-merge** 客户端 body）。base_ts 比对用 ISO 8601 UTC **字符串字典序**（不转 `datetime`，契约 §7：字典序==时间序）。铁律 D-004：进度同步层永不触碰 `/api/workspaces/{wid}/changes/*` 派发层（无路径/数据重叠）。gen:types 须在主仓跑（worktree + editable install 坑：worktree 跑 `dump_openapi.py` 会加载主仓 `app.main` 不含本改动，须 `PYTHONPATH=<worktree>/backend` 或主仓合并后跑）。
- **进度首推占位建行 + reparse 镜像滞后保护（ql-20260815-002）**：`PlatformSyncService._ensure_change_row`——接受分支（首推/常规接受）后确保 `ux_changes` 有占位行（变更中心以 ux_changes 为主表 join 进度表，只写 `platform_change_progress` 会让镜像 tar 滞后期间新建变更在界面不可见）；`workspace_id=None`（shk_live_/JWT 全局 fallback）跳过、幂等、savepoint 撞 `ux_changes_workspace_key` 静默放弃、best-effort 不阻断上行。配套 `ChangeService.reparse` 全量删除环保护：CLI 最近上行仍报 `status=active` 且**无任何文档**的占位行不删（`_progress_reported_active_keys`，查询失败降级空集=无保护）；有文档的行仍以镜像磁盘为权威删除（防 stale progress 行让本地已删变更永生）。

## 人工备注
<!-- MANUAL_NOTES_START -->
- **change 2026-08-22-team-session-unify**（会话内团队操作，团队并入统一会话页）：agent+daemon 模块核心改造。数据：AgentMission 加 `session_id` 列（FK agent_sessions.id，活跃态部分唯一索引 `uq_agent_missions_session_active` 防并发双懒建；迁移 `20260822090000_mission_session_id` 清库重建无回填）；分身派发 lease stage 常量化 `'mission_worker'`（run.role 移 lease metadata 保留语义）。**主控轮双标记（D-009）**：会话存在活跃 mission 时 inject 当轮 AgentRun 回填 `mission_id`+`role='orchestrator'`——统一锚定 `_get_main_run`/finalizer（取**最新** orchestrator run，存量 external 链路同规则兼容）与治理门/workers 列表/成本统计（查询收窄 `role!='orchestrator'`，主控轮不占 MAX_WORKERS=5 并发额度与分身成本）。**derive_status 扩展 awaiting_input 档（CC-01）**：主控轮+分身全终态、未 converge、无会话活跃 turn 且 session_id 非空 → awaiting_input；存量 external mission（session_id NULL）不进此档，complete_lease 自动收敛（依赖 derive∈{done,degraded}）零回归。**converge 语义重定义（D-010）**：按 X-Session-Id 解析 mission；分身未全终态返回 busy 引导（agent 等待重试）；全终态独立置位 `converged_at`（不依赖主控 run 状态），finalize/合并锚点=该 mission 最新 orchestrator run。**懒建兜底（B1）**：dispatch_worker 无活跃 mission 时按会话绑定工作区懒建（未绑定 422 提示走弹层显式选 scope；并发守卫=会话行 FOR UPDATE + 部分唯一索引；懒建时将当前活跃 run 补回填双标记，保懒建 mission 也有主控轮锚点）。**端点增删（D-011）**：新增 `POST /api/daemon/sessions/{id}/team-mission`（预建+scope 冻结快照+objective 占位 `SESSION_OBJECTIVE_PLACEHOLDER` 首条 inject 回填、活跃冲突 409 R-07）与 `GET /api/daemon/sessions/{id}/team-missions`（TeamTaskBlock 数据源，TeamMissionSummary DTO）；删 workspace/project 维度 create+list 四端点与独占 helper；**保留** `GET /api/missions/{id}`/`POST /api/missions/{id}/cancel`（change 详情 team-progress 在用）；MCP 五工具新增 `/api/sessions/{session_id}/missions/*` 治理口径路由族（旧 mission_id/workspace_id 显式路由保留，router include 前移遮蔽修复）。**patrol 适配（CC-08）**：僵尸判定改会话维度（分身非终态+主控会话无活跃 turn+超时）、awaiting_input 超时 `mission_patrol_awaiting_input_timeout_minutes`（默认 30 分钟）自动 converge、schedule_loop 三重收敛信号按 session 维度判定、redispatch_pending_main_runs 对新链路 no-op（存量行为保留）。`render_orchestrator_prompt` 下线；`team_mission_entry` 重构为预建入口（不再另起主控 AgentRun/lease）。gen:types 已同步 openapi.json（394 paths/462 schemas）。
- change 2026-08-21-mission-converge-patrol | mission 收敛巡检：新增 `agent/patrol.py`（MissionPatrolService 三职责 + mission_patrol_loop 常驻协程，main.py lifespan 接线：enabled 开关 create_task + finally cancel/gather）。①收敛兜底：活跃 team mission（converged/cancelled NULL，limit 100）逐个 schedule_loop——修项目维度 mission（change_id=None 回调短路）主 agent 不收敛时永久 running；②离线重派：每轮 redispatch_pending_main_runs（补启动时重派的运行中场景）；③两阶段僵尸：主 run running+有 lease+daemon 持续离线（last_heartbeat_at 判起点）超 60min → failed+orchestrator_zombie+constraints.zombie_marked_at 不收敛（schedule_loop 信号1豁免窗口 30min，信号3不豁免），窗口内 daemon 回线自动复活（翻 running+重渲染 prompt 重派，NoOnlineDaemonError 回滚），窗口耗尽标 zombie_converged 正常收敛。配置四项 mission_patrol_*（config.py）。测试：test_patrol.py 32 + test_orchestrator.py 豁免 7；agent+daemon 全量 1443 passed，mypy 0。
- ql-20260821-002（项目团队 mission 全链路审查修复，docs/project-team-mission-review-2026-08-21.md）| 后端 9 项：①cancel/get/list_mission 归属校验（`router.py _require_mission_access`——cancel 原依赖 path 参数解析缺陷恒 422 实测复现；get/list 原可跨 ws 越权读写）；②dispatch_worker 跨 ws target 权限校验（JWT 通道要求对 target 有 workspace:write，daemon apiKey 通道豁免=主 agent 编排 D-006；链路B mcp_gateway 同款校验 token 属主）；③execution 捕获 HostFsDelegateUnavailable → run failed+error_code=hostfs_unavailable（契约 503→201+终态，mission 不挂死）；④finalizer converge claim 后 finalize 失败回滚 converged_at（merge 可重试）；⑤cleanup 过滤扩全部终态 run + converge_mission_for_completed_run 全 merged 路径接 cleanup（消 worktree 副本泄漏，conflict 仍保留 X-003）；⑥orchestrator 在线判定改传 binding 属主 user_id（原全零 UUID 恒离线误导主 agent）；⑦`redispatch_pending_main_runs` 启动重派 no_online_daemon 主 run（main.py lifespan 接线）；⑧治理拒绝先 gate 后建 run（拒绝抛 400 带 reason，不再产生 killed run 污染 derive_status）；⑨mission_schema 输入上限（objective 8000/budget ge=0/worker_preset 20/scope 20）+ `_sanitize_constraints` 剥状态机保留键 + project_id 索引（migration 20260821100000）。新增 test_mission_access_control.py 8 用例；agent+mcp_gateway+daemon 1518 passed；gen:types 已同步。
- 2026-08-20-runtime-readpoint-repo-first | runtime 模块四个只读 RPC 转发（read_progress/read_user_inputs/list_artifacts/read_artifact）params 增可选 `root_path`：`RuntimeLiveService._resolve_binding` 返回扩为 `(daemon_id, root_path)`（私有方法仅同类 4 调用点），值经 `resolve_root_path_for_daemon` 容器→宿主改写。RPC 方法名/响应形状/错误映射/35s 超时零改动——老 daemon 忽略新键自然兼容（FR-03）。测试：test_live_service 四方法精确 params 断言 + 前缀改写用例 + test_router.py 精确断言同步（44 passed）。
- **2026-08-19-cross-workspace-team-mission**（跨工作区团队执行 + 项目维度会话）：backend `agent` 子域核心改造。数据：AgentMission 新增 `project_id` / `scope_workspace_ids`（JSON），AgentRun 新增 `target_workspace_id`；迁移 `20260819100000_mission_cross_workspace.py`。派发：`member_runtimes/queries.py` 新增 `resolve_representative_binding`（owner 在线优先→任意在线）；`placement.py` 新增 `representative_fallback` 旗标；`execution.py` 按 `target_workspace_id` 路由并把显式 target 回写 run 列；主 agent 路径单 ws 零回归（target=NULL 走 anchor + representative_fallback=False）。MCP 双通道：`agent/mcp_tools.py` 与 `mcp_gateway/tools.py` 同步放宽 scope 校验、透传 target、保持 NULL 语义。收敛：`finalizer.py` merge/cleanup 按 `(target_workspace_id or anchor, worktree_branch)` 分组，A 组冲突不挡 B 组。项目入口：`agent/router.py` 新增 `POST/GET /api/projects/{project_id}/missions`（scope⊆项目 422、anchor∈scope 422、项目经理/超管鉴权、anchor 缺省 type=backend-code 优先）。测试：新增 `test_integration_cross_workspace.py` / `test_mcp_tools_cross_workspace.py`；agent 模块全量 586 passed（1 预存债），mypy 0 issues。
- **2026-08-12-init-provision-local-yaml**（init 自动下发 local.yaml）：工作区 init 流程扩展——`build_claim_payload`（daemon/lease/context.py mode=='init' 分支）claim 时调 `PlatformSyncTokenService.get_or_issue` + `McpTokenService.get_or_issue`（两 service 新增方法，吊销旧+签新）签发 `shpsync_`+`shmcp_`(scope=dispatch) 明文，注入 `payload.platform_config.local_yaml`（**明文不落 lease.metadata_**，P0/D-002——daemon_task_leases.metadata_ 持久化+进审计）。daemon 收到后 writeLocalYaml 写成员本地 .sillyspec/local.yaml（platform 段覆盖/mcp 段有才留），写失败→ok:false→lease failed（D-003 严格契约）。点完初始化进度同步+MCP 接入即可用，不用手跑 connect。dispatch 阶段（start_init_dispatch）零行为改动（B1：token 在 claim 时签），仅 actor_user_id 落 metadata 供 claim 解析 created_by。
## 变更索引
- ql-20260917-007-7cec | 工作区列表平台级授权口径对齐（workspace 模块）：GET /api/workspaces 非管理员分支新增平台级授权判定——`collect_permissions_platform` 持 `workspace:read` 或 `platform:admin` 时按全量工作区返回（`allowed_workspace_ids=None`，对齐 is_platform_admin 分支），否则维持工作区级 `allowed_workspace_ids` 限定。修「列表看不到工作区却能收其待审核通知（广播收件人查找段 2 认平台级授权）、点进其内容（has_permission 段 2 认平台级授权）」的三处口径割裂（180593 持平台级 super_admin 角色实证：通知收得到、内容进得去、列表为空）。原 task-05 FR-02「平台级 workspace:read 也见空列表」边界自此作废。测试：新增 test_platform_grant_list.py 5 用例（平台级 workspace:read 全量可见/platform:admin 全量可见/工作区级成员仅见本区/无读权限平台角色 403/无角色 403）+ test_workspace_admin_management 边界用例改锁新语义（sees_empty→sees_all）；workspace+notification+auth(rbac) 518 passed（1 skip=Windows symlink 特权既有、2 xfail 既有），ruff/mypy/format 0。
- ql-20260911-003-355a（24h 审查风险修复批·backend）| ① MCP 资产库 P0-1：workspace-scan/workspace-import-apply 补工作区成员门（importer `_visible_workspace_ids`——平台 admin 放行全部，非 admin = rbac.allowed_workspace_ids(WORKSPACE_READ)；指定他人 ws 403、apply 夹带非可见候选 403 fail-fast）。② P0-2：McpServerCreate/Update 增 `secret_env_keys` 用户自定义密钥指定态（不再按键名子串自动判定；导入路径物化缺省建议）；编辑占位语义——更新时密钥键值 `<set>` = 保留既有密文（创建/新键/明文占位 422 McpServerSecretEnvInvalid）；只改指定态支持升级加密/降级解密；读侧 McpServerRead/Detail 回显 secret_env_keys、env 视图只含明文键。③ P2：get_daemon_status quota_pool——effective_agent 经 `_normalize_lease_provider` 归一（claude_code→claude，default_agent=claude_code 不再恒 miss）+ bindings/runtime/pool 三查询 ORDER BY 确定序（created_at 插入序锚）；scope_audit file-diff RPC 超时 35s→135s（≥ daemon 命令超时 120s，原写反）+ 未映射 daemon 错误码 remote_message 只进日志不随 details 下发；drain_reparse_workers 增 timeout_seconds 有界等待（默认 15s，超时记日志返回 False 不 cancel）；users.avatar 入库前 scheme/形态校验（/api/file/ 前缀或 http(s)，422 HTTP_422_AUTH_AVATAR_INVALID）；mcp-tokens gateway_url 仅取 MCP_GATEWAY_PUBLIC_BASE_URL 显式配置（未配置 null，不再信任 X-Forwarded-*/Host 推导）。④ mcp_templates 增 secret_env_keys 列 + 预置位 name 部分唯一索引（并发双 seed DB 兜底）——迁移 20260911010000。测试：mcp_registry 166（含成员门 4 反例/占位语义 9/指定态回显）+ change 516 + mcp_gateway 133 + spec_workspace drain 4 + auth avatar 9，mypy/ruff/format 0；openapi+两份 api-types 已 gen:types。
- ql-20260911-001-c0be | 对账表端点 GET /workspaces/{ws}/sillyspec/scope-audit?change=（change 模块，与 file-diff 同族）：scope_audit.py 重构——_resolve_binding/_send_scope_rpc（offline/timeout/remote 错误族+能力门 422 映射抽出共享，两端点场景化中文 hint）/_require_ok_envelope 三 helper；get_scope_audit 透传投影 ScopeAuditResponse（rows 逐字段防御类型收敛、totals 缺省回退 rows 数、excluded_foreign_declared 读 daemon 扁平键）。DTO：ScopeAuditRow（verdict/attribution 两组字段按 mode 互斥）+ScopeAuditTotals+ScopeAuditResponse。测试：test_scope_file_diff +2（200 RPC 契约+DTO 逐字段含二进制 null 行/能力门 422+未绑定 404），9 passed；mypy/ruff/format 0；openapi+两前端 api-types 已 gen:types。
- ql-20260910-017-2006 | 变更中心单文件变化比对端点（change 模块）：GET /workspaces/{ws}/sillyspec/file-diff?change=&file=（WORKSPACE_READ 门控，路由避开 /changes/{uuid} 段吞字面量路径的 FastAPI 匹配坑）。编排复刻 git_log/service.py 范式：change/scope_audit.py ScopeFileDiffService——MemberBindingResolver 解析成员自己绑定行（无绑定 404 引导）→ ws_hub.send_rpc 平名 RPC sillyspec_file_diff（显式 35s 超时）→ 错误族映射 AppError 中文文案（422 家族：sillyspec_capability_missing→SILLYSPEC_TOO_OLD 升级引导 / method_not_found→DAEMON_TOO_OLD；502 offline/remote/契约缺口缺 ok 键；504 timeout；422 参数静态预检 ScopeFileDiffInvalidParam）。参数校验：change 白名单（machines.py compare 同款正则显式拒 ..）+ file 路径形态（拒 pathspec magic/控制字符/..段/绝对路径，反斜杠归一 POSIX）。DTO ScopeFileDiffResponse（snake_case 透传）。测试：test_scope_file_diff.py 7 用例（200+RPC 参数契约含反斜杠归一与 35s 超时/422 四形态不发 RPC/能力门 422/旧 daemon 422/502+504/远端 502+契约缺口 502/未绑定 404）；mypy 921 文件 0；ruff/format 0；openapi+前端/daemon api-types 已 gen:types。
- ql-20260910-016-e8e0 | user_input 日志行截断口径 5000→50000 统一单一常量（用户反馈长输入在会话页被截断，生产库实证多条 user_input 行 len=5000；agent 实收的 SESSION_INJECT prompt 本就是全文，只有展示副本+「重新发送」吃截断）：agent/model.py 新增 USER_INPUT_LOG_MAX_CHARS = 50_000 单一取值源（模型层无服务依赖，daemon/agent/spec 三方 import 不成环），9 个写点统一引用——daemon.session.service create/inject/control/ppm_activation、daemon.group.service messages/shadow、agent.worker_redispatch、agent.mcp_tools、spec_workspace.bootstrap（step_prompt）；auto_resume 的 USER_INPUT_TRUNCATION_LIMIT（G5 截断检测）改别名同值随写入口径联动。对齐 run_sync submit 链既有 50k 口径（submit_steps content[:50000]，ql-20260626-001）；DB 列 Text 无上限，50k 防极端粘贴撑爆 SSE/日志拉取。测试：test_session_user_log 截断用例改超限输入断 USER_INPUT_LOG_MAX_CHARS、test_session_recovery G5 反例夹具 x*5000→x*常量，34 passed；group/mcp/bootstrap 115 + worker 派发 14 passed；ruff/mypy 0。
- ql-20260910-014-6c29 | 心跳 DTO DaemonHeartbeatSillySpecChange 增可选 ql_id: str | None（宽松可选零改写透传语义——daemon 侧对 quick-* 名变更条 best-effort 读 guard.json 补报，普通变更/读不到 None，旧 daemon 不上报不 422）；机器视图读 schema MachineSillySpecStatusRead 嵌套项复用心跳 DTO 自动获得（同形零转换惯例）。用途：前端变更中心快速修复抽屉据此把 ql_id 反查成 quick-<8hex> 会话名，代入 scope-audit --change 命令（变更中心展示，ql-20260910-014-6c29）。测试：test_machine_sillyspec fixture changes[] 补 quick 条带 ql_id + 读取断言改七键（旧 daemon 形态条缺省 None 补齐 / 带值条原样透传），39 passed；openapi.json + 前端/daemon api-types 已 gen:types 重生成（含此前已提交后端改动的 openapi 追平）。
- ql-20260909-016-fa10 | init lease 凭据断链降级补日志（docs/sillyspec/init-lease-silent-no-local-yaml.md 修复建议①）：build_claim_payload 的 mode=='init' 注入分支补 else——actor_user_id/workspace_id 缺失或非法导致防御降级（不签 token、不注入 local_yaml）时 log.warning("init_claim_local_yaml_skipped", lease_id, reason="missing_or_invalid:<缺失字段>")，降级语义零变化；此前 claim 照常成功 + daemon 侧跳过写盘也零提示，local.yaml platform 段缺失整链静默（2026-09-09 平台同步断链排障实证）。测试：test_init_claim_tokens 组 C 两用例补 capsys 断言（structlog 控制台渲染不进 stdlib caplog）——warning 事件在场 + reason 含对应缺失字段名，8 passed，ruff/format/mypy 0。
- ql-20260909-004-e5f9 | 修复 24h 审查低危项：_mark_entry_failed 无谓词读-改-写（scheduled_send.py）——失败翻转分支原 db.get 读 pending 后、flush 前可被并发 cancel 置 cancelled 提交，随后无谓词 UPDATE 把 cancelled 覆写回 failed（用户取消成功却看到 failed；R3 当时只给派发成功分支加了条件 UPDATE，失败分支漏）。修：改谓词 UPDATE（WHERE id + status='pending'）对齐成功分支同款，0 行命中记 scheduled_send_failed_lost_race_to_final 尊重终态不覆写。测试：test_scheduled_send_sweeper +1（并发 cancel 先落后 _mark_entry_failed 保持 cancelled + pending 正常翻转 failed 对照），15 passed，ruff 0，mypy 0。
- ql-20260906-003-a611 | 修复审计 #10：vendored pi 扩展树分发链断裂——build-bundle.sh [5/5] 已拷 vendor 进 build/bundle，但 Dockerfile 只 COPY 两个 js、dist_router 只有两条硬编码 bundle 路由，install/自更新无清单可拉 → vendor 永不到 bin 目录、pi --extension 静默跳过（subagent 工具真实部署不可用）。修三段：①**Dockerfile** 补 `COPY build/bundle/vendor /app/daemon-dist/vendor`；②**dist_router** latest.json 增 `vendorFiles` 键（按需扫描 daemon-dist/vendor 的相对 POSIX 路径，排序稳定；未打包 → 空数组兼容旧客户端）+ 新路由 `GET /daemon/latest/vendor/{path}`（分段白名单拒绝 `..`/空段/反斜杠/NUL + resolve containment 双保险，不合规与缺失统一 404；media 固定 octet-stream 防 .ts 被 guess 成 video/mp2t）；③install.sh（grep -o 提取 "vendor/..." 字面量）/install.ps1（ConvertFrom-Json 数组）按清单逐文件下载（同款白名单 + tmp+mv 原子替换 + best-effort 单文件失败仅 warn，对齐 mcp-server.js 语义）；daemon preflight 自更新新增 updateVendorBundles（同清单逐文件 tmp+rename+mkdir 目录链；无 BUILD_ID 校验——.ts/.md 非 JS bundle；单文件失败 warn 继续）挂在 updateMcpServerBundle 之后，LatestInfo.vendorFiles 可选键旧服务器 no-op。测试：test_daemon_dist +5（清单/空兼容/文件服务/404/反斜杠逃逸，fixture 显式 newline="\n" 平台无关）、preflight-download-replace +5（嵌套落盘/白名单四态拒绝/空清单 no-op/URL 形状跳过/单文件 404 继续）；backend 14 passed + ruff 0 + mypy 0，daemon 51 passed + tsc 0。遗留：openapi.json/api-types.ts 正被并行会话暂存（+530 行），latest.json 的 vendorFiles 单字段 gen:types 债待该会话收尾后统一重生成（该端点 daemon/安装器消费，前端零调用）。
- ql-20260906-001-9232 | 修复 daemon-chat（quick-chat）派发 workspace_id 在 SQLite 下未归一化（昨日审计 #6）：main.py 端点用 raw text() 查 user_workspace_roles 解析派发上下文，SQLite 下 raw SELECT 不经类型回转返回 CHAR(32) hex 字符串（PG 是 UUID 对象）——未归一化直传 placement.dispatch_to_daemon，内部 workspace_id.hex 对 str 抛 AttributeError 被端点 except Exception 吞掉，run 标 failed 误报「No online daemon runtime found」（真因掩盖；生产 PG 不受影响，违背 SQLite/PG 双方言安全惯例）。修：对齐 placement.py raw SQL 结果归一化先例（uuid.UUID(x) if isinstance(x, str) else x）后下发。测试：新建 tests/test_daemon_chat_workspace_uuid.py（端点此前零测试覆盖）——patch dispatch 捕获 workspace_id 断言 uuid.UUID 实例且等于成员关系工作区，1 passed，ruff 0，mypy 0。
- ql-20260905-001-fc24 | 修复 spec 同步两处高置信缺陷（昨日只读审计 #1/#2）：①**build_bundle 快照元数据补 manifest_versions**（PLATFORM-BUNDLE.json 第五键，仅 exists=True 行的 {path: version}）——daemon pull 落地后据此回填本地 manifest 真实 base_version；此前 pull 重建缓存全 version=0（28bf3bc3e），pull 后首次真实改动的增量 update op 必撞服务器乐观锁 conflict 且 SpecPushConflict 不回退全量 tar → 本地改动永久推不上去、下次 pull 被 push_before_pull 拦死。②**apply_ops / soft_delete_change_dir 补 bump spec_version**（对齐 _write_spec_root 唯一 bump 点的既有语义）——此前两入口改写镜像树不递增 spec_version：lease latest_spec_version 不变他机永不重拉 + gzip 整包缓存键 (ws, spec_version) 不变恒吐写前旧树（e7bef3cc0 缓存引入后的陈旧命中，且增量路径永不 bump 无法自愈）。模块级注释补「manifest 行版本变化 ⇒ spec_version 递增」缓存确定性前提（新增树写入口必须同语义 bump）。测试：test_bundle_sync 元数据键集 +1 新增 manifest_versions 用例（add→软删→仅现存行）、test_sync_incremental 新增 bump 用例（identity map 需 refresh 重读）、test_soft_delete_change_dir 新增 bump 用例；3 文件 56 passed，ruff 0，mypy 0（service.py）。
- ql-20260904-024-e59b | offline sweep 非 worker 判死 error_code 收窄回 design 边界（backend-ci 红修复 + 未评审重派回归封堵）：87d237f68（ql-20260904-020）给「非 worker 判死」补 daemon_interrupted+中文原因（事故会话 e148364e 交互轮被 daemon 离线杀掉零详情）属刻意变更，但分桶口径「active worker 之外全部」（non_worker_ids 含全部 pending 档会话）越界 design 显式边界「pending 档不加分流」（见本索引 2026-08-29-batch-session-inherit 条目）——worker pending 会话落码后命中 session_offline_sweep_once 尾部 retry_seeds 自愈查询（status=failed + parent_session_id 非空 + 末次 run daemon_interrupted + runtime 回在线 + 宽限窗内），会在下轮 sweep 被自动重派（从未开跑的 run 重建 lease+新首 run，旧设计下永不命中）。修：sweep.py 的 error_code+output_redacted 赋值收窄到 active_main_ids（active 主会话组，事故场景本身），pending 档（主+worker）run 收敛 failed 维持无 error_code（pending 档用户可见解释已有 _fail_pending_queued_bulk「排队消息未发送」文案兜底）；函数 docstring 同步补记。测试：test_main_active_suspended_regression_locked 改锁新行为（daemon_interrupted+「daemon 离线」文案，_run_row 补 select output_redacted）；pending 档两例（test_worker_pending_tier_unsplit / test_offline_worker_pending_stays_failed_without_error_code）不动、随生产收窄复绿；daemon 模块 1974 passed + patrol 4 文件 89 passed，ruff 0、mypy 0。
- ql-20260904-023-0bea | permission_response 下发 payload 补 runtime_id（ql-20260904-022 UNKNOWN 桶残余收口）：respond_permission 三处下发点（plain 审批、_respond_dialog dialog 应答、_on_timeout 超时 deny）ws_payload 统一带 runtime_id——daemon _dispatchControl 从 payload 提取后消费即可按 runtime 桶立即回执（ql-022 immediateAck），此前无此键落 UNKNOWN 桶、无后续事件时 ack 等不到捎带（超时 deny 过期把 pending timer 状态的 run 一并判死是同一误杀的另一变体）；旧 daemon 忽略未知键向后兼容，超时路径 runtime_id=None 省略。daemon 侧 protocol.ts PermissionResponsePayload 加可选 runtime_id 标注契约。测试：test_session_permissions 三处断言补 runtime_id + 新增 dialog 应答 payload 用例（事故路径回归），6 套件 97/97 绿，ruff 0，mypy 未跑全量（改文件 ruff 0）。
- ql-20260904-016-7cab | 会话首响性能优化（排查会话 f0f76381 首响 46.5s）：**create 路径去掉 8s ready 死等**——create_session/_activate_tool_report_session 原地 await get_session_readiness().wait(timeout=8)，冷启动 daemon create 全链 3~31s 必超时，POST /api/daemon/sessions 实测挂 8.2s（46.5s 中占 1/6）；改为立即发 SESSION_INJECT，早到安全由 daemon 三重兜底承接（60s park 窗口 ql-20260831-006 + 控制指令三段式补拉 + firstPrompt 10s fallback），实测 POST 8.2s→0.1-0.3s。**spec bundle 传输 gzip 协商 + 字节缓存**（真正 30s 根因：36MB 全树 tar 经 Docker Desktop localhost 转发 ~2.4MB/s，后端每次冷打包经 bind mount 逐文件读 15-20s，daemon 30s fetch 超时被打穿、pull 恒失败回落旧缓存）——build_bundle 加 gzip_output（tarfile w|gz 流式），/spec-workspace/bundle 与 platform_sync /changes/-/spec-bundle 两端点按 Accept-Encoding 协商（Content-Encoding: gzip + Vary，浏览器/undici/httpx 透明解压，明文 tar 语义与下载文件名不变，对齐 ql-20260827-018 logs gzip 先例），gzip 变体按 (ws, spec_version) 缓存整包字节（每 ws 留最新 2 版，36MB→9.9MB、命中零打包零 mount 读，实测缓存命中 pull 30s+超时→~1.5s）。测试：test_bundle_sync +3（gzip 往返/路由协商 identity 回退/缓存命中与 plain 不缓存）。
- ql-20260827-018-dbd5 | GET /sessions/{id}/logs 响应 gzip 压缩（会话回显慢主因）：长会话 logs 全量 JSON（上限 5000 行 × content_redacted 50KB）明文传输，WAN 部署下打开/回看会话秒级等待；daemon/router.py get_session_logs 改按 Accept-Encoding 协商——客户端接受 gzip 且正文 >1KB 时返回 gzip 编码 Response（compresslevel=6，Content-Encoding+Vary 头；浏览器 fetch 与 Next rewrite 代理均透传，调用方零改动；小载荷保持明文）。返回类型 list[AgentRunLogEntry]→Response（FastAPI 对直接返回的 Response 跳过 response_model 序列化，json.dumps+model_dump(mode=json) 等价形状）。新增 test_session_logs_gzip.py 5 用例（gzip 生效+内容等价/流式消费完整/小载荷跳过/after 游标正交/identity 回退），test_session_history 17 用例零回归，ruff+mypy 0。
- ql-20260826-012-2802 | 性能批次二 + incident 时区修复：**incident 时区**——incidents/postmortems 共 5 时间列是全仓唯一「DateTime 无 timezone + naive utcnow 默认值」例外（service 层一直写 aware，asyncpg 剥 tz 后 naive/aware 混比 TypeError），model 改 DateTime(timezone=True)+now(UTC) 默认值，迁移 20260826210000 用 USING AT TIME ZONE 'UTC' 平滑转换（用户确认数据可重置）；**platform_sync upsert N+1**——upsert_agent_log_entries 循环内逐 entry select 改 (workspace_id, log_path) IN 批量预取 dict 命中（复合唯一约束保证语义等价，全量重推几百条 entries 由几百次查询降为 1 次）；**workspace 状态收集 N+1**——collect_scope_workspace_statuses（mission_status 入口）与 probe_workspaces（弹层批量探测入口）原每 ws 4 条查询（4N），新增 collect_many_workspace_statuses 共享组装函数 _workspace_status_entry，3 条固定查询（workspaces/bindings/daemons 各一次 IN；online=属主匹配+status=online 等价 query_daemon_online_by_id），git_probe 仍逐 ws 回调；条目顺序契约钉死跟随 scope_workspace_ids 声明序——IN 查询不保证返回顺序，首跑暴露简报渲染乱序后按 valid_ids 重排修复；**三个无界列表补固定上限 200（不动 OpenAPI 免 gen:types）**——list_dialog_history（取最近 N 反转恢复创建序）、list_session_runs（已 desc 加 limit）、list_change_sessions（SQL 加 nulls_last+desc+limit 取数窗口，Python 最终排序保留）。测试：upsert 查询计数解耦（3 vs 12 条 execute 次数差 ≤1）、批量与单查口径逐字段相等 + 查询数恒 3、三处上限裁剪各 1 用例（常量 monkeypatch 小值+超额播种）、incident 默认值 aware+UTC 零偏移钉死；aiosqlite 往返丢 tz 为已知测试库差异不做读回断言。
- ql-20260826-011-da0a | 全仓排查批量修复（8 项，缺陷 5 + 性能 3）：**IDOR 越权**——incident 4 端点（get/update/postmortem×2）与 release 5 端点（approve/deploy/promote/rollback/approvals）原用 require_permission_any（任意工作区有权限即过）+ 裸 id 取对象，A 工作区用户可读/改/审批/部署 B 工作区单据；改 get_current_principal + 新增 _require_*_permission 对象级校验（has_permission(workspace_id=obj.workspace_id)，对齐 agent/file 惯例；approvals 收紧为 WORKSPACE_READ）；**preview_office 令牌死签**——issue_file_token 原吞 Redis SET 异常仍签发，consume 端要求 jti 键存在致恒 410 无日志，改 fail-fast 503（新错误 PreviewFileTokenStoreUnavailable）+ docstring 同步；**diff_collector 孤儿进程**——两处 wait_for 超时路径补 _kill_proc（kill+wait，对齐 git_gateway）；**parser 缓存竞态**——_MODULE_MAP_CACHE 写侧（插入+stale 迭代清理）持 threading.Lock（to_thread 多线程迭代+插入可抛 RuntimeError）；**性能**——llm-proxy 转发客户端改进程级共享单例（_get_llm_proxy_client/close_llm_proxy_client，lifespan 关停回收，原每请求 new AsyncClient 零复用=每推理一次 TLS 握手）；publish_submitted_messages 两路 channel 各自 pipeline 批量发布（原逐条 await publish，几百条日志=上千串行 RTT，pipeline 保序）；Settings.resolved_commit_sha git 探测结果缓存（原每次 access 同步 spawn git，/health 高频轮询周期性阻塞事件循环）。测试：incident router +5（跨区 403×3/平台管理员放行/deny 正控）、release router +4（approve/deploy/promote/approvals 跨区 403）、preview +1、diff_collector +1（kill 断言）、parser +1（锁串行化）、config +3（探测一次/失败缓存/显式跳过）；test_llm_proxy 加单例复位 autouse fixture；session_sse/assistant_override 假 redis 补 pipeline 假件（AC-06 失败注入改 execute 时抛）。不改 OpenAPI/DTO/migration，无需 gen:types。
- ql-20260825-011-76cf | 会话消息后端真实排队（忙轮追问不再 409/刷新不丢）：新表 agent_session_queued_messages（迁移 20260825160000，pending 上限 5=SESSION_QUEUE_MAX_PENDING）；inject HTTP 端点恒 queue_when_busy=True——锁内忙轮改入队（queued=true/queue_entry_id，响应 run_id 可空），service 身份路径默认 False 保持 409 零回归；close_interactive_run 终态后 fire dispatch_next_queued_message（先查 pending 才起任务，独立 DB session）按 created_at 派发队头（成功删行/失败转 failed 留队）；end_session 收口 pending→failed；新端点 GET /sessions/{id}/queue + DELETE /queue/{eid} + POST /queue/{eid}/retry。test_session_queue.py 新增 9 用例（入队/满员/派发/失败/收口/管理），router 409 契约测试改排队契约。
- ql-20260823-007-2c9e | 修任务轮询误认领 reopen 租约（ql-20260823-006 排查遗留）：`GET /runtimes/{rid}/pending-leases` 排除 reopen 租约（metadata 带 `reopened_from_status`——reopen 租约只经 daemon:session_resume WS 消费，混进轮询会被 daemon 认领后因无 prompt/run_id 报 interactive_missing_fields 裸退、租约永挂 claimed）；`mark_session_recovery_failed` 翻 failed 时挂起租约（pending/claimed）同事务收敛 cancelled（对齐 sweep/DS-5 语义）。存量清理：13 条孤儿/死挂 claimed 租约 → cancelled（08-21 起累积）。test_lease_ownership +1（排除）/ test_session_reopen +1（收敛）。
- ql-20260823-006-80c8 | run 终态翻会话终态补发 SESSION_END（修僵尸会话 reopen 死循环的后端侧源头，2026-08-23 bdec91a4 事故）：`close_interactive_run` 翻 ended/failed 时 commit 后经新模块级 helper `_send_session_end_best_effort`（session/service.py）补发 SESSION_END（daemon 侧 end() 收口清内存副本）；多轮 active 不发。test_close_interactive_run_session_status.py +2 用例（单轮终态发 SESSION_END / 多轮 active 不发）。daemon 侧配套修复见 sillyhub-daemon.md 同 ql 条目。
- ql-20260625-003-4d7a | AgentRunResponse 与 session SSE tokens/turn_completed 透出缓存读取/写入 token 字段，供前端运行日志与会话消息展示 cache 用量。
- ql-20260626-001-4a8e | 放宽 AgentRunLog content 落库与 SSE 推送截断 5000→50000（run_sync/service.py submit_messages），避免 agent 长答复/最终总结被硬切。
- 2026-06-26-daemon-client-spec-sync-fix | daemon-client spec 同步修复：SpecPathResolver platform_managed mode（FR-01~04）+ apply_sync .runtime/last_synced_at（FR-06/07）+ daemon_change_writes lease-polling change-write 三端点 + proxy_create_change（FR-08~09）。
- ql-20260703-001-643f | build_claim_payload interactive 分支 provider 归一化（_normalize_lease_provider：claude_code/claude-code→claude，其余原样），与 daemon normalizeProvider 双保险——修 backend 透传 adapter id 'claude_code' 致 daemon _agentPaths.get 失败、interactive 静默早返回、lease 永远 claimed/run 永远 pending。

- ql-20260709-001-7e3a | tool_result 命令输出截断 3000→100000（run_sync/service.py `_extract_sdk_messages`，新增常量 TOOL_RESULT_MAX_CHARS）+ 超长追加中文标注，避免 scan/构建/测试日志尾部被砍（interactive 路径，daemon 透传不截）。

- ql-20260709-002-1b8c | thinking/TOOL_USE 命令行截断 2000→2万 + result_summary 兜底 4000→5万（service.py `_extract_sdk_messages`），A 类日志截断放宽（B 类防刷屏保留）。

- ql-20260723-010-32d6 | 后端测试提速：conftest 设 `AUTH_BCRYPT_ROUNDS=4`（测试档位，省每认证测试 ~0.3s 哈希）+ pyproject 加 pytest-xdist 并行（`-n auto` 全量 ~50min→7min）+ 根 conftest 新增 `seed_spec_root_fn` fixture 让 5 个 change/task 测试经依赖注入取根 helper（根治 `from conftest import` 因 9 个同名 conftest 模块名共享在 xdist 下解析顺序歧义）+ git_gateway 参数化 `list(frozenset)`→`sorted`（消 xdist 多 worker 收集顺序不一致）。

- ql-20260726-001-ac8a | daemon-borrow 迁移 revision 碰撞修复：本变更 3 迁移与 llm-provider 变更撞 revision id `202607251100` 致 alembic 双 head（生产 `alembic upgrade head` 必报 Multiple heads crash-loop，daemon-borrow verify-result.md P0）。renumber 内容 revision 1100/1200/1300→1400/500/600（1400 down_revision 接 llm-provider 的 1100，形成 1000→1100llm→1400→1500→1600 单 head 链），同步改 3 测试硬编码 id 断言。13fc1dc9 已先纯重命名文件名但未改内容，本 ql 补全内容修复。

- 2026-07-25-daemon-borrow-for-business | 业务/管理人员（business_member 角色）借用工作空间共享 daemon 跑 agent 读源码出业务方案。数据：workspace_member_runtimes 加 shared 列 + daemon_borrow_audit 表 + business_member 角色种子（DAEMON_BORROW=daemon:borrow + task:run_agent + workspace:read）。派发：4 路 resolver（placement dispatch/decide/interactive + member_runtimes writeback）收敛到 agent/borrow_resolver._resolve_borrowed_or_own_runtime（先自有零回归，无则借用三重校验 权限→shared→online）。落点：close_interactive_run 回调落 FileService（owner_type=workspace, text/markdown 白名单）+ 审计。接口：PUT /my-binding/shared + GET /shared-daemons + GET /api/file/list。零回归：shared 默认 false、DAEMON_BORROW 默认不授、helper 第1步自有原路径。

- ql-20260728-002-21aa | 登录爆破防护（安全审计 P0-8/P1-14 修复）：同 IP 60s 窗口 INCR 限流（`auth_login_rate_limit_per_minute=5` → 429）+ 失败计数（达 `auth_login_fail_threshold=3` 后该 IP 须带 captcha_token）+ Pillow 滑块验证码（背景含凹槽+滑块块，target_x 仅存 Redis 不返前端，±6px 容差验过签发一次性 captcha_token；slider/token 均一次性消费防重放爆破）。新增 `modules/auth/captcha_service.py`；router 加 `GET /captcha/slider`、`POST /captcha/verify`，登录端点串 check_rate_limit→assert_captcha_if_needed→record_login_failure；Redis 故障降级放行不阻断登录（同 api_key 缓存降级哲学）。

- ql-20260728-003 | 滑块验证码下线换点按式人机确认（滑块交互 ±6px 难对齐、体验差）：`captcha_service.py` 删 Pillow 滑块生成/校验，新增 `create_confirmation`/`verify_confirmation`（一次性 captcha_id→captcha_token，取到即签发，无坐标判断）；限流/失败计数/`assert_captcha_if_needed`/`_consume_captcha_token`/Redis 降级全保留。schema 删 `SliderCaptcha*` 加 `ConfirmCaptchaResponse`/`CaptchaVerifyRequest`/`CaptchaVerifyResponse`；router `GET /captcha/slider`→`/captcha/confirm`、`POST /captcha/verify` 体 `{captcha_id,x}`→`{captcha_id}`；登录端点/423 触发不动。Pillow 因 PPM 模块在用保留依赖。`tests/test_login_captcha.py` 重写为 6 测试（限流/423/全流程/id 一次性/未知 id/降级放行）。

- ql-20260729-001-30e5 | 修复登录验证码吞掉密码错误提示：阈值触发后用户带有效 captcha_token 登录但密码错时，后端不再绕回 423"需要验证码"（token 已被 assert_captcha_if_needed 提前一次性消费，陷"验证→又让验证"循环、用户永远看不到密码错误），改为明确返回 401"用户名或密码错误"。根因 router.py login 的 except AuthInvalidCredentials 分支达阈值后把所有密码错误转 LoginCaptchaRequired。修复：`assert_captcha_if_needed` 返回 bool（本次是否已通过人机验证/消费有效 token），`captcha_verified=True` 时密码错正常抛 AuthInvalidCredentials。爆破防护不降（每次试密码仍须先过验证码 token 一次性 + IP 限流）。`test_login_captcha.py` 加 `test_captcha_verified_then_wrong_password_returns_401` 回归。

- ql-20260729-002-833d | 修后端测试 collection ERROR 阻塞：backend/conftest.py 的 db_engine fixture 漏 import app.modules.ppm.project.model（ppm_project_maintenance/customer/member/stakeholder 4 表），而 workspace.model 的 ppm_project_workspace 外键→ppm_project_maintenance.id，该表未注册致 sqlite create_all 报 NoReferencedTableError，连带所有依赖 db_session/client 的测试 collection ERROR（auth 全模块 138 errors）。补一行 import，外部依赖链全满足（organizations→admin.model、users→auth.model 均已在上文注册）。修后 auth 全模块 137 passed（暴露 1 个预存 migration 测试债 test_alembic_head_includes_new_revision 过时断言"是 head"，实际 202607271700 被 202607281500 接续，需另修）。

- ql-20260729-003-2ff0 | 修 migration 测试债让 auth 全绿：test_refresh_token_index.py::test_alembic_head_includes_new_revision 断言过时（断 revision 202607271700 是 head，但 ppm-project-link-workspace 的 202607281500 down_revision=202607271700 已把 head 接续成 202607281500，链健康单 head）。AC-10 真实意图是验证 refresh-token-index 的 migration 已落地进 alembic 链——断言从"是 head"改"在链中存在"(script.get_revision 非 None)，保留"链单 head 无分叉"检查(len(heads)==1)，不绑定具体 head(随新 migration 演进)。同步更新文件级 docstring AC-10 描述。auth 全模块 137 passed/1 failed→138 passed/2 xfailed 全绿零回归。

- 2026-07-28-llm-provider-presets-and-usage | LLM 供应商预设模板 + 用量/余额查询：llm_provider 模块新增 `POST /api/llm-providers/{id}/usage`（owner 级，跨用户 404/403 不泄漏）。schema 加 `UsageData`（plan_name/total/used/remaining/unit/is_valid 全 Optional）+ `UsageResult`（success/data/error）；新增 `usage_handlers.py` 按 balance（DeepSeek `/user/balance`、硅基 `/v1/user/info`、OpenRouter `/api/v1/credits`）与 token_plan（Kimi For Coding、智谱 coding-plan/quota、MiniMax coding_plan/remains）两路径逐家硬编码 query+parser；`service.query_usage()` 解密 key + `_detect_usage_provider(base_url)` 路由 + 错误两态（瞬时网络/5xx/429/超时 → raise 5xx；确定性 401/403/空 key/未知供应商 → success:false）+ SSRF 复用 `tool_policy.assert_public_hostname`（15s 超时）。无 DB 字段（D-004，detect_provider(base_url) 实时路由）。新增 `tests/test_usage.py`（46 用例：各家正常 + 错误分类 + detect 路由 + SSRF 拒私网/IPv6 + key 明文不入响应）。

- ql-20260731-001-3abf | 平台技能清单 manifest 增 skills 摘要字段：`skills_bundle_service.py` 新增 `_parse_skill_frontmatter`（解析 SKILL.md 开头 YAML frontmatter 取 name/description，无围栏 / YAML 语法错 / 解码错均返回空 dict 不抛异常）+ `_summarize_skills`（按顶层目录聚合 name/description/file_count，name=目录名与 daemon 同步路径一致，注意目录名 `sillyspec-archive` 与 frontmatter `name: sillyspec:archive` 不同）；`build_skills_manifest` 返回新增 `skills` 字段供前端清单页展示每个技能描述（不动 `files`，daemon 同步与 version 计算零影响）。`test_skills_bundle.py` 加 4 测试（frontmatter 解析 / 聚合 / 端到端 description / 无 frontmatter 空兜底），15 passed。

- ql-20260803-003-cb34 | POST /api/workspaces 复用已存在工作区时显式提示：`WorkspaceRead` 加可选 `creation_notice`（仅创建端点填，列表/详情恒 None）；`WorkspaceService.create` 加 `notice` 注入参数（默认 None 零影响）在「同 root_path 已有 active→reused_active / pending→activated_pending / 软删→resurrected」三分支填 kind；router 据此转中文提示（含 daemon 绑定未写入提醒）。`test_router.py::test_create_duplicate_returns_existing` 补回归断言（新建无提示 + 复用必带「复用」文案）。

- ql-20260809-003-56db | 多代理审计 8 个低风险单点修复（backend 部分 5 处）：release promote 死路由 require_permission→require_permission_any（路径无 workspace_id 占位符，原装饰器恒 422）；incident `update` 补 VALID_SEVERITIES 校验与 create 对称（堵 update 直接入库非法 severity）；core/errors `_request_id` 优先读 request.state.request_id 再回退 header/uuid（与 main.py 中间件 + x-request-id 响应头/慢请求日志对齐）；knowledge parser `_read_file_safe` 大文件改限量读前 MAX_CONTENT_BYTES//4 字节（OOM 防护，不整读后切片）；ppm/kanban `_parse_date_range` datetime.combine 补 tzinfo=UTC（匹配 PlanTask.end_time timestamptz）。配测试 errors×3/incident×2/knowledge×2/kanban×2/release×1，后端 pytest 110 passed + ruff 全过。

- change 2026-08-09-security-backend-guardrails | 后端防护加固（incident 状态机 + SSRF 三连）：①`incident/service.py` 加 `INCIDENT_TRANSITIONS` 放宽版图（open→{investigating,resolved}；investigating→{mitigated,open,resolved}；mitigated→{resolved,investigating}；resolved→{investigating}）+ 复用 `ppm/common/fsm.assert_transition` 校验非法迁移返 `InvalidTransition`(422)，值非法仍 `IncidentError`(400)，同态幂等，resolved 重开→investigating 清 resolved_at/by（D-001/002/006）；②新建 `core/ssrf.py` 统一 SSRF 入口 façade——`assert_public_url`（scheme 白名单 + 复用 `tool_policy.assert_public_hostname` IPv4+IPv6 全量 SSRF）+ `assert_safe_repo_url`（纯协议白名单不查 IP，拒 ext::/file:///裸路径/Windows 盘符，放行 https/ssh/git/scp-like 含内网 git）+ `UnsafeRepoUrl`(400)；③`mcp_gateway/service.py` create 注册前 + `_deliver_one` 投递前双查（投递 best-effort catch）；④`worktree/git_runner.py` clone_bare 前置 `assert_safe_repo_url`；⑤`tool_gateway/service.py` `_handle_http_get` 改 `follow_redirects=False` 手动逐跳≤3跳 + 每跳 `assert_public_url`（同时修 IPv6 私网 + 重定向不复查两缺口，工具返 result dict 不抛）。顺补 `conftest.py` db_engine 漏 import incident+release model 的 pre-existing 测试基建债（解 incident 全套 collection ERROR）。新增 test_fsm(16)+test_repo_url_guard(22)+test_ssrf(6)+test_webhook_ssrf(6)=50 用例，incident/worktree/tool_gateway/mcp_gateway 回归全绿。ruff 过、不改 OpenAPI/DTO/migration 无需 gen:types。

- change 2026-08-09-security-credentials-hygiene | 安全凭据卫生（后端部分）：`core/config.py` 加 field_validator 拒 bootstrap 管理员弱口令——模块级 `_WEAK_BOOTSTRAP_PASSWORDS` 黑名单（admin123/password/12345678 等 12 项）+ 与 email 本地部分相同拒，配置加载期 fail-fast（ValidationError）连 lifespan 都不进；`None` 放行（bootstrap 可选 D-004）。新增 `tests/modules/auth/test_bootstrap_password_strength.py` 16 用例（弱口令表参数化逐项拒 + email 同名拒 + 强口令过 + None 过）。ruff/mypy 过、auth 目录零回归（9 failed 为 login rate-limit 族既有，与本次无关）。

- change 2026-08-09-security-ppm-ownership | PPM 代填冒名防护（已上线模块，纵深防御）：新建 `ppm/common/ownership.py` 归属校验原语 `resolve_owner`（鸭子类型仅读 `actor.is_platform_admin`/`actor.id`，不查库、无 isinstance）+ `PpmOwnershipDenied`(403, code=`HTTP_403_PPM_OWNERSHIP_DENIED`，仿 `tool_policy.SsrfBlocked` 经 `core/errors` 全局 handler 按 http_status 自动映射、无需改 router)——非管理员显式填他人 execute_user_id/check_user_id/current_user_id/user_id → 403，平台管理员可代填（运维纠错），None（未指定）/自填放行。校验放 service 层（非 router）：task/problem service 7 个写方法（`PlanTaskService.start`/`execute_plan`、`TaskExecuteService.create`/`update`、`WorkHourService.create`/`update`、`ProblemService.execute_problem`）落库前各字段过 resolve_owner；router 仅透传 `actor=user`（删 execute_problem/start 的 `execute_user_id or user.id` 收窄，避免 service 层兜底遮蔽）。既有 `test_task.py`(13)/`test_problem_flow.py`(9) 处补 `actor=_ADMIN`（types.SimpleNamespace stub，test_task 含 11 直调+2 helper）放行造数，PlanTaskService.create 建计划（非冒名面）不动；新增 `common/tests/test_ownership.py`——resolve_owner 纯函数 4 分支 + PpmOwnershipDenied 错误语义（code/http_status/details）+ `start_plan_task` 端点双角色（非 admin 代填→403+code / 自填→201，admin 代填由既有 task `test_router.py` admin token 填随机 execute_user_id 走通回归）。ppm 全量 496 passed（489 既有 + 7 新增）、ruff 过、不改 OpenAPI/DTO/migration 无需 gen:types。

- change 2026-08-08-llm-provider-openai-format | llm_provider 加 api_format（anthropic/openai_chat）列 + 完整 URL 按格式归一（不加 is_full_url 列）；fetch-models/probe 按 api_format 产鉴权头（openai=纯 Bearer）+ 候选 URL，schema 透传 api_format + litellm_registered 响应；新增 litellm_client.py（register/unregister LiteLLM admin API，gap-A 定稿 model_info.mode=chat 强制 Chat Completions 走 /v1/chat/completions 非 Responses API，best-effort R-09 失败不阻塞 set/unset/delete 主流程）；lease context.py provider_config openai 形态（litellm_base_url/auth_token/model_name 6 字段，不含上游 api_key D-003/NFR-01，上游 key 只在 LiteLLM 注册）；config 加 litellm_base_url/litellm_master_key 设置；alembic 迁移 202608091100_add_llm_provider_api_format 加列；openapi.json + api-types 重生成。

- change 2026-08-10-sillyhub-platform-sync | SillySpec 跨仓进度同步契约后端落地：新建 `platform_sync` 模块（model/service/schema/auth/router 5 文件 + tests 15 用例）3 端点——POST `/api/changes/{name}/progress`（读 3 个 `X-SillySpec-*` header[User/Base-Ts/Pushed-At] + §4.2 base_ts ISO8601 字典序乐观锁：无 base_ts/stored≤base_ts→200 / stored>base_ts→409 body `{conflict,platform_progress,last_pushed_at}` 不 auto-merge）/ GET `/api/changes`（裸数组轻量列表 `[{name,current_stage,last_pushed_at,last_pusher}]`）/ GET `/api/changes/{name}/progress`（完整六表 JSON+顶层 last_pushed_at，404）。`PlatformChangeProgressORM`(BaseModel,table) 单行存 `latest_progress`(JSON)+`last_pushed_at`/`last_pusher`+`updated_at`；alembic 迁移 `20260810150000_create_platform_change_progress`（down_revision=202608091100，方言无关 create/drop）。`require_platform_sync` Bearer=APIKey(`shk_live_`)/JWT 双路径鉴权（复用 `ApiKeyService.authenticate`+`get_current_user`，无新鉴权原语）。router 不自带 prefix 避尾斜杠 307；POST 409 用 JSONResponse 绕 response_model 校验保 body 严格按契约。裸 dict 透传（NG-6 不强类型），铁律不碰派发层（D-004，与 `/api/workspaces/{wid}/changes/*` 正交）。契约 §13 校验清单 8 项 + §4.2 三分支 + §7 字典序 + 鉴权双路径 15 测试全过。gen:types 同步 `openapi.json`(361 paths)+`api-types.ts`（含 `/api/changes*` 路径 + `ChangeListItem`）。

- ql-20260811-001-8cb0 | 修复 tool_gateway test_ssrf.py mypy CI 错误（Invalid base class "real_client"）：`_patch_http_client` 里 `real_client=httpx.AsyncClient` 变量赋值后 `class _Client(real_client)` 用变量作基类，mypy 不接受非 Final 变量作基类；改 `class _Client(httpx.AsyncClient)` 直接继承、删中间变量（class 定义在 `monkeypatch.setattr` 前求值，此时 `httpx.AsyncClient` 仍是原始类，注入 transport 逻辑等价）。全量 `uv run mypy app` = Success no issues found in 589 source files（原 test_ssrf:71 error 消除，仅剩 4 个预存 annotation-unchecked note 非 error），pytest test_ssrf 18 passed。

- change 2026-08-13-platform-managed-file-sync | spec 文件同步从「整树 tar 全量覆盖」改「文件级增量 diff + 乐观锁 + 软删备份」：spec_workspace 模块新增 `SpecFileManifest` 独立清单表（D-011，**不复用 scan_documents**——scan_docs reparse 只认 docs/ 会翻转非 docs 行，独立表职责分离）+ `apply_ops`（add/update/delete/rename + per-file base_version 乐观锁 conflict=true+server_versions、软删 move 出 spec_root 到 `spec_data_root/spec-backups/{ws}/{ts}/{path}`（D-010 move 非 copy，spec_root 兄弟目录 build_bundle 拉不到 D-008）+ exists=False+version+1、containment 校验对齐 `_extract_spec_tar_to_staging` 机制 + .runtime 拒、R-07 无行兜底 version=1、R-06 30 天机会式修剪）；新端点 `POST /api/workspaces/{ws}/spec-workspace/sync-incremental`（conflict=True 时 **HTTP 200** body 带 server_versions，端点不额外抛 409——daemon 侧据字段提示人工拍板）；旧 tar `_write_spec_root` 落盘后 `DELETE FROM spec_file_manifest WHERE workspace_id=?`（Q7 旧 tar 失效清单，下次增量 R-07 全量重算）。scan_docs 模块零改动（D-011）。alembic 迁移 `20260813160000_create_spec_file_manifest`（down_revision=20260811150000 实际 head）。gen:types 同步 openapi.json+api-types.ts（新端点+3 schemas）。注意：端点**挂 prefix=/api**（非 /api/daemon），daemon client URL 用 /api 前缀（QA 揪出 P0）。

- ql-20260819-004-695a | apply_ops 软删行复活语义（坑 2026-08-19-quick-done-autoarchive-misfire 缺陷②）：①rename 目标是 `exists=False` 软删墓碑**不算占用**——原实现 `target_row is not None` 即判 conflict 跳过，un-archive 愈合方向（archive→active，CLI 对哈希相同搬回恒发 rename）永久卡死；现原地复活墓碑（content_hash/new version=目标路径谱系+1/exists=True），不走「删墓碑+插新行」（SQLAlchemy flush 先 INSERT 后 DELETE，同 path 撞 `ux_spec_manifest_ws_path` 唯一约束），R-07 无旧行 rename 落墓碑同样原地复活。②add 落在软删行上（CLI 把 exists=f 过滤出 serverPaths → 恒发 add base_version=0）：原实现落乐观锁分支——同内容豁免 no-op 提前返回留僵尸 exists=f（daemon 对齐推送还会每次重发）、异内容判 conflict；现写盘+原地复活（version+1）。test_sync_incremental.py 新增 TestSoftDeleteRevival 4 用例（rename 复活/add 同内容/add 异内容/事故组合单请求 rename+delete+update+add 全落盘守护，组合用例证明 rename 蒸发疑点③隔离不可复现）。

- change 2026-08-14-audit-system-completion | 审计体系补全（漂移点 #1/#2/#3 修复，docs/architecture-4a.md §8）：①`main.py` lifespan 挂 `register_audit_hooks(get_engine())`——此前生产从未调用、自动审计休眠，现 77 表注册 + 幂等（event.contains）；②`workflow/model.py` AuditLog 旁定义 5 个 action 常量（AUTH_LOGIN_SUCCESS/AUTH_LOGIN_FAILED/PLATFORM_SETTING_CREATE/PLATFORM_SETTING_UPDATE）+ `AUDIT_PLACEHOLDER_ID`（全零占位 UUID，方案 B 常量集中 D-005，不设 _DELETE——Grill C-6 settings 无 delete 端点）；③`auth/service.py` login 成功/失败/禁登三分支手工审计（登录无 Bearer→无 audit_context 需手工；失败/禁登 resource_id 占位 + raise 前 try/except commit 不阻断 R-03）；④`settings/router.py` 两条写路径（PUT 循环 + _write_setting_json）per-key 手工审计（CREATE/UPDATE 按 old_value 判定，PK 非 UUID 挂 hooks 也跳过故手工，D-004）。测试 13 新用例（hooks 五场景 + auth 4 + settings 4），全量 3960 passed/4 failed（test_router_transition 4 failed 为 main 预存债隔离复跑证实）。决策 D-001~D-005 全 accepted。

- ql-20260815-001-f305 | DB 阻塞应急观测（monitoring.py 第 4 件套）：`slow_request_middleware` 在请求耗时 >=10s（`DB_BLOCKING_SAMPLE_THRESHOLD_S`）时 `_fire_db_blocking_sample` 后台 fire-and-forget 采样 pg_stat_activity——非空闲会话快照（pid/state/wait_event_type/wait_event/query_duration/query 截断 300，LIMIT 50）+ `pg_blocking_pids` 锁等待链（blocked_by 数组 + blocker 侧 SQL/state），打 `db.stat_activity_sample` 结构化日志（trigger 携带 path/request_id/duration 关联 slow.request）。要点：①采样走独立 `NullPool` 短连接引擎（`_get_sample_engine` 懒建缓存，**不占共享池**——DB 阻塞时共享池往往已耗尽，且不挂慢查询监听防自我递归）；②30s 节流（`DB_BLOCKING_SAMPLE_MIN_INTERVAL_S`，`_last_sample_at` monotonic）防日志爆炸；③`asyncio.timeout` 5s 兜底 + 全异常兜底打 `db.stat_activity_sample_failed` 不上抛（观测手段不能成为新故障源）；④非 PG（测试 SQLite）返回 None 跳过。动机：事后 `docker exec psql` 抓现行依赖容器存活，容器重建现场即丢，落进 JSON 日志才能事后追因。新增 `app/core/tests/test_monitoring.py` 9 用例（触发阈值/节流/非 PG 跳过/超时兜底/连接失败兜底/引擎缓存/fire 调度），core 套件 80 passed，ruff+mypy 通过。

- change 2026-08-15-change-step-visibility | 变更中心 step 级进度读侧透出：service.py `_project_current_stage` 三元组返回（多带 latest_progress，零新增查询）+ 两处二元组解包适配（enrich_summaries/_resolve_pending_change_keys）+ `_extract_step_progress` 提取器（STAGE_ORDER+quick 兜底排序/completed_at strptime 本地→UTC ISO 归一化失败保留原串/七值 status 透传/output 截 200/防御 isinstance 缺失返 None None）+ enrich 两路填充；schema.py 新增 StepProgressSummary/StepTimelineEntry DTO + ChangeSummary.step_progress（列表仅摘要）/ChangeRead.step_progress+steps 全 optional 零 migration。测试 test_step_progress.py 20 用例（降级矩阵/守护/集成），模块 336 passed。
- change 2026-08-16-change-owner-from-token | 变更责任人来源 token 身份+履历事件时间线：新表 change_events（通用事件模型 event_type+JSON detail，owner_change 首类，后续事件零 schema 变更接入）；platform_sync push_progress 接受分支 `_sync_change_owner`（savepoint 原子：SELECT 重查行+UPDATE owner+INSERT 事件，None 首填不记事件/变化记事件 detail from_user_id to_user_id 逐字/相同幂等零写，失败回滚 savepoint 不阻断，X-SillySpec-User→last_pusher 零变化）；change 读侧 enrich 批量 join users 填 owner_name（display_name 优先，查询次数锚定禁 N+1）+ 时间线合成事件条目（kind=event/责任人变更/A → B/stage 归一化近似/混合序列重编 ordering key 唯一）+ 截断两层分离（明细全量/列表摘要 200）。测试 test_owner_sync 5+冒烟 e2e 1+step_progress 34，模块 424 passed。
- change 2026-08-16-change-center-quick-tab | 变更中心「快速修复」tab 双链路（quick 操作平台展示位）：platform_sync 新表 quicklog_entries（UNIQUE(workspace_id,ql_id) 幂等 upsert，migration 20260817010000）+ `POST /api/quicklog-entries` CLI 推送端点（shpsync_ 令牌鉴权 workspace 仅从 token 派生 payload 不收 workspace 字段，scope.workspace_id None 403 fail-closed）；change 模块新 quicklog_parser.py（QUICKLOG 条目解析：`## ql-` 头切分/全半角冒号标签/4 状态形态/文件 5 分隔符+bullet 括注/白名单 linked_changes，mtime 指纹缓存）+ quicklog_service.py（双源合并 PG 优先 ql_id 去重/stale 24h 派生/author enrich users.username→display_name 批量 IN/affected_modules 复用 ChangeParser._load_module_map 前缀匹配/全文搜索标题+正文+正文标签段）+ `GET /api/workspaces/{id}/quicklog-entries` 列表（search/status/author/linked_change/include_placeholder 筛选+分页）与 `{ql_id}` 详情端点。测试 parser 17（真实语料 492 条冒烟零异常）+push 7+service 12+router 5+表 smoke 2。
- ql-20260822-007-d62a | 修复 backend-ci Mypy 红灯：patrol.py 加 mission.session_id None 收窄；test_session_team_mission.py 删 3 处多余 type:ignore。本地 mypy 678 文件零报错，agent+daemon 1818 测试通过。
- ql-20260822-006-f113 | 修复变更详情步骤时间线偏 8 小时：sillyspec CLI 用 `toLocaleString('zh-CN')` 写 CLI 宿主机墙钟（无时区标记），`ChangeService._normalize_completed_at` 原用 `naive.astimezone()` 随后端进程时区解释——Docker 容器 UTC 把东八区墙钟当 UTC，前端转浏览器本地后整体 +8h（owner 事件 stage 归属/插入定位同受影响，比较基准同源偏移不致排序错乱）。修复：`core/config.py` 新增 `cli_progress_timezone`（默认 Asia/Shanghai，validator 经 `resolve_cli_tzinfo` 接受 IANA 名或 ±HH:MM 固定偏移，非法启动期 fail-fast）+ `_normalize_completed_at` 改 `replace(tzinfo=cli_tz)` 归一，与进程时区彻底解耦；pyproject 加 tzdata（Windows venv 无系统 tz 库必需，Linux 兜底无害）。测试 test_step_progress.py 改固定时区断言（原「进程本地时区往返」断言在 UTC 容器恰固化 bug）+ 新增 settings 驱动回归用例（UTC/+05:30 两配置形态）；模块 394 passed。归一在读侧进行、DB 存原始串，存量数据零迁移即修。
- change 2026-08-24-sessions-live-updates | 会话列表 SSE 变更信号 + 轮询兜底（design §3，task-01/02/03/04/07）：新增 `backend/app/modules/daemon/session_events.py`（Redis 全局频道 `agent_sessions:changed` + `publish_sessions_changed` 静默容错）；按 design §3 生命周期契约表在 SessionService（8 写入点）、run_sync、lease、sweep、agent service/placement、platform_sync 埋点（created/status_changed/deleted）；`backend/app/modules/daemon/router.py` 新增 `GET /api/daemon/sessions/events` SSE 端点（鉴权同 list_sessions、user_id 过滤、30s keepalive、finally 清理 pubsub）；发布携带 user_id，订阅端仅下发属主信号。测试新增 `test_session_events.py` / `test_session_events_cross.py` / `test_sessions_events_stream.py`；全量 pytest 绿（数量见同 change verify-result.md）。
- change 2026-08-24-platform-session-feedback-fix（verify 返工后，分支未合 main）| 会话实时反馈事件链（方案 A 复用 `agent_session:{id}`）：schema.py 新增 7 DTO（PlanModeEntered/BashStatus/BashChunk/AgentTaskStatus 事件 + PlanSummary/PlanResponseDecision/PlanResponseRequest）；run_sync 新增 `publish_session_event`（Redis publish 静默容错）与 `publish_bash_chunk_event`（100ms 节流 + 8KB 截断 + is_final 必达 + monotonic + 4096 键防泄漏）；router 新增 `POST /api/daemon/sessions/{id}/plan-response`（归属/run 校验、revise|cancel feedback 必填、decision 落 `session.config.plan_response`、经 WS Hub best-effort 发 `daemon:plan_response`——**daemon 侧接收处理已随 verify P0 返工闭合（89649656：resolvePlanResponse 注入 turn，真环境 WS 闭环实证 delivered:true+帧到达）**）+ 4 个 daemon ingestion 端点（plan-mode-entered/bash-status/bash-chunk/agent-task-status，`get_current_principal` 鉴权 + path/body 一致性 422，转发 Redis）；SSE 生成器 `AgentService.stream_session_logs` 本就透明透传非终态事件，新事件零改动直达前端。测试 test_session_plan_bash_events.py 10 用例（发布/节流/端点 200-404-422 全路径）。
- ql-20260824-020-6f01 | `agent_run_logs` 加 `edit_patch` 列透传 Edit structuredPatch（配合前端进度视图真实文件行号 diff，方案 A）：`_extract_sdk_messages` tool_result 分支从 SDK user message 顶层 `tool_use_result`（daemon 原样转发，结构化 dict 形态）提取 `structuredPatch` hunks 经 `json.dumps(ensure_ascii=False)` 注入 flat record；`submit_messages` 落库 `AgentRunLog.edit_patch`（Text NULL，无索引仅随行读出）+ `published_logs` payload（run channel）与 `publish_submitted_messages` session_payload 双 channel 透传（`.get()` 兼容 override envelope）；`AgentRunLogEntry` DTO 加字段（from_attributes，main/agent/daemon 三处 logs 端点 `model_validate` 自动透传零调用点改动）。migration `20260824130000_agent_run_log_edit_patch`（down_revision 接并行变更 2026-08-24-platform-session-feedback-fix 的 `20260824120000`，串行链接避多头；batch_alter_table 兼容 SQLite/PG；注意该父迁移文件当前以未跟踪形式暂存主树、Docker DB 已应用，本 quick 不提交它）。测试：新 `test_extract_sdk_edit_patch.py` 3 纯函数用例（Edit tool_result 注入 edit_patch / 无 patch 不带 / tool_use_result 无 structuredPatch 不带），daemon+agent 全量 1785 passed；openapi.json 重生成（AgentRunLogEntry + edit_patch）。
- change 2026-08-24-session-team-mission-context | 会话团队任务上下文五层机制（task-01~14 全量）。**简报注入链路（FR-01/D-002/D-004/D-013）**：`agent/orchestrator.py` 抽 `collect_single_workspace_status` / `collect_scope_workspace_statuses` / `render_scope_brief` 共享渲染（patrol 路径不传 git_probe 输出结构等价）+ 新 `render_session_orchestrator_briefing`（会话主控首轮任务简报：主控角色+mission_id+目标+锚点工作区+派发范围（机器名/在线/模式）+dispatch_worker 用法+mission_status 提示+禁越权约束）；新 `agent/mission_context.py` —— `should_inject_first_turn_briefing` 一次性判定（纯空白 prompt 不注入不消耗、failed/killed 不烧断可重注、已有 pending/running/completed orchestrator run 不再注入、懒建回填的 pending run 天然短路）+ `build/resolve_first_turn_briefing`（inject/create 两路径共用入口；delegate 不可得降级省略模式字段不抛）。**三态探测与直通派发（D-006@v2/D-007@v1）**：`daemon/host_fs/delegate.py` 新 `probe_workspace_git_mode`（git/direct/unknown 三态，stat 绝对路径，非降级通道）；workspace 模块新 `POST /api/workspaces/probe` 批量探测端点（任一成员 binding 口径）；`execution.dispatch_worker` 按探测分流——direct 跳过 worktree 直通工作区根（root_path=ws 根、lease 不写 branch、`worktree_branch` 保持 None），finalizer 合并/清理按 `worktree_branch IS NOT NULL` 天然跳过直通 worker（混合 mission 只合并/清理 git worker）。**mission_status 查询路由（task-03）**：`GET /api/missions/status`（X-Session-Id 定位）+ `GET /api/sessions/{sid}/missions/status`，`MissionStatusResponse` DTO（scope 工作区机器名+在线+git 模式实时探测、workers 非 orchestrator run 概要）；无活跃 mission 200 `active=false`+hint 优雅返回。**E1 预建 + E2 解析（D-009@v2/D-010@v1/D-014@v1）**：daemon `SessionCreateRequest` 增 `team_mission` 块（`TeamMissionCreateBlock` 七字段=TeamMissionTriggerRequest+orchestrator_workspace_id，`validate_team_mission_block` 共享校验函数与 trigger 端点复用）；create 路径经 `_precreate_mission_flush` flush-only 同事务预建（中途异常整体回滚无孤儿 session/mission，objective 空用首句 prompt 回填）；E2 主 agent 工作区解析——`orchestrator_workspace_id` 缺省按 (锚点 W, 创建者) binding 钉定（binding 缺失 422），显式传但 ∉ scope 422。**收尾（task-14）**：混合 mission finalizer 直通跳过双断言（merge+cleanup）+ 简报 token 量化（scope=5 上界口径 ≤1500）+ gen:types 三端同步（openapi.json/api-types.ts 含 TeamMissionCreateBlock/MissionStatusResponse/probe 端点）。测试：test_mission_context/test_mission_status/test_session_team_mission/test_team_mission_create_block/test_delegate_probe/test_probe_endpoint/test_dispatch_worker_direct_mode/test_inject_first_turn_briefing 等，后端全量 5272 passed。
- ql-20260825-001-f85b | 会话相关代码审查修复（四路并行审查 backend/daemon/前端 后的批量修复，backend 侧 15 项）：**P0 SSE 钉死 DB 连接**——`core/auth_deps.py` 四处鉴权点（get_current_user/get_current_principal/两个 checker）在返回 User 前 `session.expunge(user)` + `await session.rollback()` 归还 PG 连接（裸 rollback 会过期 ORM 对象触发 MissingGreenlet，expunge 后分离对象已加载属性可同步读；此前 yield 依赖 teardown 要等 SSE 全部帧发完才归还，50 并发 SSE 即打满 20+30 连接池全站挂起），router.py 两处「零连接占用」错误注释同步修正；**SSE 通道加固**——`_stream_sessions_events` finally 两步清理隔离（unsubscribe 抛错不再跳过归还）+ 废弃 close→aclose、非 dict JSON payload 防御（list/str 不再炸流）、keepalive 改「距上次产出帧 ≥25s」强制心跳（原仅 get_message 超时驱动，繁忙频道下他人消息致本连接零输出被代理掐断）；**daemon 上行 5 端点归属校验**——ready/plan-mode-entered/bash-status/bash-chunk/agent-task-status 原仅 get_current_principal 无归属检查（任意认证主体可向他人会话伪造 ready/事件），session service 末尾新增 `get_session_for_runtime_owner`（join runtime 校验 api-key owner=runtime owner，对齐 end_session ql-20260623-004 先例，不匹配 404 不泄露存在性），5 端点发布前统一校验；**会话服务**——archive/unarchive 补发 publish_sessions_changed（并行合并遗漏，归档后其它客户端列表不刷新）、end_session/_end_session_for_delete 对齐 interrupt 模式（锁内收口 commit 后再 best-effort 发 SESSION_END，原锁内等 WS 发送最长 10s）、幂等扩含 failed（failed 会话不再被翻成 ended）、lease 仅非终态时收口；**run 状态机**——close_interactive_run 读 AgentRun 加 with_for_update（与 end_session 并发时 killed 不再被 completed 覆写）、LeaseSyncRequest.status 改 Literal 四值 + sync_agent_run_status 终态守卫（终态 run 收 running 记 warning 幂等返回，脏字符串不再穿透落库，openapi/api-types 已随 ed954822 重生成）；**边界**——recover/confirm/mark-recovery-failed 9 处早退补 rollback、SessionReadiness._events 改 pop 键有界、reopen_session runtime_id None 显式抛 DaemonSessionInvariantViolation（assert 在 python -O 下被剥除）、_end_session_for_delete 只查活跃 run、`agent/service.py` 两处 SSE 生成器同款 finally 隔离+aclose（F1 遗留补修）。测试：新增 test_auth_deps_db_release（4）/test_session_review_fixes（21）+ test_session_plan_bash_events 归属守卫 4 + keepalive/payload/断连清理等扩展；daemon 全模块 1018、agent 1336、l10n 116 绿。注：router.py/schema.py/session service 部分被并行会话 ed954822 一并带入提交。
- ql-20260825-002-3e67 | 会话优化第二轮（上轮审查搁置项，backend 6 项）：**inject 附件组装移出行锁**——普通读归属校验+gate 解析+MinIO 组装（_preassemble_inject_attachments，backend/app/modules/daemon/session/service/__init__.py）→ 收口只读事务 → FOR UPDATE 取锁 → 锁内重校验可变状态（status/lease 不变量/活跃 turn/gate 基准漂移复核 :2431-2450）→ 写入 commit，对象存储慢读不再占用行锁与 DB 连接（附件上限 5MB×5+20MB×5 原\"锁内等 MinIO\"窗口可数十秒）；**tool_report 懒激活透传**——_activate_tool_report_session 签名扩收 agent_profile_id/llm_provider_id/attachment_ids/prelocked_attachments（:1380-1385），切换字段落首轮 run 快照+lease metadata+会话三列与 config_snapshot，附件 draft→bound 回填+标记行+SESSION_INJECT payload；空 prompt 即使带切换字段走 DaemonSessionNotActive 409 中文拒绝（daemon 拒建空 prompt 会话，激活会留 pending 死轮 :1424-1437）；**活跃词表单源**——backend/app/modules/agent/model.py:37 `ACTIVE_RUN_STATUSES = frozenset({pending,running,pending_approval})`，daemon/session/service（保留 ACTIVE_TURN_STATUSES 别名）、daemon/router 两处 _session_has_active_turn、agent/finalizer、patrol、mcp_tools 全部切单源（原硬编码 [pending,running,interrupting] 漏判 pending_approval——审批中的 run 在 current_run_id/团队任务 awaiting_input 档位漏判；interrupting 为前端展示态不落库已去除），5 处\"含 interrupting\"陈旧注释同步修正；**_merge_lease_metadata 行锁**——raw SELECT 改 ORM select().with_for_update()（PG 渲染行锁 SQLite 自动忽略，防与 claim 并发丢 claim_token 更新）；**create_session 前导提前**——build_change_context_preamble/page_context 组装移到首个写事务前并 commit 收口只读事务（:1002-1030），磁盘遍历不再拉长写事务窗口（team briefing 依赖事务内 mission 行保留原位）；**pg_trgm 搜索索引**——migration 20260825150000（down_revision=20260824130000 单头已验）：CREATE EXTENSION IF NOT EXISTS pg_trgm + agent_run_logs.content_redacted GIN trgm 索引，PG 方言守卫双侧对称 downgrade（SQLite no-op），修会话搜索 q 前导通配 ilike 全扫。测试：test_session_optimize_round2.py 12 用例（锁外组装顺序/并发 end 重校验/激活透传全链/空 prompt 拒绝/pending_approval 判活跃/词表单源/迁移链+方言守卫）；daemon 1030+agent 942+tests/modules/daemon 78+core 184 绿、ruff 过；dev PG 未 upgrade（alembic check 提示待应用，部署时升）。
- change 2026-08-29-usage-by-provider-model（backend 侧） | 用量明细与模型选择：新表 agent_run_model_usage（run×model 四维+api_requests，UNIQUE(run_id,model)，迁移 20260829010000）；close_interactive_run/complete_lease 终态落明细（savepoint 幂等 delete+insert，best-effort）+ run.model 填最大消耗行 + llm_provider_id 仅空时填（R-08：dispatch 已写防切供应商竞态错归因）；InteractiveRunResultRequest 补 cache 两维（存量漏损：DTO 缺字段致 daemon 发送的 cache 自 task-16 起被 pydantic 静默丢弃）；get_runtimes_usage 增 by_provider 分组（COALESCE 去重沿用，NULL 供应商归「未记录」）；inject_session 扩 model 三态（None 不动/空串重置/非空选模，无供应商 422）+ 快照级 default_fallback_model 同步（R-07 消 injector 兜底遮蔽）；SessionCreateRequest 恢复 model 字段（D-002@v1）+ config_snapshot.model 显式优先。测试：run_sync 4/lease 7/runtime 6/inject 7/create 2/e2e 1 新用例 + 回归零失败，mypy 0。
- change 2026-08-29-daemon-platform-resilience（backend 侧） | 对接稳定性四场景改造：①控制指令可靠投递——新表 daemon_control_commands（kind String(32)/status pending|delivered|acked|expired/expires_at 按 kind inject 10min·permission_response 6min·其余 30min，迁移 20260829120000）+ ControlCommandService（enqueue/fetch_pending 仅 pending/delivered 不重发 D-006/ack/gc）+ enqueue_and_push 三段式（预生成 command_id 注入 payload→WS 推送→delivered，失败保持 pending）接入 session 七下发点+permission 三路径+provider_switch 共 11 处；GET runtimes/{id}/pending-controls + POST controls/ack 两端点（业务性失败也 ack 防毒丸）；心跳响应加 pending_controls 聚合计数；control_command_gc_sweeper 独立 60s 节拍+inject 两条过期路径联动 run failed(interactive_inject_send_failed 幂等)。②WS 断开 10s 延迟降级（执行时复查 is_connected(daemon_instance_id) 取消，D-007）+placement 三处候选联查 _runtime_row_ws_alive 剔除假在线行（_query_online 去 LIMIT 1 全量过滤）。③lease 过期 GC 接线 lease_expiry_sweeper 60s（经 handle_expired_leases_batch 单链路+facade 构造防 Redis 事件丢失）+lifespan 启动对在线 daemon pending lease 重发唤醒。④会话挂起：AgentSession.status 加 suspended（非终态可 recover）；suspend_sessions_for_daemon 三步幂等收敛（run failed(daemon_stopped)+session suspended+lease cancelled）+POST sessions/suspend-batch；offline sweep active→suspended（pending 维持 failed，D-007 本地无快照无人 recover）+24h 超龄 GC（coalesce(last_active_at,created_at) 基准）+suspended 只广播 status_changed；recover 维持非终态一律 reconnecting（不加白名单）。⑤PERMISSION_REQUEST HTTP 上行端点（X-Claim-Token secrets.compare_digest 常量时间比对，WS/HTTP 同源汇聚）；runs/result+session end 幂等（终态守卫重放 200 no-op）13 用例锁定。测试：新增 test_control_commands 15/test_control_command_dispatch 17/test_ws_disconnect_offline 12/test_lease_expiry_sweeper 7/test_session_suspend 15/test_permission_http_uplink 8/test_terminal_idempotent 5/test_resilience_integration 6 + 4 既有测试文件 fake hub 化（15 用例债清偿）+command_id 键集断言同步。
- change 2026-08-29-daemon-selfupdate-safety（backend 侧） | daemon_instances 加 pending_update JSON nullable 列（迁移 20260829150000）+心跳 upsert 三语义（同三元组保留原 since 哨兵测试/变化刷新盖 now/无字段置 NULL——与兄弟字段「非空才覆盖」反向，四处注释锚定单机单 daemon 无交错）+MachinePendingUpdateRead 子类注入 /machines 与 /runtimes/page（其余共用组装端点 shape 零变化实测锁定）；心跳响应体不动。reason 不收紧 Literal（心跳保活通道宁宽勿断）。5 新用例+142 受影响回归绿 mypy 0。
- change 2026-08-29-batch-session-inherit（backend 侧） | worker 会话中断重派继承——①分流挂起：suspend_sessions_for_daemon 与 session_offline_sweep_once 按 parent_session_id 分流（worker 子会话 failed+ended_at+AgentRun.error_code=daemon_interrupted+SuspendBatchResult.workers 种子；主会话 suspended 逐字不变回归锁定；pending 档不分流显式边界）。②自动重派（worker_redispatch.py 新文件）：prepare_interactive_dispatch 复用原子会话（不走 dispatch_to_daemon 防 worker 脱 mission 树）+双表上下文（AgentSession provider/cwd/tree_depth/agent_profile_id + 首 AgentRun model/objective/role/read_only/mission_id/worktree_branch）+prompt 按 objective 重渲染 build_worker_briefing+resume_session_id=agent_session_id（NULL 回退 run.session_id）注入 lease metadata+原 runtime 钉定（pinned_runtime_id+skip_owner_check——worktree 机器局部不可换机）+三互斥守卫（mission converged/cancelled+patrol④排除 daemon_interrupted or_防 NULL+30min worker_force_end 宽限窗标记+超窗双形态）+节流（kind=interactive lease 行数>=3）+suspend/sweep 事务后异步 fire 失败下轮 sweep 自愈。③claim interactive 分支补 resume_session_id 白名单透传（照 batch :795 先例缺省不下发）。22 用例+patrol 49+挂起 36+agent 模块 1194 全绿 mypy 零新增。
- change 2026-08-27-session-token-usage-fix（backend 侧） | AgentRun 新列 ctx_tokens；submit_messages 提取守卫（ctx_tokens LWW、其余四维 max）；SSE payload 与 SessionRunRead 透出 ctx_tokens

<!-- MANUAL_NOTES_END -->
- ql-20260815-002-c14e | 进度首推占位建行：platform_sync 接受上行后确保 ux_changes 占位行（变更中心可见）+ reparse 全量删除环对「progress 仍报 active 且无文档」的占位行不删（镜像滞后保护）。
- ql-20260815-003-0488 | 孤儿提问卡作废：run 终止（后端/daemon 重启恢复路径）时 pending dialog 置 cancelled + 两级 pending 列表过滤非活跃 run + respond 终态卡先于 run 检查给明确 409/404。
- change 2026-08-19-spec-mirror-tombstone-sync | spec 镜像全量同步删除对账收敛（墓碑语义补全，修「镜像只增不删」）：①`_write_spec_root` merge 循环后新增对账删除阶段——`_converge_stale_files`（静态 helper 整体 asyncio.to_thread）：基准=merge 实际落盘集（`landed_paths`/`landed_hashes`，收集点在 `_load_member` 成功后，local.yaml 天然不入集），镜像里不在落盘集的文件软删 move 到 `spec-backups/{ws}/{收敛批ts}/`（与增量 apply_ops delete 同构）+ 自底向上 rmdir 空目录（幽灵变更目录整目录消失）+ 复用 `_prune_spec_backups`；双护栏（FR-04）：落盘集空跳过（converge_skipped_empty_landing）/ 磁盘文件数 > 2×max(落盘集,200) 中止（converge_aborted_ratio，防坏 tar 清空镜像）。②manifest 对齐替代全表 DELETE（Q7/R-01 语义升级）：落盘文件行 version+1/exists=True（无行插 v1），对账删除文件行 exists=False+version+1 墓碑（保留乐观锁谱系，配合 ql-20260819-004 复活语义）；daemon 拉新 manifest 效果等价（version 全体推进）。③`_write_spec_root` 返回三元组 `(spec_ws, converged_files, converged_dirs)`，SSE done 事件 + sync_applied 日志加两字段 + spec_workspace.converged 结构化日志。④change 模块占位行保护加 7 天时效窗（FR-03，补 ql-20260815-002）：`PLACEHOLDER_PROTECT_WINDOW_DAYS=7`，`_progress_reported_active_keys` 按 `platform_change_progress.updated_at`（tz-aware，naive 归一 UTC；非 String(64) 的 last_pushed_at）过滤——一次性上行后停滞的占位行不再永久滞留「进行中」（生产实例 6 条根因），CLI 恢复时 `_ensure_change_row` upsert 重建不丢数据。reparse 触达零新增（对账删在 reparse 前，幽灵目录消失后既有删除环自然收敛 changes 行）。测试：新建 test_full_sync_convergence.py 5 用例 + test_reparse_guard.py 追加 2 时效用例 + 2 个断言旧保留语义的既有测试更新为对账/墓碑语义；两模块 468 passed，ruff 干净；无 OpenAPI 变化（SSE 流文本）免 gen:types。
- change 2026-09-02-changes-overview-card | 机器 sillyspec 进度总览：daemon_instances.sillyspec_status JSON 列（迁移 20260903090000）+ 心跳 None=清除落库 + register 恒清 + 机器视图嵌套透出（宽松 DTO 防 422 心跳保活通道）。test_machine_sillyspec 29 绿。
- ql-20260904-025-45f7 | 归档变更步骤时间线数据丢失修复（变更中心已归档变更详情页时间线整张不渲染）：根因=CLI 归档把目录改名「去源日期+拼归档日期」（如 2026-08-29-x → archive/2026-08-30-x），`_detect_renames` 同日期前缀匹配必然 miss → 旧行误判 orphaned 进删除环物理删 + `_delete_progress_rows` 连带清 `platform_change_progress` 收件箱 steps；修复=①`_detect_renames` 增描述段兜底（模块级 `_strip_date_prefix` 剥「YYYY-MM-DD-」前缀，唯一候选才配对，≥2 静默放弃防误配）②新增 `_rename_progress_rows`（主 commit 后独立短事务 best-effort，照 `_delete_progress_rows` 范式）：新名无行→源行 change_name 改名；新名已有行（CLI 归档后新名推过）→目标 steps 空且源非空则回填 latest_progress.steps 再删源行，防同变更双行投影不确定。测试：test_reparse_delete_closure.py 新增 3 用例（跨日期匹配+迁移/目标已存在回填合并/描述段二义放弃），`_seed_progress_row` 加 steps 参数 + `_seed_archived_change` helper；change 模块 500 passed + platform_sync 189 passed，ruff+mypy 干净。存量已丢 steps 的归档行不可恢复（项目未上线，按规则 11 不补历史兼容）。
- ql-20260904-027-25f6（backend 侧） | 修三件部署遗留之 backend 两件：②register runtime 归属自愈——`daemon/runtime/service.py` register_daemon 的 runtime 更新分支（else 支）原本从不写 `rt.user_id`，实例归属改绑（如管理员把机器从用户 A 改绑 B）后 runtime 行永挂旧用户，pending-controls/lease 等一切 runtime 级端点对 B 恒 404 且无自愈路径（实事故：7 runtime 全 404，本次先用 SQL 同步存量 7 行）；修复=else 支 `rt.user_id != user_id` 时对齐+`daemon_runtime_owner_realigned` 日志（register 已保证实例归属=注册者，runtime 是实例从属行）。测试 test_register_heartbeat_daemon.py 新增 `test_reregister_realigns_runtime_owner_with_instance`（A 注册→改绑 B→B 重注册→2 runtime 全归 B）。③entrypoint 启动阻塞修复——`docker-entrypoint.sh` 的 `chown -R app:app /data/spec-workspaces` 是死代码：容器以 USER app 运行，bind mount（两部署实际形态）chown 恒 EPERM 被 `|| true` 吞掉（67847 文件逐个失败实测 114s/次阻塞 alembic 前），named volume 属主已由 Dockerfile 构建期 chown 覆盖——直接删除该行。daemon 模块 1975 passed、register 文件 15 passed、ruff/mypy 干净、sh -n 过；daemon 侧同 ql 条目见 sillyhub-daemon.md。
- change 2026-09-04-conflict-resolve-entry（backend 侧） | 机器级 SillySpec 冲突裁决/幽灵清理指令通道：router.py 新增 `POST /machines/{instance_id}/sillyspec-resolve`（strategy Literal keep_local/take_platform 非法 422、change 走白名单正则首字符字母数字+长度 1-128）与 `POST /machines/{instance_id}/sillyspec-ghost-cleanup` 两端点（RuntimeAdminUser=RUNTIME_ADMIN 权限 + 非 owner 非平台管理员归属 404 + daemon 离线或 WS 发送失败 504 `DaemonRuntimeOffline`；fire-and-forget 不排队不落库）；protocol.py 两条 Server→Daemon 消息 `daemon:sillyspec_resolve`/`daemon:sillyspec_ghost_cleanup` + ws_hub.py `send_sillyspec_resolve`/`send_sillyspec_ghost_cleanup`（照 send_sillyspec_update 范式走 send_to_runtime 锁内 10s 超时）；心跳新字段 `sillyspec_command_result`（`DaemonHeartbeatSillySpecCommandResult` DTO）两态落库——对象=整包直写/键不出现=置 NULL 清除，落 daemon_instances 新 JSON nullable 列（迁移 20260904223000 仅 ADD COLUMN）+ register 恒清；`_build_machine_read` 经 `MachineSillySpecCommandResultRead` 在 GET /machines 机器视图透出。test_sillyspec_platform_commands 全绿；gen:types 已同步。
- change 2026-09-07-conflict-diff-compare（backend 侧） | 冲突对比编排端点（task-04）：`GET /machines/{instance_id}/sillyspec-conflicts/{change}/compare`（router.py :1621）——权限同裁决端点（RuntimeAdminUser + `_get_owned_instance` 越权 404）+ workspace 成员校验；change 路径段白名单 422 先于任何 RPC 外呼；RPC 腿 sillyspec_conflict_snapshot 显式 15s 超时，机器离线/超时走既有 504 家族、daemon 业务失败（DaemonRpcRemoteError）映射 502。新 `sillyspec_compare.py`（SillySpecCompareService）——asyncio.gather 并行「RPC 快照 + 平台侧 spec_root/progress 定位」、平台侧逐路径 containment（拒 `..`/resolve 落点须在 spec_root 内，越界按平台缺失不读取）、difflib 服务端 diff（四分类 + dropped_paths + 截断护栏：单文件 diff ≤5000 行、整响应 ≤2MB 按文件倒序丢 diff_rows）、progress 六字段白名单对比行（缺失显式「—」）；`DaemonHeartbeatSillySpecConflict`（:313）增量可选 `ql_id`（:329）零改写透传。测试 test_sillyspec_compare.py；daemon 侧见 sillyhub-daemon.md 同 change 条目。

- change 2026-09-14-session-export（backend 侧） | 会话一键导出：daemon 会话域新增 `POST /api/daemon/sessions/export`（SessionExportRequest：session_ids 1~50 + tier chat/full；TaskRunAgentUser 闸门；_ENDPOINT_ORDER 字面量前置于 /sessions/{session_id}）。服务层 session/service/export.py 模块函数——权限对齐详情口径（owner+软删 404→群参与者探测含影子读→404 不泄露）、自建 timestamp ASC+limit 20000 保最早截断（truncated/dropped_rows）、stdout 噪声排除纯函数派生助手正文（与前端 classifySessionLog 同源，表驱动 32 例锁定）、full 档 zip 含 MinIO 附件本体（512MB 预检 413/单附件失败 missing 降级）、RFC5987 中文文件名。test_session_export.py 52 用例；gen:types 已同步。

### 2026-09-07-agent-liveness-states（agent 会话活性状态推导）

- `platform_agent_logs` 增 state/state_derived_at/state_evidence/last_event_at 四列（迁移 20260907141041）；`POST /api/agent-logs/states` 批量 upsert-create（origin=liveness-discovered，X-001 裸会话通道）；blocked 段转移检测 + agent_blocked 通知（120s 段龄按 derived_at 差值、推送即调度、段级+unresolved 双去重）；`list_workers` running worker 附 WorkerLiveness（直查本表最新行，spike-01 定稿）。

### 2026-09-20-agent-log-session-replay（messages 响应扩展独立重做，replay-redo 分支）

- backend/app/modules/platform_sync/：schema.py 新 AgentLogUsage{input_tokens,output_tokens,cache_read_tokens,cache_write_tokens} + AgentLogMessageItem 五可选字段（turn_id/model/is_meta/turn_end/usage）+ Response.totals；router.py 转换层仅一行外层映射 totalUsage→totals（messages 内层 snake_case 零改名直通，与 2026-09-19 实现的逐字段映射路线对照）；老 daemon 全 None 兼容（pytest 新/老双态断言）。
