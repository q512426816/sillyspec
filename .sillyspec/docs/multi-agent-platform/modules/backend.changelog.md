# backend 变更索引

> 自动生成。正文历史已迁出，详见 backend.md。

- ql-20260919-001-25c7 | update_entry 坏编码守卫（24h 审查 M1 残余缺口收口）：知识文件网页编辑链 GET 经 parser._read_file_safe（errors="replace"）回传的基底对非 UTF-8 文件（Windows GBK 残留）已是 U+FFFD 版，update_entry 整文件替换保存会静默毁坏原始字节且 update 不进 spec-backups——ql-20260918-005 只收口了 merge/preview 的 _read_raw 路径。修法：新增模块级 _decode_knowledge_strict 共享 helper（_read_raw 内联 try/except 收敛进来），update_entry 大小守卫后对磁盘原文严格解码探测（≤1MB，坏编码抛既有 KnowledgeFileEncodingInvalid 422，文件与 manifest 版本不动）。附带 workspace/router.py probe_workspaces docstring 勘误（原「只读无状态变化」与 repo_url 回填写副作用自相矛盾）。测试：test_writer 27（+2：坏编码编辑 422+GET 基底含 U+FFFD 前置自检+磁盘原字节/manifest 版本未动；合法 UTF-8 中文对照组）+ 相邻面 test_router/test_parser/test_distill 72 绿；ruff/format + mypy scoped 0。
- ql-20260918-012-ac1b | 工作区 Git 地址自动识别（probe 端点 repo_url 识别回填，用户需求「工作区显示对应 git 地址、不手填」）：WorkspaceProbeItem 增 repo_url 字段——probe 端点对 git_mode=git 且 DB repo_url 为空的工作区经新 delegate 方法 git_remote_url（host_fs.git_remote RPC，`_via_rpc_or_degrade` 降级 fail-safe）实时识别，识别成功且值变化时回填 workspaces.repo_url（本端点唯一写副作用、仅此一列；已识别直接回 DB 值零额外 RPC，首次识别后 RPC 开销回基线）；direct/unknown 态为 None，RPC 降级（含旧 daemon 不识别新方法 → DaemonRpcRemoteError）归 None 不抛。gen:types 同步 openapi.json/api-types.ts。测试：test_probe_endpoint +4（识别回填落库/已填充跳过 RPC/direct 不发 RPC/降级 None 不 5xx）共 13 passed；ruff/format/mypy scoped 0。
- ql-20260910-004-db16 | answered_by 透传链补全（askuser-pi-cursor 已知限制②③收口）：SessionDialogRead/WorkspaceDialogRead 增 answered_by 字段（from_model 透传，历史/恢复读可渲染答题人）；已答 409 DaemonDialogAlreadyResolved details 携带 answered_by（第二答题端即时数据源）。测试：test_session_permissions 41 passed（+1 透传双通道断言）+ workspace dialogs 9 不回归。
- ql-20260910-001-4560 | 影子会话 pending 提问读侧放开（2026-09-09-askuser-pi-cursor 已知限制①收口）：list_pending_dialogs 的 ownership 404 门对群聊影子会话（session_kind=group_member）复用 task-09 落地的 _resolve_shadow_member_answer_session 影子成员分支（群成员+未移除+软删群过滤，条件与答题侧同源）——群成员可见成员提问卡，关闭「能答却看不见」缺口；越权反例（外人/已移除成员/普通单聊非属主）仍 404 不泄露。测试：test_session_permissions 40 passed（+3：成员拉取成功/外人+移除成员 404/单聊语义零变化）。
- change 2026-09-09-askuser-pi-cursor | AskUser 群聊授权放开+caps 镜像：①permission_service 影子会话答题授权放开——respond 的 ownership 404 门对「群聊影子 dialog 行应答」走 _resolve_shadow_member_answer_session（session_kind='group_member'+resolve_shadow_member 未移除群成员+软删群过滤+FOR UPDATE），manual_approval 第二道守卫对 ask_user 类 dialog 豁免（权限审批语义不动，单聊零变化）；answered_by 记实际答题人（修正影子场景记群主失真）+SSE permission_resolved 增 answered_by_actual_user 契约字段。②provider_caps 增 dialog 三值镜像+对齐测试 _TS_BOOL_PAIR_RE 扩 string 枚举（键 8→9，未知回退 none）。测试：test_session_permissions 37（+9 影子授权含越权反例）+对齐 4；verify 真实 HTTP 集成断言（外人 404/成员过全门禁）。
- ql-20260909-016-fa10 | init lease 凭据断链降级补日志（docs/sillyspec/init-lease-silent-no-local-yaml.md 修复建议①）：build_claim_payload 的 mode=='init' 注入分支补 else——actor_user_id/workspace_id 缺失或非法导致防御降级（不签 token、不注入 local_yaml）时 log.warning("init_claim_local_yaml_skipped", lease_id, reason="missing_or_invalid:<缺失字段>")，降级语义零变化；此前 claim 照常成功 + daemon 侧跳过写盘也零提示，local.yaml platform 段缺失整链静默（2026-09-09 平台同步断链排障实证）。测试：test_init_claim_tokens 组 C 两用例补 capsys 断言（structlog 控制台渲染不进 stdlib caplog）——warning 事件在场 + reason 含对应缺失字段名，8 passed，ruff/format/mypy 0。
- ql-20260903-013-b057 | backend-ci 连续 8 次失败三处测试修复（CI 扫描，生产代码零改动）：①test_delegate_worktree 9 例 TypeError——6f92dc49d 给 worktree 三方法显式传 timeout=150s 但 _MockWsRpc.send_rpc 缺 timeout 形参，mock 对齐 HostFsWsRpc 协议签名（delegate.py send_rpc 协议）并记入 calls（既有断言按键取值不受影响）；②test_worker_redispatch::test_daemon_interrupted_run_not_recovered assert 0==1——6a2248ccc 职责④ NULL resume_token 跳过守卫把无 token 对照 run 一并挡下，_make_run 工厂补 resume_token 形参，两条 run 都带 token 使中断 run 的排除只可能来自 error_code 过滤（保测试原始意图，不搭 token 守卫便车）；③test_platform_deleted_hidden_migration 8 例 CI 偶发（02:39 run 实证，本机难复现）——_load_migration 子串匹配可命中 30f7418b14cf_merge_heads_20260829130000_.py（merge 迁移 slug 嵌父 revision 字符串，os.listdir 顺序敏感随机遮蔽），改 alembic 命名约定精确前缀 f.startswith(f"{revision_id}_")。worktree 10 + redispatch 26 + migration 15 用例绿，ruff/format/mypy 0；其余 7 个迁移测试共用同款子串加载器（当前无遮蔽文件名、暂无风险），模式债留档本次不扩面。
- ql-20260903-011-1b58 | CLI 合成鉴权错误（远端 401 误报 Not logged in）自动重投一次：run_sync.close_interactive_run 终态 commit 后挂 _maybe_autoretry_auth_transient_turn——error.raw 命中签名（Not logged in / Please run /login，模块级 _CLI_AUTH_TRANSIENT_RE）且会话仍 active → 本 run 的 user_input 追加为 agent_session_queued_messages 排队消息（携带 run 的 llm_provider_id/agent_profile_id 快照 + sender=run.user_id + position=MAX+1），由 close 末尾既有排队派发钩子（ql-20260825-011）随即重放；防循环双保险——紧邻上一条同会话 run 同为鉴权失败且 user_input 相同=已是重投结果不再追加 + 同文 pending 去重；全程静默容错（rollback+warn 不影响已 commit 终态）。背景：claude CLI 把模型网关 401 统一合成误导文案且 retryable=false，实证同一进程同密钥 13 秒后重发即成功（2026-09-03 会话 cb56fabf）。test_auth_transient_autoretry.py 5 用例 + close 既有 7 用例回归 12 绿 ruff 0；坑记录 docs/sillyspec/2026-09-03-cli-401-misreported-as-not-logged-in.md。
- ql-20260826-012-2802 | 性能批次二 + incident 时区修复：**incident 时区**——incidents/postmortems 共 5 时间列是全仓唯一「DateTime 无 timezone + naive utcnow 默认值」例外（service 层一直写 aware，asyncpg 剥 tz 后 naive/aware 混比 TypeError），model 改 DateTime(timezone=True)+now(UTC) 默认值，迁移 20260826210000 用 USING AT TIME ZONE 'UTC' 平滑转换（用户确认数据可重置）；**platform_sync upsert N+1**——upsert_agent_log_entries 循环内逐 entry select 改 (workspace_id, log_path) IN 批量预取 dict 命中（复合唯一约束保证语义等价，全量重推几百条 entries 由几百次查询降为 1 次）；**workspace 状态收集 N+1**——collect_scope_workspace_statuses（mission_status 入口）与 probe_workspaces（弹层批量探测入口）原每 ws 4 条查询（4N），新增 collect_many_workspace_statuses 共享组装函数 _workspace_status_entry，3 条固定查询（workspaces/bindings/daemons 各一次 IN；online=属主匹配+status=online 等价 query_daemon_online_by_id），git_probe 仍逐 ws 回调；条目顺序契约钉死跟随 scope_workspace_ids 声明序——IN 查询不保证返回顺序，首跑暴露简报渲染乱序后按 valid_ids 重排修复；**三个无界列表补固定上限 200（不动 OpenAPI 免 gen:types）**——list_dialog_history（取最近 N 反转恢复创建序）、list_session_runs（已 desc 加 limit）、list_change_sessions（SQL 加 nulls_last+desc+limit 取数窗口，Python 最终排序保留）。测试：upsert 查询计数解耦（3 vs 12 条 execute 次数差 ≤1）、批量与单查口径逐字段相等 + 查询数恒 3、三处上限裁剪各 1 用例（常量 monkeypatch 小值+超额播种）、incident 默认值 aware+UTC 零偏移钉死；aiosqlite 往返丢 tz 为已知测试库差异不做读回断言。
- change 2026-08-28-session-ppm-task-binding | 会话关联 PPM 任务/问题（绑定 + 上下文前导注入）：**ppm common/** 新增 `session_binding.py`——`ppm_item_session_links` 表（单表 kind=plan_task/problem + item_id/session_id/workspace_id，唯一约束 `uq_ppm_item_session_link_pair`，迁移 20260828100000）+ `bind_session_to_ppm_item`/`resolve_item_workspace_id`（项目关联工作区 workspace_id 升序取首个，D-004@v2）/`load_ppm_item`/`load_item_files` helper；`common/router.py` 新端点 `GET /api/ppm/item-sessions`（kind+item_id 返回本人关联会话，软删过滤），main.py 挂载 ppm_common_router（跨 task/problem 两子域中立落点，对齐 common/crud.py 先例），common/tests 新增 test_session_binding.py 15 用例。**daemon 模块**——schema.py `SessionCreateRequest` + `ppm_item_kind/ppm_item_id`、`SessionInjectRequest` + `bind_ppm_item_*`（成对校验，只传其一 422）；session/context.py 新增 `build_ppm_item_context_preamble`（【PPM 任务上下文】/【问题上下文】标题/描述/状态/项目/责任人/周期全字段前导，消费附件行）；session/service.py create_session 绑定/工作区解析回填/附件物化接线（`_materialize_ppm_attachments` 走 FileService._can_access 口径 + session attachment storage 物化，五类降级；物化在前、前导消费 attachment_lines、事务拆分 flush-only；item 不存在降级普通会话仅 warning 不报错）、inject 追问绑定分支（唯一约束幂等）、`list_agent_sessions` ppm 子查询筛选；router.py 三层透传；新增 test_ppm_session.py 14 用例 + 既有 test_session_service/test_sessions_list_filters/test_change_session/test_page_context_preamble/test_inject_first_turn_briefing 适配。**alembic merge 6756e634f119**（合并 20260828100000/20260828120000 并行双 head）。sillyhub-daemon 仓零改动（附件走既有 SESSION_INJECT 协议与 session-attachments 下载端点，D-006 物化保 id 兼容）。真实容器集成验证 8 场景通过（含前导真实注入 daemon lease prompt）。
- ql-20260826-011-da0a | 全仓排查批量修复（8 项，缺陷 5 + 性能 3）：**IDOR 越权**——incident 4 端点（get/update/postmortem×2）与 release 5 端点（approve/deploy/promote/rollback/approvals）原用 require_permission_any（任意工作区有权限即过）+ 裸 id 取对象，A 工作区用户可读/改/审批/部署 B 工作区单据；改 get_current_principal + 新增 _require_*_permission 对象级校验（has_permission(workspace_id=obj.workspace_id)，对齐 agent/file 惯例；approvals 收紧为 WORKSPACE_READ）；**preview_office 令牌死签**——issue_file_token 原吞 Redis SET 异常仍签发，consume 端要求 jti 键存在致恒 410 无日志，改 fail-fast 503（新错误 PreviewFileTokenStoreUnavailable）+ docstring 同步；**diff_collector 孤儿进程**——两处 wait_for 超时路径补 _kill_proc（kill+wait，对齐 git_gateway）；**parser 缓存竞态**——_MODULE_MAP_CACHE 写侧（插入+stale 迭代清理）持 threading.Lock（to_thread 多线程迭代+插入可抛 RuntimeError）；**性能**——llm-proxy 转发客户端改进程级共享单例（_get_llm_proxy_client/close_llm_proxy_client，lifespan 关停回收，原每请求 new AsyncClient 零复用=每推理一次 TLS 握手）；publish_submitted_messages 两路 channel 各自 pipeline 批量发布（原逐条 await publish，几百条日志=上千串行 RTT，pipeline 保序）；Settings.resolved_commit_sha git 探测结果缓存（原每次 access 同步 spawn git，/health 高频轮询周期性阻塞事件循环）。测试：incident router +5（跨区 403×3/平台管理员放行/deny 正控）、release router +4（approve/deploy/promote/approvals 跨区 403）、preview +1、diff_collector +1（kill 断言）、parser +1（锁串行化）、config +3（探测一次/失败缓存/显式跳过）；test_llm_proxy 加单例复位 autouse fixture；session_sse/assistant_override 假 redis 补 pipeline 假件（AC-06 失败注入改 execute 时抛）。不改 OpenAPI/DTO/migration，无需 gen:types。
- ql-20260826-007-8666 | workspace slug 创建后锁定不可变：errors.py 新增 `WorkspaceSlugImmutable`(400 HTTP_400_WORKSPACE_SLUG_IMMUTABLE)；service.update 删 slug 唯一性预检改为「显式传不同值即 400，同值幂等放行」（slug 是 mirror 目录名/lease 元数据稳定键），R14 并发注释收紧为仅 root_path（uq_workspaces_slug 不再可能触发）；WorkspaceUpdate docstring 同步。test_router 旧 409 冲突用例改写为不可变 400（占用/未占用双断言）+ 新增同值 no-op 200；gen:types 同步 openapi.json/api-types.ts（仅描述透传，形状零变化）。
- ql-20260825-009-ca4d | 团队任务简报注入 workspace root_path——collect_single_workspace_status/render_scope_brief/render_session_orchestrator_briefing 渲染行加 path= 字段 + ScopeWorkspaceStatus DTO 加 root_path，主控 agent 可只读调研定位；简报 token 预算 1500→1600。
- change 2026-08-28-session-ppm-task-binding/ql-20260828-003-f2b4 | ppm common item-sessions 端点前置 data_scope 条目可见性守卫（不可见返回 []，堵越权枚举他人会话）；create_session PPM 物化三阶段并行化（资格判定保图≤5/文≤5 顺序水位→gather 并行读写存储→原序组装降级）；item 全链传参 load_ppm_item 3 次降 1 次
- ql-20260828-012-2a3e | set_my_binding_shared 补 daemon 归属校验（防「共享的共享」）：quick-18951370 放宽借用绑定后，成员可绑他人共享的 daemon，但共享开关端点无归属校验——借用者能以自己名义 upsert enabled workspace grant，原 lender 撤销（自己那行 grant disabled）后借用者的 grant 仍在，撤销语义被击穿。修复：service 层 binding 有 daemon_id 时校验 daemon.user_id==调用方，属他人 → 403 daemon_not_owned（不开 grant 不写 shared 列）；daemon_id null 的无 daemon binding 维持原行为（本就不开 grant）。新增用例 test_quick4a55e2dc_borrower_cannot_reshare_shared_daemon（借用者 403 + grants 仍只有 lender 一行 + binding.shared 未被置位），文件 25 用例全绿 + ruff/mypy 净。
- ql-20260828-013-1a7d | backend-ci mypy 拦门修复（3d8432d9 引入，mypy 挂在 pytest 前 → 后端测试自该提交起在 CI 未跑到）：①ppm/common/session_binding.py load_ppm_item——条件表达式联合 type 变量进 select() 被 SQLAlchemy 类型重载归并到公共基类（BaseModel），返回类型与签名不符（return-value），改按 kind 分支各自具体类型 select；②daemon/tests/test_ppm_session.py _connect_mock_ws——在 mock 对象属性上写类型注解（misc non-self attribute），改局部 sent_messages 列表再赋给 mock 属性。顺手修 auth captcha 契约测试环境敏感（local.yaml C 段）：本地 .env AUTH_CAPTCHA_ENABLED=false（592e0435 开发开关，gitignored，CI 无此文件走默认 True）使 needs_captcha 恒 False → token 不被消费，token 一次性消费 + 401 明示两用例本地必红（CI 实际会过），fake_redis fixture 显式钉 auth_captcha_enabled=True 消除本地/CI 差异（免验证开关用例自会显式改回 False）。mypy 762 文件 0 错 + daemon/ppm 相关 109 用例 + captcha/migration 守护 21 用例绿 + ruff 净；alembic 双 head 已自愈（单 head 6756e634f119，D 段可移除）。
- ql-20260828-015-796d | backend-ci 首跑 Pytest 全量暴露三预存债（9569a7a9 mypy 解锁后首次执行，均为 8/27 后提交引入但从未在 CI 跑到）：①daemon/router.py 会话列表 ppm 成对 422 detail 英文文案违 l10n 守护（3d8432d9），与 schema.py _require_ppm_item_pair 同口径中文化「ppm_item_kind 与 ppm_item_id 必须成对提供。」（grep 证实无测试断言旧英文串）；②test_agent_run_log_tool_kind.py PublishIntent 构造缺 task-05 新增字段 ctx_tokens（TypeError），补 ctx_tokens=None（None → payload 不带该键，原断言语义不变）；③test_login_captcha 禁用开关用例 monkeypatch.undo() 连 fixture fake_redis 补丁一并撤销——CI 无真实 Redis 时 record_login_failure 降级返回 0，失败计数静默丢失 → 第 3 次 401≠423（本地有 Redis 跑绿是假象）；去 undo 改显式 setattr 翻回开关，且全程 fake_redis 后 7 请求会撞默认 5 次/分钟限流 429，同步调高 auth_login_rate_limit_per_minute=100 隔离关注点（限流另有专测）。l10n+tool_kind+captcha 127 用例 + daemon ppm/list_filters 57 用例绿，mypy 0 错 ruff 净。
- ql-20260829-004-a7ef | permission-requests HTTP 上行补 runtime 归属校验：handle_permission_request_http 新增 principal_user_id 参数，principal 不 own 会话所挂 runtime（daemon_runtimes.user_id）→ DaemonSessionNotFound 404 resource-hiding（对齐 pending-controls owner-only 惯例；归属校验先于 claim_token，借用 runtime 场景 runtime 属 lender=daemon 凭证主体不受影响），router 传 user.id；新增跨主体 404 用例 + 正向用例种子对齐 admin 主体。顺手清偿 test_inject_session_model.py 6 处 mypy Optional 预存债（config_snapshot/details 前置 is not None 收窄；c4e4f385 引入，会挡 commit hook 全量 mypy）。uplink 9 + inject_model 7 用例绿，mypy 776 文件 0 错。
- change 2026-08-29-session-usage-stats | 会话内 Token 用量统计（backend 侧）：GET /api/daemon/sessions/{id}/usage 聚合端点（owner-only 404 resource-hiding）+ SessionUsageRead/SessionUsageModelItemRead DTO——SessionService.get_session_usage 两段全 SQL 聚合（agent_run_model_usage GROUP BY model 为主 + 无明细行 run 四维列 NOT EXISTS 兜底，ctx_tokens 快照列排除，「未记录」桶恒末位 api_requests=0），test_session_usage.py 7 用例（纯明细跨 run SUM/纯兜底 COALESCE/混合同名桶相加/空会话/归属三态）；gen:types 三端同步。命中率口径 D-003 前端算。
- ql-20260831-002-f683 | 会话级上下文窗口覆盖（前端上下文环分母可编辑，本地模型/本机默认派生不出分母的主场景）：agent_sessions 加 nullable INT 列 ctx_window_tokens（迁移 20260831120000，NULL=未覆盖走前端自动链 one_m→常量表→1M 兜底）；AgentSessionRead 透出该列；新端点 PATCH /api/daemon/sessions/{id}/ctx-window（SessionCtxWindowUpdateRequest，ctx_window_tokens None=清除 / 边界 1_000~100_000_000 防 0/负/巨值，204；照 archive 模式 service owner 校验 404 resource-hiding、幂等同值早退）+ DaemonService.update_ctx_window 一行委托。纯展示配置：不进 daemon 注入链、不发 agent_sessions:changed 列表信号。test_session_ctx_window.py 10 用例（设置/清除/幂等/跨主体 404/零信号/DTO 边界 4/Read 序列化）；gen:types 同步 openapi.json+api-types.ts。
- change 2026-08-31-session-queue-ux | 会话消息排队体验修复与增强（backend 侧）：**position 排序列（FR-04/D-002）**——agent_session_queued_messages 加 position INT NOT NULL（迁移 20260831130000 三步走：加 NULL 列→CTE ROW_NUMBER() OVER (ORDER BY created_at, id) 回填→SET NOT NULL，对齐 202607240900 先例；不加唯一约束——行锁串行+created_at 次序兜底）；入队行锁内 position=MAX(position)+1（含 falsy-zero 缺陷修正：or 兜底在 max==0 时回卷致连续入队全 0，改显式 None 判）；派发序 list/dispatch 两处 ORDER BY position, created_at。**dispatch 循环化（FR-01/FR-02）**——外层 while 连派至无可派发/会话忙：非 active 且非终态（pending/reconnecting/suspended）rollback 返回保持 pending 不动；仅终态 {ended, failed}（词表无 cancelled，D-010）批量转 failed；连续失败 ≥2 即停整轮（R-05 防系统性故障连环刷屏），成功清零计数。**恢复钩子（D-008）**——confirm_session_reconnected 翻 active 的 commit 后 fire 补派发（SessionService 新增 _fire_background_task helper，照 run_sync 强引用防 GC+独立 DB session 模式；无 pending 自查自弃）。**三端点**——PATCH /sessions/{id}/queue/reorder（ids 全量校验，多/少/错/重复→422 QUEUE_ORDER_MISMATCH，路由先于 /{entry_id} 注册防路径参数吞字面量）；PATCH /sessions/{id}/queue/{entry_id}（prompt 1..8000；TASK_WAKEUP 系统通知条目 409 不支持编辑，防 like 去重匹配失配，D-009；failed 保存重置 pending+清 error+尝试派发）；POST /sessions/{id}/queue/{entry_id}/dispatch-now（行锁内 failed 重置+置顶全量重写 position→空闲当场派发/忙时 interrupt 接力，抽 _send_interrupt_control 供 interrupt 端点共用，置顶 commit 先于 interrupt 下发不丢语义 R-03）；三动作均补发 queue_changed（reordered/edited/dispatch_now）。**缺陷修正**——rollback 过期 ORM 实例两处（reorder MISMATCH details 与 dispatch-now 非 active 拒绝路径：标量快照先取再回滚，否则 MissingGreenlet 500）。测试：新 test_session_queue_actions.py 27 用例（MAX+1/reorder 全量与四态 MISMATCH/edit 三态与 TASK_WAKEUP 409/dispatch-now 空闲与忙时 mock hub/循环化连续失败上限与瞬态续派/非 active 保持 pending/终态批量 fail/confirm 恢复钩子）+ 既有 test_session_queue 与 test_session_user_preamble 适配，54 用例绿；gen:types 同步 openapi.json+api-types.ts（三端点+四 schema+position）。
- ql-20260904-005-a41b | CLI 鉴权自动重投防副作用重复（24h 审计 M1）——_maybe_autoretry_auth_transient_turn 补 tool_call 活动守卫：401 发生在 turn 中途（本 run 已有 tool_call 日志）时跳过重投（重放会再执行已落地的工具副作用）；仅无工具活动的干净轮（首 LLM 调用即 401）保持自动重投自愈。新增 test_tool_activity_skips_enqueue 回归（6 用例绿 ruff/mypy 0）
- ql-20260909-008-d78c | 会话列表首条 user_input 摘要查询三处性能优化（阿里云 2026-09-09 slow.query 实测 3.1s）：daemon/router/session_crud.py、change/router.py _fetch_session_titles、agent/router.py 三处同款窗口函数（ROW_NUMBER 分区取每会话首条 user_input 做标题摘要）原样 SELECT content_redacted 全文（线上单行均值 33KB/上限 50KB）回 Python 只取前 30 字——改 SQL 内 substr(content_redacted,1,64) 截断（PG/SQLite 双方言语符语义，64>30 保证派生零回归），免 TOAST 全文解压与传输；session_crud 一处额外只对 title 为空的会话查询（有 title 会话跳过，tool_report 会话带自动标题，下方 get(r.id) or 派生不变）。纯性能优化零行为变化；三模块 3887 用例绿 + ruff/format/mypy 净。
