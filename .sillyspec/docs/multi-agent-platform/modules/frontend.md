---
schema_version: 1
doc_type: module-card
module_id: frontend
source_commit: ba87eec
author: qinyi
created_at: 2026-06-24T01:16:42
---
# 前端控制台（frontend）

## 定位

multi-agent-platform 的 Web 控制台，用户操作平台的唯一图形入口。基于 Next.js 14 App Router + React 18 + TypeScript 构建，向用户呈现工作区、运行时会话、SillySpec 变更中心、PPM 项目管理、Agent 运行面板、权限审批、健康状态等功能。运行时依赖 backend 的 `/api` 接口；daemon 相关交互经 frontend 的 `/api/daemon*` route handler 与后端/守护进程协调。

技术栈：Next.js 14.2、React 18、TypeScript、Tailwind CSS 3.4、Ant Design 6 + @ant-design/icons、Radix UI、TanStack React Query（数据层）、Zustand（状态）、Zod（校验）、ECharts（图表）、@xyflow/react（流程图）、Vitest（单测）、Playwright/Puppeteer（E2E）、pnpm。

## 契约摘要

对外契约是浏览器渲染的页面与少量 BFF route：

- **页面路由**（App Router）：根 `page.tsx`；`(auth)/login` 登录；`(dashboard)/` 下含 workspaces、runtimes、settings、admin、ppm、**agent-profiles** 等区域，各自带 `layout.tsx`。
- **AgentProfile 前端**（2026-08-04-agent-profile-ui-redesign）：`(dashboard)/agent-profiles` 全局卡片墙页（独立一级菜单，跨工作区聚合视图）+ ws 内页 `workspaces/[id]/agent-profiles`（复用卡片墙 + workspace 预筛）；组件 `components/agent-profile/`（agent-profile-card 角色卡 / card-grid 卡片墙 / preview 人设预览）+ 重做 `agent-profile-form`（900px 双栏左填右实时预览 + 工作区上下文选择器）+ `agent-profile-select`（选档下拉 antd Select）。
- **BFF route handlers**：`src/app/api/` 下 daemon、daemon-chat、workspaces，承接需要服务端代理的 daemon 通信与 SSE/WS 转发。
- **后端依赖**：所有领域数据来自 backend `/api/*`；daemon 实时会话走 WebSocket/SSE。**模型错误可见性**：run failed 时 `listSessionRuns`（`lib/daemon.ts`）取 `GET /api/daemon/sessions/{id}/runs` 的 error_detail，normalize（`agent-log/normalize.ts`）生成 error 类日志项，`RunErrorItem` 渲染原因/hint/actions（重发/切换供应商/详情）；agent 页（agent-run-panel）与 runtime 聊天窗（interactive-session-panel）两面接通。重试按钮自 ql-20260904-010 起对所有失败卡显示（不再按 retryable 门控，重试约束由提交守卫承担）；会话页「切换供应商」定位会话底部 SessionConfigBar 供应商下拉（providerOpenSignal 信号触发，不再跳 /settings），dialog 浮窗/agent 页仍跳设置页。ql-20260904-013 起会话页时间线（session-log-assembler）与 normalize 同口径丢弃 `[ASSISTANT]` 模型网关/CLI 鉴权错误特征行（不再渲染成回复气泡，展示归 RunErrorItem 失败卡）；logsToTurns 对带前导块的 user_input（影子直聊等无「干净条」兜底的路径）剥前导后剩余正文进 prompt（用户气泡可渲染、失败卡可重发，常规双写路径同主体归并不双显）。
- **工作区 Skills 页双栏完整文件编辑**（2026-08-26-workspace-skill-edit）：`workspaces/[id]/skills` 页从只读列表（旧 D-006）升级双栏——左栏 skill 卡片+文件树+新建文件/删除文件/删除 Skill（SKILL.md 删除禁用）+「新建 Skill」对话框（radix Dialog，名+描述）；右栏 textarea 编辑器（未保存标记/保存/重置，保存 toast「已保存（下次同步对新会话生效）」）；删除二次 confirm（skill 级明示目录不可恢复）；前端校验 validateSkillName/validateSkillFilePath 与后端白名单同口径（中文报错）；数据层 5 fetch+4 mutation（workspace-skills-view.ts，写后失效 workspaceSkillsView+workspaceSkillFile 双键；queryKeys 新增 workspaceSkillFile.detail）。
- **工作区 MCP 配置页双态**（2026-08-26-workspace-mcp-edit）：`workspaces/[id]/mcp` 页从只读（旧 D-006）升级查看/编辑双态——编辑态 textarea JSON + zod 校验（`required_error`/`refine` 可靠写法，中文错误定位 server 名：`server "X"：仅支持 stdio 类型…`）+ 三条提示（`<set>` 保留语义 / 平台白名单生效条件 / agent 画像 mcpRefs 可能收窄）；数据层 `workspace-skills-view.ts` 新增 `updateWorkspaceMcpConfig` + `useUpdateWorkspaceMcpConfig`（成功 invalidate `workspaceMcpConfig.detail`），手写类型替换为 api-types 生成别名（McpConfigUpdateRequest/McpServerEntryPut/View）；api-types/openapi.json 重生成（PUT + daemon workspace_id query）。
- **文件全屏预览贯通**（2026-08-26-file-fullscreen-preview）：统一预览弹窗 `FilePreviewModal` 增全屏态（`defaultFullscreen` prop + 工具栏 Expand/Compress 切换 + 100vw/100vh CSS 伪全屏 + `fill={fullscreen}` 透传渲染器；普通态零变化，四既有入口零改动）；渲染器层 `PreviewerProps` 增 `fill`（六渲染器高度适配）+ 新增 HtmlPreviewer（iframe sandbox，原型 html 可全屏）+ registry 增 html/svg/bmp/ico 分发；变更文件树（change-file-tree）非文本态改造（图片内联 antd Image 可缩放/其他类型文件卡片）+ 工具栏「全屏预览」（fetch 恒走 `lib/change-files.ts` 新增 `fetchChangeFileRaw` 裸 fetch+Bearer，D-009 不走 1MB 截断的 content 端点）；explorer 文件浏览器图片改 antd Image + 头部「全屏预览」全类型可用（target 不携带 officeSource）。零新增 npm 依赖。
- **构建产物**：`next build` 产出独立 Node 服务，Docker 中以独立容器运行，端口对 backend 反代或直连。
- **浏览器级 E2E**（2026-08-29-frontend-e2e-playwright）：`frontend/e2e/`（env/fixtures TestApiClient/helpers + auth.spec/navigation.spec）+ `playwright.config.ts`（chromium/workers:1/zh-CN）；身份=bootstrap admin 造数 + run-id 唯一用户挂 workspace:read 角色，API 登录注入 localStorage persist v1（格式与 session.ts 落盘形状锁死，user 复刻 fetchMe 映射）；**后端登录 account=username 非 email（D-001，backend/app/modules/auth/service.py）**；双栈隔离 vitest exclude e2e + tsconfig include e2e；运行 `pnpm test:e2e`（前置见 e2e/README.md，限流须放宽 60）。

## 关键逻辑

- **目录组织**：`src/app`（路由）、`src/components`（40+ 业务组件，含 daemon/、agent-log/、layout/、charts/、permissions/、ui/ 子树及大量 ppm-/workspace-/admin- 前缀组件）、`src/lib`（工具/API 封装）、`src/stores`（Zustand）、`src/styles`、`src/test`。
- **核心组件**：app-shell（外壳布局）、top-bar、workspace-tabs、mission-console（任务控制台）、agent-run-panel、agent-log-viewer、runtime-session-dialog、permission-approval-dialog、ask-user-dialog-card、health-card、server-status-card、sillyspec-step-progress、**run-error-item**（模型调用失败结构化展示：type→图标/颜色/文案/hint/actions）。**changes-overview-card**（2026-09-02-changes-overview-card：工作台活跃变更总览卡片——健康条/变更行管线/ghost 折叠/冲突区/过滤 tab/null 占位与数据过期标记/32KB 超限计数降级，消费 GET /machines 的 sillyspec_status_map 工作区级快照（14a50351d 起按当前工作区取，缺席显式占位不串台；旧 daemon map 为 null 回退机器级 sillyspec_status 单槽位）；platform-sync-section 同款取数自 ql-20260910-012 起）。
- **数据层**：React Query 管理服务端状态，Zustand 管 UI/会话状态；daemon 聊天与权限流为长连接交互。
- **脚本**：dev/build/start/lint/typecheck/test，CI 跑 lint+typecheck+test+build 全链路。
- ql-20260915-013-0433 | 手机端会话页二轮收敛（用户实机反馈「字体太大、下面太乱，要简洁大气一行内，去不必须文字，改点击弹层」）：①正文回调——globals.css mobile 块 16px→14px（行高 1.7，标题 16/15、行内 code 13，气泡 14px/24px，94% 宽保留）；②配置区一行化——mobile 去「本轮完成后解锁切换」锁提示（禁用态+title 表意）；QuotaPill/CtxUsageBar 加 mobile prop：胶囊 compact 只显剩余百分比（模型名/窗标签/重置时间/「等 N 家」全收进既有点击浮层），desktop 完整形态照旧；③任务执行折叠条加 mobile prop：摘要去「（成功 X / 失败 X）」括注，一行只留 运行中/任务/轮次（展开页签内仍可见）；④头部「打断本轮」mobile 图标化（aria-label 保语义）；⑤输入占位 running/restoring 长状态句 mobile 短句化。测试：ctx-usage-bar +2（compact 只显百分比+浮层照旧/desktop 锚）、task-execution-panel +2（mobile 去括注/desktop 锚）、session-config-bar +2（mobile 无锁提示/desktop 锚）共 5 套件 126 用例全绿；tsc 0 新增错误（并行会话在途 turn-state-subagent-routing.test.ts 3 个错误非本改动，ql-20260910-015 先例不代修）、eslint 0 新增。
- ql-20260915-011-59b7 | 手机端会话功能入口收纳（用户约束「不能一股脑收掉——该有的功能手机端也要有，可以收进按钮弹窗」）：①mobile 用量条从整条不渲染（ql-009 初版）改为收进头部 ⋯ 菜单「会话用量」区（session-mobile-usage-section，开菜单才挂载即取数数据恒新鲜，明细表 overflow-x-auto 不撑破 240px 菜单；页面 inline 仍不常驻）；②SessionConfigBar mobile 增设行尾「设置」展开钮（config-mobile-more，aria-expanded/Settings2 图标）——默认收起的思考档位/档案控件/中断自动续跑点开全部可达，再点收起回紧凑态（mobileReveal=!mobile||mobileMoreOpen 单一开关，desktop 恒 true 零变化）。测试：session-config-bar +1（展开/收起往返）+desktop 用例补设置钮不渲染断言、session-panel-variant +1（菜单开才取数渲染）共 59 用例全绿，tsc 0、eslint 0 新增。
- ql-20260915-009-dc08 | 手机端会话页降噪+正文放大（用户反馈「乱七八糟东西太多、重点会话内容太小」，对照竞品聊天页「内容为王」形态）：仅 `/m` 会话页 `variant="mobile"` 生效，desktop/dialog 零变化。①降噪：SessionUsageBar（六项统计条）mobile 页面不常驻（ql-011 起收进 ⋯ 菜单，见上条）；SessionConfigBar 加 `variant` prop——mobile 默认只留 供应商+模型+额度（思考档位下拉/档案控件/中断自动续跑开关/弹性占位默认收起，控件值截断 160→110px）；输入框占位 mobile 去键盘操作提示（deriveSessionPlaceholder/derivePreSessionChrome 加 mobile opt，状态性文案保留）。②放大：globals.css 新增 `[data-variant="mobile"]` 元素级排版块——markdown 正文 12→16px/行高 1.75（双 important 下特异度 (0,3,0) 压 MarkdownText compact (0,2,0)）、标题 18/16px、行内 code 14px；气泡（.turn-bubble 新标记类=用户气泡+旧路径答复气泡，+.seg-text-bubble 段模型文本气泡）16px/行高 27px、限宽 80/82%→94%；时间线滚动区 px-5→px-3。测试：session-panel-variant +2（mobile 用量条不渲染/ desktop 照常）+ session-config-bar +2（mobile 收控件/desktop 零回归）共 9 文件 249 用例全绿，tsc 0、eslint 0 新增。
- ql-20260812-002-7ca3 | vitest 配置：`environmentMatchGlobs` 把 src/lib 下 20 个纯逻辑 .test.ts（白名单）切 node 环境，省 jsdom 每文件初始化（5 个 use-*/daemon-session 用 renderHook/fake EventSource 依赖 jsdom，保留默认环境）。全量 144 文件 1402 用例零回归；墙钟 77→76s（jsdom 组件测试初始化在并行 worker 下本非瓶颈，累计 environment/collect 时间略降，墙钟收益有限）。
- ql-20260812-009-976d | 测试质量审查 P1 前端隔离加固（纯测试基建）：①`vitest.config.ts` 加 `clearMocks:true`（每测试自动清 mock 调用计数，对齐多数测试已手写 beforeEach(clearAllMocks) 的惯例）。**试用 `restoreMocks:true` 已撤**——逐测试还原 spy 致 21 个依赖 describe/beforeAll 级 spy 持久化的既有测试（layout 工作区守卫 14 / workspace-access-guide 3 / interactive-session-panel-offline 3）组件渲染为空 `<div/>`；采用 restoreMocks 需先把这些 spy 下沉 beforeEach，属独立重构。②`team-progress.test.tsx`「活跃态自动轮询」从真 timer（等 50ms×3 撞 CI event loop 抖动）改 `vi.useFakeTimers({toFake:[setTimeout,setInterval,...]})` + `advanceTimersByTimeAsync`（对齐 workspace-config-card 模式），消撞 1s 兜底 flaky。全量 vitest 144 文件 1402 passed/0 fail。

## 注意事项

- UI 文案与文档尽量用中文（项目硬性规则），仅专业术语保留英文。
- frontend 容器 healthcheck 曾因 busybox wget 走 Docker 注入代理误报 unhealthy，属探针问题非服务故障；当前 Dockerfile 用 node20 内置 fetch 零依赖探测。
- 改 daemon 交互类组件（runtime-session-dialog 等）要同步看 backend daemon 模块与 sillyhub-daemon protocol 的契约一致性。
- **daemon-client changes 入口（2026-06-26-daemon-client-spec-sync-fix）**：daemon-client workspace 新建 change 调 `POST /api/workspaces/{id}/changes/proxy-create`（带 `runtime_id=workspace.daemon_runtime_id`），由 backend 经 `daemon_change_writes` lease-polling 让 daemon 代写文件；daemon 离线时按钮禁用 + tooltip 引导，端点返 `DAEMON_CLIENT_NO_SESSION`(400)。区别于 server-local/repo-native 走原 `changes/create`。

## 人工备注
<!-- MANUAL_NOTES_START -->
- **change 2026-08-22-team-session-unify**（会话内团队操作前端，8e47dc70/3a3f190f）：①**触发入口（task-11）**：session-panel 输入区「派团队」按钮（Claude 可用、Codex 置灰 tooltip「团队需要 Claude 引擎」）+ 新组件 team-trigger-popover 配置弹层（范围：当前工作区/项目+scope 多选/预算/分身预设折叠）+ 就绪/进行中状态 chip；`/team` 指令前缀拦截等价按钮路径；「用团队分析」改造走触发弹层（session-panel page/dialog 双模式 + interactive-session-panel 透传两处）；活跃 mission（planning/running/awaiting_input）5s 轮询、终态停止。②**TeamTaskBlock（task-12）**：新组件嵌入消息流（概要行：状态徽标（含 awaiting_input 档）/N 分身成败/花费；展开：主控行+分身行+日志/产物入口+取消按钮→`POST /api/missions/{id}/cancel`），数据源 `GET /api/daemon/sessions/{id}/team-missions`；turn-segment-views 进度视图分身段块（violet：角色/目标工作区徽标/状态/耗时）+ dispatch_worker 等 MCP 工具卡；`lib/daemon.ts` 新增 triggerSessionTeamMission/listSessionTeamMissions/cancelTeamMission client（TeamMissionSummary 手写保留——生成版 status 裸 string/workers 可选，注释差异）。③**删旧入口（task-13）**：删 mission-console.tsx、workspaces|projects 两 missions 页面路由、「Agent 团队」菜单项、`lib/agent.ts` create/list×4 client（保留 getMission/cancelMission 供 change 详情 team-progress）；ppm 项目页「发起团队」按钮改跳统一会话页。api-types.ts 随 backend OpenAPI 重新生成。
- ql-20260821-002（项目团队 mission 前端审查修复，docs/project-team-mission-review-2026-08-21.md）| mission-console.tsx 六项：①ACTIVE 集合去 degraded（终态不再 10s 轮询/不显示"取消任务"，原语义错误）；②`displayedMissionIdRef`+`selectMission` 轮询竞态守卫（切换 mission 丢弃在飞响应，不再闪回旧任务）；③worker 日志游标 earliestTimestamp→latestTimestamp（after=最新一条，消除每 5s 近乎全量重拉 5000 行 TEXT）；④budget 输入 0/负数/Infinity 阻断提交；⑤错误文案复用 errMessage（杜绝 [object Object]）；⑥anchor 默认过期注释更新（后端已按 backend-code 词表比对）。projects/[id]/missions/page.tsx：projectId 缺失渲染错误态（原永久 loading）；listProjectMissions 403 显式"仅项目经理"提示（原静默吞掉显示"无历史"）。游标 2 用例更新新语义 + missions-page 新增 degraded/budget/403 三用例；全量 1795 passed，tsc 0。
- 2026-08-20-runtime-readpoint-repo-first | /workspaces/[id]/runtime 页：user-inputs 渲染超 `USER_INPUTS_MAX_DISPLAY=50000` 字符时 `slice(-50000)` 尾部截断（追加式日志末尾最新）+ 含完整文件路径的截断提示行；副标题改「优先本机仓库，回退同步缓存」。lib/runtime.ts 接口客户端零改动（12 passed）。
- **2026-08-19-cross-workspace-team-mission**（跨工作区团队执行 + 项目维度会话）：新增项目维度会话入口 `app/(dashboard)/projects/[id]/missions/page.tsx`；扩展 `components/mission-console.tsx` 支持 `projectMode`（scope 多选、anchor 单选、目标工作区徽标列、调用 `createProjectMission`/`listProjectMissions`）；`lib/agent.ts` 新增 `createProjectMission` / `listProjectMissions`（类型从 `api-types.ts` MissionResponse/MissionCreateRequest 导入）；`lib/api-types.ts` 随 backend OpenAPI 重新生成。 vitest 全量 1684 passed，tsc exit 0。

## 变更索引
- ql-20260914-001-b14c | 群聊系统提示行放行（24h 审查 P2，防用户可感沉默）：group-chat-panel parseGroupLiveLog 对 channel=system 无 consensus_card 事件由整类 ignore 改渲染既有 kind:system 条目（union 与渲染器 group-system-event 本就预留、此前零产出）——限频/成员打断/成员触发失败/汇总收口失败四类后端既定「实时流提示」（ephemeral 不落库，打字指示走独立 typing 频道不受影响）此前全被吞，收口轮失败时用户无任何群内提示；无 log_id 合成 id（timestamp+内容前缀）供去重定序，空白内容仍 ignore。测试：group-chat-panel.test 同用例改新语义（无卡→system 条目+合成 id / 空白→ignore）64 passed；tsc 0；eslint 0 新增（7 条预存 warning 不在改动区）。同批 daemon 侧两修见 sillyhub-daemon 模块（atomic-write 目录 fsync、reload 白名单派生化）。
- ql-20260912-001-081d | 头像保存误删自愈（H-3，24h 审查修复批第二轮）：lib/auth.ts updateMyAvatar 原形态 PATCH 成功后 `await fetchMe()` 与 PATCH 同抛——桌面/移动账号页的 catch 会把「刷新失败」误判成「保存失败」并对**已绑定成功的新头像文件**执行 tryReclaimOrphanAvatarFile 软删（连 MinIO 对象一起删，头像永久死链）；修复为 fetchMe 失败 best-effort（console.warn 不上抛，store 短暂滞后由下次 fetchMe 收敛），PATCH 失败仍原样上抛保住孤儿回收路径。测试：lib/__tests__/auth.test.ts 新建 4 用例（刷新失败不抛/成功路径/PATCH 失败仍抛/null→空串三态契约）+ 桌面 10/移动 5 账号页回归全绿；tsc 0。
- ql-20260911-024-9098 | 平台同步区命令回显提速第二级（用户反馈「执行很慢，基本都要十秒以上」——实测命令本体 0.2-0.7s，大头是结果回显两级 15s 定时器）：platform-sync-section.tsx 下发后开 15s 加速窗（ECHO_FAST_WINDOW_MS）内 machines 查询 refetchInterval 从 15s 切 5s（ECHO_FAST_POLL_MS，≈5s×3 次），窗口到期 setTimeout 清 pollBoostUntil 触发重渲染回退常规节拍（react-query 随 refetchInterval 选项值变化重排轮询定时器）；裁决下发（handleCompareDispatched）与 ghost 清理 onOk 两下发点均开窗。第一级（daemon 结果落槽即补发心跳）见 sillyhub-daemon.md 同 ql 条目——两级合力把平均 ~15s/最差 ~30s 的回显延迟压到 ~5-10s。测试：platform-sync-section.test.tsx +1（fake timers 钉窗内 5s 间隔/窗外回退 15s 节拍）共 15 passed；tsc 0。
- ql-20260911-001-c0be | scope-audit 命令卡升级为结果卡（用户反馈「你就在页面展示这个玩意？？」——命令不是目的，对账结果要直接看）：scope-audit-command-card.tsx 重构——挂载即经 GET /sillyspec/scope-audit 跑对账（identifier：change=change_key / quick=useQuickSessionName 反查会话名），卡面出锚点+文件·行数合计（+N 绿/−M 红）+三态计数 chips（full-flow：✓计划内/⚠️计划外/⚠️计划未动；quick：✓已声明/🔍软归属/⚠️未声明，语义 token 色阶），「查看明细」开宽弹窗渲染三态全表（行=徽章+path+kind+行数），行点击联动 scope-file-diff-modal 看单文件 diff；两条本地命令（sillyspec 前缀）折叠为卡尾 details 次要区保留——链路不可用/降级（ok=false、quick 反查失败）兜底。target.change 增 workspaceId（详情页挂载点同步）。测试：卡 7 用例（三态计数锚点合计/降级/quick 反查取数/反查失败不发对账/明细弹窗+行联动 diff）重写 + 抽屉 mock 增 getScopeAudit 默认降级载荷，变更中心 21 相关套件 237 passed；tsc 0；eslint 0。
- ql-20260910-017-2006 | 变更中心点击文件看内容变化比对（用户指定参考冲突比对弹窗视觉）：新组件 scope-file-diff-modal.tsx——antd Modal 壳对齐 conflict-compare-modal，diff 行渲染复用 git-log/file-tree 已导出的 parseUnifiedDiff（+ 行绿 bg-success/10 / - 行红 bg-error/10 同款语义 token + 双侧行号），头部锚点条显示对账锚点与变更名；note 态（untracked 新文件 / 窗口内无改动）、422 升级引导族（sillyspec/daemon 版本过旧——无重试只给指引）、502/504 失败重试态、5000 行前端渲染截断各自文案。取数 getScopeFileDiff（lib/changes.ts，GET /workspaces/{id}/sillyspec/file-diff）。入口两处：快速修复抽屉变更文件行点击（change=useQuickSessionName 反查的 quick-<8hex>，反查失败行不可点+脚注提示；该 hook 自 scope-audit-command-card 抽出两处共用）与变更详情页变更文件树选中文件头部「变化比对」按钮（changeKey 经 ChangeFilesCard 新可选 prop 下传，缺省不渲染零回归）。测试：弹窗 5 用例 + 抽屉 +2（反查命中行可点开弹窗且 change 传会话名 / 反查失败行不可点出脚注）共 12 passed；变更中心 14 相关套件 164 passed；tsc 0；eslint 零新增（change-file-tree 1 条 HEAD 预存）。
- ql-20260910-015-8e1b | 用量上限类报错前端升级分类（用户反馈 Cursor 会话失败卡只显「运行失败 · unknown」，生产实例会话 7fb5022f：raw 实为 "ActionRequiredError: You've hit your usage limit Get Cursor Pro…"）：normalize.ts buildErrorLogItem 增 isUsageLimitRaw 特征识别（usage limit / insufficient[_ ]quota / exceeded your current quota / 中文额度·使用上限），type 未识别（unknown）且 raw 命中时升 quota_exceeded + 中文 message「供应商额度或用量已达上限」+ hint「等待用量重置或升级套餐（如 Cursor Pro），也可切换供应商后重新发送」，raw 原样保留——RunErrorItem 额度耗尽橙卡/操作按钮复用既有，历史已落库 run 的 error_detail 不用改数据即修好显示（会话页 turn-timeline/session-panel 三消费点都经 buildErrorLogItem）。根因两层均在前端之外但被此修复兜住：daemon 分类器非 claude 引擎一律兜底 unknown（classifier.ts D-001 扩展点未覆盖 cursor/codex）；claude 规则 quota 判定被 has429 前置，usage limit 无 429 不命中。仅动 unknown 不覆盖后端明确分类（rate_limited 等照旧）。照 isCliAuthTransient（ql-20260903-011）同款先例。测试：normalize.test.ts +4 用例（Cursor 原文升级 / OpenAI insufficient_quota·中文额度 / 明确分类不覆盖 / 普通 unknown 不误升）91 全绿；下游 session-panel-dialog + turn-timeline-auto-resume-badge + run-error-item 102 绿；tsc 我方 0 错误（scope-audit-command-card 2 个为并行会话在途预存）。
- ql-20260910-014-6c29 | 变更中心展示 scope-audit 范围对账命令卡（前端半）：新组件 changes/scope-audit-command-card.tsx——表格版 + --json 版两行可复制命令，命令前缀单一取值点 SCOPE_AUDIT_CMD_PREFIX="node src/index.js"（sillyspec 工具 2026-09-10-change-scope-audit 版本发布后改 "sillyspec" 只动该常量一处，全部命令经 buildScopeAuditCommand 拼接）。挂载两处：变更详情页右辅侧栏尾（target=change，identifier=change_key 直代，已归档变更也可查）+ 快速修复抽屉结构化视图底部（target=quick，identifier=机器 sillyspec 快照 changes[] 按 ql_id 反查的 quick-<8hex> 会话名——取数链复刻 platform-sync-section：fetchMyBinding→listDaemonMachines→sillyspec_status_map 工作区级优先（缺席不回退单槽位防串台）；查不到（旧 daemon/会话已结束 guard 清理）→ <quick会话ID> 占位符 + 手动替换提示，不阻断展示）。测试：组件 6 用例（纯函数拼接/前缀锚点/change 代入/quick 反查命中/占位兜底/clipboard 复制）+ drawer +1（挂载与原始 md 门控）共 11 passed；变更中心 6 相关套件 97 passed；tsc 0。
- ql-20260910-013-3b38 | 变更详情页删除两张死数据卡（用户裁定方案 A）：右辅侧栏 ChangeTaskBoardCard（任务看板摘要）与 ChangeReviewHistoryCard（审核历史）连同组件/测试删除，page.tsx 移除挂载、taskBoard 取数与 reviewHistory 派生（loadSide 少一路 getTaskBoard）。依据 DB 实证：本地 PG 341 变更 stages 带 review_history 的 0 条——写入代码 2026-08-14 上线、平台内审批流转最后一次 2026-08-12（审计日志），CLI 驱动工作流下审批不落平台 review_history，卡片恒空；看板摘要则须任务页手动 reparse 才更新、状态名英文裸显且与步骤条/时间线三重展示进度。后端 review_history 写入端点与 lib/tasks getTaskBoard（任务看板页在用）保留不动。三个页面级测试清 vi.mock（page-team-toggle「保留只读展示区」用例断言收窄）；顺手修无关旧债：delete-change-confirm.test 的 @/lib/daemon 整模块 mock 缺 SESSION_ENGINE_OPTIONS（ChangeUsageCard→quicklog-sessions-card→session-list-panel 真实链收集期炸，HEAD 上即可复现）改 importActual 部分 mock。5 测试文件 65 用例全绿，tsc 0，eslint 零新增（kanban.ts 等预存 warning）。
- ql-20260910-012-392f | 变更中心「平台同步」区串台修复（用户反馈：总览卡显 3 条未决冲突、变更中心却看不到）：platform-sync-section.tsx 快照取数仍读机器级 sillyspec_status 单槽位——daemon 工作区级化后每轮采集逐目标互相覆盖单槽位（轮内最后一位获胜），机器多工作区绑定时变更中心显示的是别的工作区数据；总览卡 14a50351d（2026-09-09）已改 map 优先、本组件漏改。修：对齐总览卡同款取数——sillyspec_status_map 非空时按当前 workspaceId 取（缺席=该工作区未被采集，整卡不渲染，刻意不回退单槽位，那正是串台来源）；map 为 null（旧 daemon 未启用工作区级采集）回退机器级 sillyspec_status。测试 +2（map 优先不串台机器级单槽位数据 / map 启用但当前工作区缺席整卡不渲染）共 14 passed，tsc 0。
- ql-20260909-004-e5f9 | 修复 24h 审查 M6：轮次跳转翻页循环卸载后残留请求（session-panel-page.tsx handleJumpToTurn）——循环（≤8 页×100 条/页）无卸载守卫，串台两道保护（epoch/abort）在 sessionId effect 体内、整页卸载不执行，已死实例的 epoch 校验恒过 → 路由离开后带着死游标发完剩余页请求 + 幽灵兜底 toast。修：循环条件加组件级 mountedRef.current（:584 既有，cleanup 置 false 卸载必达）+ 翻页后早退不弹 toast。测试：page.test.tsx +1（可控 deferred 挂起第 1 页 → 卸载 → 放行后断言零后续请求零 toast），36 passed。注：frontend typecheck 存在 2 个既有错误于并行会话未提交的 changes-overview-card.test.tsx（本条零涉及，归该会话收尾）。
- ql-20260908-012-a32c | docs/qa 剩余三份带日期快照预防性 snapshot 豁免（ql-20260908-011 对齐收尾）：subsession-backend-audit-2026-08-26（60 处引用退出，frontmatter 收纳散落 author/created_at）/ 2026-08-21-daemon-cleanup-code-review（1 处退出）/ sillyhub-functional-review-2026-05-31（0 引用，语义统一标注）。docs check 914→853（61 处退出与逐文档计数 60+1+0 吻合）。**附带发现并记录工具缺陷（规则 15 → docs/sillyspec/docs-gate-shared-worktree-parallel-block.md）**：docs gate pre-push 校验整个工作区而非推送内容，本次推送被并行会话正在编辑的未提交文件（model.py/hub-client.ts）的 7~8 处瞬时锚点失效拦住（两轮运行失败数波动证实移动靶；全部失败目标都落其脏文件，本提交干净）——按规则 10 不跳钩子、不修他人移动靶、不 stash 他人半成品，两条提交（64bbb7653 + 缺陷记录）留本地待并行会话落定后随下次推送带上；缺陷记录含三条修复建议（推送范围驱动 / 校验 HEAD / --against HEAD 模式）。
- ql-20260908-011-8526 | QA 审计文档行号敏感债消除（评估三方案择一落地，纯文档零代码）：docs/qa/subsession-daemon-frontend-audit-2026-08-26.md 对 session-list-panel.tsx 的 file:line 锚当日随代码改动漂移两轮、两度触发 docs gate 拦截。方案评估：①known_failures 豁免不可行——源码确认 docs-check 的 known_failures 只服务 decisions.* 命名空间，引用失效无豁免键；②区间宽锚（:起-止）保留部分棘轮但中等重构仍破、且历史文档漂移告警零可操作性；③frontmatter `doc_type: snapshot` 机制对口——docs check 的 snapshot/known_failures 双通道豁免之一（docs-check.js 位于 sillyspec 安装目录，仓库内无此文件，去锚引用），带日期审计报告语义即点时快照（发现项已修复、痕迹在代码注释），弃漂移告警成本≈零。落地③：文档头加 frontmatter（doc_type: snapshot + 收纳散落 author/created_at + 注释说明），该文档 20 处引用退出校验（934→914），gate 0=基线放行。**同目录另三份带日期审计快照已由 ql-20260908-012 补齐**。
- ql-20260908-010-7c94 | machines 空数组引用收敛（react-hooks/exhaustive-deps 预存 warning 清偿，一行重构）：useSessionListSharedData 的 `machineCandidates ?? []` 在数据未就绪（undefined）期间每渲染生成新空数组，下游 runtimeToMachine/archivedWorkspaceIds 等以 machines 为依赖的 useMemo 每次渲染恒重算；照 eslint 建议包 useMemo（deps=[machineCandidates]），undefined 期间恒返回同一空数组引用。验证：该文件 eslint 0 warning 0 error + tsc 0 + panel/portal/page 3 套件 168 用例全绿；行为零变化未重部署。附带：推送时 +4 行致 QA 审计锚点再漂 2 处（subGrouping:2226→2247 / shownSessions:2226→2235），随 8c8f20fe1 修锚归零——该债已由 ql-20260908-011 snapshot 豁免根治。
- ql-20260908-008-d7d9 | 前端 unused lint 债顺手清（4 条 warning 归零，纯死代码删除零行为变更）：①session-list-panel.test 的 clickEngineTab 助手整体删除——引擎胶囊 Segmented tab 随平铺形态退役（ql-20260823-003）后零调用遗留；②page.test 的 png Buffer 声明删除——fetch mock 直接回 canned 201 响应，buffer 声明后从未使用；③runtime-session-helpers 的 cn 导入删除（文件早已不用）；④session-list-panel.test 群聊折叠用例的 row 绑定改裸 await——仅作 findByRole 等待锚点、绑定未读。三文件 eslint 0 warning 0 error + tsc 0；154 用例全绿（panel 100 + page 29 + helpers 25）。未动 machines-memo（react-hooks/exhaustive-deps 类，需 useMemo 重构非删除可解，另立处理）。
- ql-20260908-007-b871 | 会话左栏智能体筛选下拉补齐 pi/cursor（用户反馈「PI 和 cursor 也能选择了，选项与实际不符」）：session-list-panel AGENT_TABS 原 hardcode claude/codex 两档，但平台交互会话引擎已是四值（后端 InteractiveProviderLiteral / daemon VALID_PROVIDERS = claude,codex,pi,cursor），pi/cursor 会话无法按引擎筛选。收敛单一源：lib/daemon/runtimes.ts 新增 SESSION_SUPPORTED_PROVIDERS（有序四值数组）+ SESSION_ENGINE_OPTIONS（value+label，label 取同文件 PROVIDER_META——注意初始化顺序须在 PROVIDER_META 之后防 TDZ）；三处消费方改派生——session-list-panel 智能体下拉（AGENT_TABS=SESSION_ENGINE_OPTIONS 派生）、pre-session-picker 白名单（建 Set 快查，原手写 Set 退役）、runtime-session-helpers onlineProviders（原函数内手写数组退役）。测试：session-list-panel mock @/lib/daemon 补两组新常量+PROVIDER_META pi/cursor 标签 + 新增 pi/cursor 筛选用例（100/100）；sessions-portal/page 两测试文件 mock 同步补（page 用 importOriginal 取真实常量；缺 SESSION_SUPPORTED_PROVIDERS 会变空 Set 静默筛掉全部引擎，坑位注释落盘）；blast-radius 10 套件 181 + picker 27 + helpers 25 + runtimes 页 16 全绿；tsc 0；eslint 0 新增告警（5 条预存）。
- ql-20260908-005-0759 | 会话左栏两层筛选胶囊改下拉 + 选择记忆（用户反馈「机器/智能体数量多容易撑爆」「刷新不要重新选」）：session-list-panel 筛选区 D-107 两层 FilterPill 胶囊 tab（第一层机器含 Monitor 图标横排、第二层智能体 Claude Code/Codex）改为两个 antd Small Select——#slp-machine（flex-1、showSearch 可搜、「全部机器」清空）+ #slp-agent（w-28、选中机器后才出现，依赖语义不变，含「全部智能体」），一行放下彻底消除换行撑爆；FilterPill/EngineMark 组件与 Zap/Command 图标导入随胶囊退役。筛选值经 localStorage 记忆（SESSION_TREE_FILTER_LS_KEY=sillyhub.sessions.tree.filter，形状 {machineId,agent}，先例 ql-20260824-002 展开记忆同款模式）：pickMachineTab/pickAgentTab 变更即落盘（机器清空 agent 随清）、挂载惰性恢复（machineId 空则孤儿 agent 不恢复）、恢复的机器 id 已不在机器列表时兜底 effect 重置两层防「下拉显裸 id+树空」。R-05 筛选重置/纯视图过滤语义零改动。测试：session-list-panel.test 锚点迁移（机器tab/智能体tab 胶囊点击→chooseAntdOptionByText 下拉点选，aria-label 智能体筛选层→#slp-agent）+ 新增 4 用例（落盘/恢复/陈旧 id 兜底/坏 JSON 容错）+ change scope 退役守卫改锚 .ant-select-multiple（#slp-machine id 已被新单选下拉合法复用）；sessions-portal.test 补 antd Select 助手并迁移 3 处；sessions/page.test.tsx 共享机器用例改开下拉断言选项。3 文件 167 用例全绿，tsc 0，eslint 零新增告警（3 条预存 warning 行号平移）。
- ql-20260906-001-9232 | 修复 isAssistantApiErrorText 全文任意位置匹配误吞合法回复（昨日审计 #8）：normalize.ts 四条正则（API Error/Request rejected/Not logged in/Please run \/login）收紧为**行首锚定**（`^` + trimStart）——成功 run 的助手回复正文**中段**提到这些词（如帮用户排查报错、引用错误原文）时，classifySessionLog 返回 null 整条丢弃且无 RunErrorItem 失败卡兜底，UI 直接丢内容；CLI/daemon 合成错误行恒以特征词开头，锚定后真阳性零回退。四处调用点同享收紧：classifyLog 重分类 error、新旧轨 normalize 合流拦截（hidden/独立 error 项）、session-log-assembler 丢弃判定；assembler 注释同步修正（原「正文真含这些词不受影响」的表述在全匹配期不成立，锚定后才为真）。测试：normalize.test 补中段提及不误判用例（含行首真阳性不回退）、session-log-assembler.test 补中段提及保留 kind=reply 用例，2 文件 160 passed，tsc 0。
- ql-20260904-021-ea77 | 本地 Agent 日志收纳会话面板顶部（用户反馈「不要放会话主面板里，放顶部点击再展示」）：AgentLogCard 从对话流尾部气泡条目（turn-timeline streamFooter 挂载，2026-08-23-agent-activity-sessions D-004 形态）改面板级整宽折叠栏——session-panel 挂横幅之下、会话主体之上（新增可选 mobile prop 对齐面板横幅 px-3 收窄），默认一行「🧾 本地 Agent 日志 · N 个 · 最新 X 前 ▸」细栏（border-b bg-muted/40），点击展开明细面板（max-h-[45vh] 滚动；说明行/条目明细/复制/查看内容对话化/展开全部/刷新交互全部保留）；无上报/加载中/出错仍返回 null 不占位；纯 tool_report 主体（AgentLogSessionBody 即日志条目流）不重复挂载；turn-timeline streamFooter 注入口保留备用（注释标注暂无消费方）。测试：agent-log-card 23 用例（用例 1 补顶部栏根 testid 断言）+ session-panel×15/turn-timeline×5 相关套件 231 用例全绿，tsc 0。
- ql-20260902-006-7965 | 管理员重置密码三处修复（用户实测 422）：①默认重置后端早改为随机一次性口令经响应 plaintext_password 下发（backend/app/modules/admin/users_service.py:807，固定 SillyHub@123 已废弃），前端却丢弃响应并提示写死旧密码——重置后无人知道真实新密码；新组件 admin-reset-password-dialog.tsx（从 users/page.tsx 内联弹窗抽出）默认路径成功后弹窗内展示一次性密码（仅一次+CopyButton 复用+保存警示+「我已保存，关闭」），自定义路径成功即关闭。②前端预校验对齐后端 assert_password_strength（≥8 位+同时含字母数字，validateResetPassword 纯函数导出），纯字母/纯数字在提交前拦下并禁用按钮（此前只校验长度，纯字母放行后被后端 422 拒）。③422 具体原因透传：extractResetPasswordError 剥 pydantic "Value error, " 前缀取 details.errors[0].msg（弱口令黑名单等原因直达用户），弹窗行内+页面 toast 双点展示（此前只有笼统"重置失败"/"请求参数校验失败"）。lib/admin.ts ResetPasswordResponse 手写类型 {password,message} 纠正为 {plaintext_password}（对齐 OpenAPI 生成版）。测试：新组件 14 用例+admin lib 契约 1 例改写，admin 相关 5 套件 78 用例全绿，tsc 0，eslint 0 新增告警（Tooltip 预存债未动）。
- ql-20260827-020-c8d0 | 会话输入框功能按钮合并（用户需求「派团队按钮并入附件按钮位，改一个＋弹菜单」）：session-input-bar 📎 附件按钮改 ＋ 功能按钮——点击弹自定义功能菜单（daemon 组件族 absolute bottom-full + z-30 惯例，避用 antd 浮层）四入口：附件（原 📎 行为，attachmentsDisabled 下沉到菜单项 title 门控）/ 派团队（onTeamTrigger 回调开 TeamTriggerPopover，teamTriggerDisabled/Title 沿用原按钮门控口径；onTeamTrigger 缺省不渲染该项）/ 选择技能（向光标插入 / 驱动联想浮层，词中插入自动补空格保 detectMention 词首规则）/ 关联变更·快速修复（插入 @，无工作区禁用）；菜单外点/Esc/选中即关，＋ 钮含外点 ref 内防双翻、开合旋转 45°；＋ 不随输入框 disabled（终态/离线仍可开菜单看各入口禁用原因）。session-panel TeamTriggerRow 移除派团队按钮、无 chip/错误/弹层时整行不渲染；三宿主（page 真会话/预会话/dialog）接线新 props。新增 session-input-bar-plus-menu.test.tsx 8 用例；适配 session-panel-pre-session（门控用例改走菜单 + streamSession 三参断言补遗 ql-20260827-018 漏改）/ session-panel-team（按钮查询改 menuitem）共 13 用例；相关 9 suites 174 用例全过，tsc 0、lint 无新增告警。
- ql-20260827-018-dbd5 | 会话聊天回显与实时/重载一致性修复（用户反馈「回显慢+有时加载不出来+实时与重新加载不一致」）：①page 模式建流竞态——session-panel 建流 effect 原并行发起历史预取与 SSE，活跃会话重开时 SSE 先到建 turn 致回灌条件（prev.turns 空）不满足、历史被整体丢弃（旧轮内容永久缺失，dialog 模式 establishStream 已修同款竞态）；改 await 历史先回灌再建流；②streamSession 新增 cursor/initialSync 首连缺口同步（daemon.ts）——建连前跑 runs 快照合成+logs 增量回放（syncGapFromDb 提取自 resyncAndReconnect，同序保段内时序干净），补「历史快照→SSE 订阅」窗口内发布的事件，预取失败时 initialSync 全量对账兜底 + 建连后 5s 终态复核；dialog 模式同接线；③replayLogsFromDb 合成 envelope 补透传 parent_tool_use_id/subagent_type/depth/tool_kind/edit_patch——断线 resync/轮后对账补放的子代理日志此前平铺渲染、与硬重载不一致；④新增 maxLogTimestamp 导出。新增 daemon-session-stream-sync.test.ts 7 用例；配套修 page.test.tsx 模块 mock（补 maxLogTimestamp 真实实现+streamSession 第三参断言）与 daemon-session.test.ts SSE mock（REST 路由 JSON）。相关 54+132+26 用例零回归，tsc 0、eslint 0 新增告警。
- ql-20260827-017-c7d3 | 修复多标签页登录态互踢（用户反馈「登录后过一会就要重新登录」）：根因是 token 经 zustand persist 落 localStorage 共享，但 persist 不监听其它标签页写入，各页内存各持旧 refresh token——A 页续票轮换后 B 页持旧 token 续票，超后端 60s 复用宽限窗（`auth_refresh_grace_seconds`，`backend/app/modules/auth/service.py` 重放检测）即判重放攻击吊销该用户全部会话，全部标签页被踢回登录页（开第二页后约 20-40 分钟周期性触发）。修复（`stores/session.ts`）：落盘 key 提为常量 `SESSION_STORAGE_KEY` + 模块级 storage 事件监听，其它标签页写入的 token/user 回放进本页内存（缺字段不误清、坏 JSON 忽略、hydrated 不回放、SSR/HMR 安全），同秒并发续票竞态由后端 grace 兜底；顺带实现登出/换账号跨页同步。新增 `session.test.ts` 6 用例，token-refresh/layout/page 守卫 30 用例零回归，tsc 0、eslint 0。
- ql-20260827-011-e756 | 桌面登录页加移动端入口二维码（`(auth)/login/page.tsx` 新增 MobileQrEntry 卡片）：编码当前站点 `/login`（buildMobileEntryUrl 纯函数导出），手机扫码后由 middleware 按 UA rewrite 到 `/m/login`，登录后落地 `/m/workspaces` 移动工作台（呼应 2026-08-26-mobile-workspace-page）。QR 库 react-qr-code@2.2.0（纯 SVG 零依赖，白底衬板保扫码对比度），SSR 占位挂载后取 origin 防 hydration 不匹配；卡片下方小字展示编码 URL（localhost/局域网一眼可辨）。新增 `page.qr.test.tsx` 2 用例（URL 构造纯函数 + 渲染断言）。
- ql-20260825-011-76cf | 会话聊天页 6 项 UX：①队列改服务端真实排队——use-message-queue 重写为 GET/queue+轮询+refresh（弃前端投递状态机；dialog 弹窗测试无 QueryClientProvider 故不用 react-query），session-panel 忙轮直发 inject（后端入队）+SSE turn 事件刷队列，daemon.ts 增队列三件套；②发送中可打断——inflightSendRef 占位标记，「打断本轮」在 inject 在途时回退消息进输入框并对迟到 run 补发 interruptSession；③输入草稿按会话存 localStorage（sillyhub.sessions.draft.<id>，预会话 __pre__ 键，rAF 门闩防回读前覆写）；④文字可选中——折叠行去 select-none/加 select-text+hasActiveTextSelection 拖选守卫（思考/工具/子代理/分身/团队块/前导），流式自动滚底遇选中暂停；⑤上下文注入收进进度视图——去「（创建轮，仅 AI 可见）」括号、PreambleSegmentView 默认收起可展开、历史 prompt 气泡 stripPreambleText 去重复展示；⑥团队任务块默认收起+列表区 max-h-220px 滚动、Bash/后台任务卡仅进度视图渲染、左树+面板头工作区 display_alias 优先。
- ql-20260624-003-a7f1 | 优化 /runtimes 会话弹窗布局样式：扩大 RuntimeSessionDialog 工作区，改造会话列表为左侧栏，统一交互式会话与历史回看面板高度和输入栏间距。
- ql-20260624-004-c8a2 | 优化 /settings/api-keys 页面和 API Key 创建弹窗：统一页面容器、标题区、卡片、状态和空态样式，补充统计概览与表格密度整理。
- ql-20260625-003-4d7a | 优化 Agent/会话运行日志展示：默认突出用户消息、Agent 回复和思考缩略，补充 token/cache 用量、额外日志类型开关，以及会话实时/历史消息技术日志折叠。
- ql-20260626-001-4a8e | 修复 agent 日志展示：thinking 多行渲染对齐 normalize（mergedThinkingContent!=null 即走折叠，修多行思考裸露成 INFO）+ 顶部「对话/全部」单选 tab 默认隐藏工具调用（真正落地 ql-003 丢失的对话视图诉求）+ 放宽 content 截断。改 agent-log-viewer.tsx（isThinking 判定 + viewMode/defaultViewMode + isConversationLog 过滤）。
- 2026-06-26-daemon-client-spec-sync-fix | daemon-client changes proxy-create 入口（带 runtime_id）+ daemon 离线禁用引导（FR-08/09）。
- ql-20260702-002-4ee9 | agent 控制台 pending run 可见性修复：pending 并入活跃面板（排队中琥珀徽标+角标），原 runningRuns/completedRuns 两派生流都过滤 pending 但"总运行"=runs.length 计入致数字与列表不一致。

- ql-20260709-001-7e3a | BashToolPreview 加 100000 字符展示兜底（displayResult 截断+标注，标题行数与正文同源，复制按钮保留完整原文，防超大命令输出 OOM；后端/daemon 已截断的双保险）。改 tool-renderers.tsx。

- ql-20260709-002-1b8c | Write content 预览 5千→5万 + Agent prompt 预览 3千→2万（tool-renderers.tsx），A 类日志截断放宽。

- ql-20260709-003-a2f5 | normalize.ts [SYSTEM:thinking_tokens] 行默认 hidden + 不打断 thinking 合并（修 thinking 被 token 估算行穿插切成碎片；推翻 D-002@v2 折叠显示决策）。

- ql-20260709-004-f0a1 | 变更详情页「变更文件」区增强：① html/.htm 文件渲染预览（iframe srcDoc + sandbox=allow-scripts allow-popups，不设 allow-same-origin 隔离唯一源，安全）；② 内容区交互反转——默认预览、点「编辑」才进文本编辑（.md→Markdown / .html→iframe / 纯文本→只读源码，统一默认预览）；模式切换保留未保存改动。改 change-file-tree.tsx（抽 FilePreview + mode 状态）。

- 2026-07-25-daemon-borrow-for-business | 借用 daemon 前端：lender 工作空间设置「共享我的 daemon」开关（shared-daemon-toggle）+ owner 成员/设置页管理共享列表/撤销/授 business_member 角色（shared-daemon-manager + workspace-member-row 加业务成员选项）+ 业务人员触发 agent 无感借用（agent/page 门禁放宽 canBorrowSharedDaemon）+ 方案查看（borrowed-solution-files/-panel + workspaces/[id]/files/page + lib/file/api listFiles + lib/workspace-binding canBorrow/共享端点封装）。

- ql-20260726-004-e9db | 业务人员工作空间入口门禁放宽（agent 页 task-13 已放宽，入口门禁漏补）：列表页 workspaces/page + 顶栏 switcher + 移动端 m/workspaces + 详情 guard 的未绑定分支加 canBorrowSharedDaemon 判定——business_member（无自有 daemon + daemon:borrow 权限）未绑定时直接进入（靠借用），不弹/不渲染 daemon 绑定 Dialog/表单。switcher 测试补 mock 导出 + beforeEach 重置默认 false + 加 business_member 放行用例。

- 2026-07-26-ungate-workspace-entry | 工作区入口门禁后移（daemon 要求下沉到操作点）：列表 workspaces/page + 顶栏 switcher + 移动端 m/workspaces + 详情 guard 四入口移除未绑定→拦分支，always 导航/切换/提示电脑端；guard unbound→return null（降级为已绑定编辑入口）；新建 DaemonRequiredNotice 组件（feature/workspaceId/canBorrow/onConfigured，复用 WorkspaceAccessGuide，内联非阻断）+ runtime/scan-docs 无 binding 主区渲染空态；components 页 daemon 无关不接入（数据走 getWorkspaceComponents backend API）；概览复用既有 WorkspaceConfigCard 为可选配置入口。纯前端零回归，无后端/schema/API 变更。

- ql-20260728-002-21aa | 登录爆破防护前端（配合后端 423 need_captcha）：登录失败达阈值 → 弹 `SliderCaptcha` 拖拉滑块（指针事件支持鼠标+触控，凹槽垂直固定 CSS `calc(50%-22px)` 对齐后端 `_SLIDER_Y=53`），拖对取 captcha_token 后自动带 token 重试登录。新建 `components/ui/slider-captcha.tsx`；改 `app/(auth)/login/page.tsx`（needCaptcha/captchaToken 状态 + doLogin 拆分 + handleVerified 自动重试）+ `lib/auth.ts`（login 加 `captcha_token` 形参 + fetchSliderCaptcha/verifySliderCaptcha，类型暂手写待 `pnpm gen:types`）。

- ql-20260728-003 | 滑块下线换「我不是机器人」点按 + 登录页 UI 现代化 + 修 token 闭包 bug：删 `slider-captcha.tsx`，新建 `components/ui/confirm-captcha.tsx`（点按一次性 captcha_id→token，lucide Shield/ShieldCheck/Loader2 四态）；`lib/auth.ts` 改 `fetchConfirmCaptcha`/`verifyConfirmCaptcha`（去 x）。**关键 bug 修复**：`handleVerified` 原 `setCaptchaToken(token)` 后立即 `doLogin`，闭包读到 setState 前的旧 `captchaToken` → login 漏带 token 仍 423（"验证过了登不进"根因）；改为 `doLogin(values, token)` 直传。`(auth)/login/page.tsx` UI 重写为现代深色品牌风（左侧深蓝渐变+网格+光斑+lucide 特性条，右侧亮色玻璃拟态卡，token primary #2563EB，无新依赖）；`m/login/page.tsx` 补齐原本缺失的整套 423/验证码链路并接入 ConfirmCaptcha。

- 2026-07-28-llm-provider-presets-and-usage | LLM 供应商预设模板 + 用量/余额查询前端：新增 `config/llmProviderPresets.ts`（10 家 claude 风格预设常量，6 家标 `usage:{type:balance|token_plan}`，settings_config env 块抄 cc-switch）+ form 顶部预设选择器（网格按钮、分类排序 官方/国内官方/聚合站、💰可查用量标记、＋自定义重置，点预设 setState 填 name/base_url/auth_field/model/website_url，api_key 留空）；`lib/api/llm-providers.ts` 加 `queryUsage(id)` + `detectUsageProvider` + UsageResult/UsageData 类型；新增 `usage-footer.tsx`（多 tier 余额条逐 UsageData 渲染 + 翻红 + keep-last-good 保留上次成功值 10 分钟，移植 cc-switch `resolveDisplayUsage` + 不支持文案）；list 每行挂 UsageFooter + 💰 徽标 + 进页面自动查 + 手动单家刷新。3 件 `__tests__/*.test.tsx`。
- ql-20260729-004-5845 | 补 model-error-visibility 测试债：normalize.test.ts 追加 task-08「模型错误可见性」describe 块——buildErrorLogItem 8 类 type 参数化映射 + type 非法→unknown + message 缺失→「运行失败」 + retryable 严格===true + code/hint/raw 缺失/非字符串→null + null/非对象→null；isAssistantApiErrorText 识别 API Error/Request rejected 且不误判普通回复；classifyLog [ASSISTANT]+API Error→error（修正原 :352 全归 assistant 缺陷）；normalizeLogs errorDetail 追加结构化 error 项（hidden=false, R-02 不进 NOISE）+ 有结构化错误时 [ASSISTANT] API Error 行 hidden；brownfield runStatus=failed 无 errorDetail→兜底「运行失败（无详情）」；成功路径不追加 error 项零回归。normalize.test.ts 60 tests 全过。

- ql-20260729-005-6122 | /runtimes 会话弹窗对话/过程分流 + 气泡视觉升级：session-log-sanitize 新增 classifySessionLog（reply/thinking/tool/stderr 分类，丢弃规则与原函数一致）；SessionTurnView 加 details 过程项，实时 SSE onLog 与历史 logsToTurns 两链路分流，output 只装答复正文；面板头部加「对话/全部」二态切换（参考 agent-log-viewer 无二级筛选），全部视图过程项=思考折叠块（复用 CollapsibleSection）+工具行+stderr 行；气泡大圆角+助手 Bot 图标+text-sm leading-6+运行中「正在思考…」三点动效占位。改 interactive-session-panel.tsx/session-log-sanitize.ts/runtime-session-helpers.tsx + 新增 runtime-session-helpers.test.tsx，受影响 6 文件 83 测试全绿。【补漏】实测对话仍显示 tool——daemon task-runner 工具调用双发（channel=stdout 的 [TOOL_USE] 文本行 + channel=tool_call 的 JSON），原分类只拦 JSON，[TOOL_USE]/[TOOL_RESULT] 文本行漏判成 reply；classifySessionLog 补按内容前缀识别 [TOOL_USE]/[TOOL_RESULT]（含 channel=null）归 tool 剥前缀，实时+历史两链路同源，补 3 测试后 78 全绿。

- 2026-07-29-sidebar-menu-restructure | 侧边栏菜单信息架构重组：`menu-permissions.ts` MenuSection 改 6 值（workspace/agent/config/governance/system/ppm）+ 菜单按功能域重组 5 组（工作区 8 / 智能体 4 / 配置中心 4 / 协作治理 3 / 系统管理 4，ppm 14 项隔离不变），新增 3 独立菜单技能管理 + MCP 管理（智能体组，平台级 /settings/skills、/settings/mcp，复用 settings:admin）、我的供应商（配置中心，独立页 /settings/providers 复用 LlmProviderSection，新增 llm_provider:read 显隐）；守护进程运行时归配置中心（D-006）。`app-shell.tsx` MENU_ICON_MAP 补 Puzzle/PlugZap/Cloud，emoji icon 字段确认无渲染消费者（侧边栏图标按 href 解析）。`settings/page.tsx` 瘦身移除 4 EntryCard 卡片入口 + providers Tab，仅留工作区信息/智能体配置/安全策略/集成 4 Tab 默认工作区信息。受影响测试 4 文件（menu-permissions 37 + permission + picker 40 + 新 providers 页 2）+ settings 相关全绿，前端全量 121 文件 1224 全绿 + typecheck exit 0。

- ql-20260730-003-f13c | /runtimes 会话弹窗工具 use/result 配对+状态徽章+思考按序合并+md 渲染：classifySessionLog 拆 tool→tool_use/tool_result + isToolResultDenied 纯函数（拒绝/denied/error/失败/fail 判 deny）；SessionTurnView 改 processItems 有序过程项（替代 toolEvents+details），onLog 与 logsToTurns 两链路按真实到达顺序构建——tool_use 推 running、tool_result 配对最近 running 设 ok/deny、孤儿降级 raw 空 tool 项兜底；TurnDetailsList 连续同类合并（连续 thinking 拼成一个卡片，被工具穿插则分段保持顺序，修「一股脑全合并丢顺序」）；ToolEventCard 工具结果默认折叠（太长）+ ✓/✗/⏳ 徽章；思考与工具结果均 MarkdownText 渲染（与对话气泡一致）。daemon 测试全绿 + tsc 0 错。

- ql-20260731-001-3abf | 平台技能清单显示每个技能描述：`lib/custom-skills.ts` 加 `PlatformSkillSummary` 类型（name/description/file_count）+ `PlatformSkillsManifest` 加可选 `skills` 字段（manifest 端点返回 dict[str,Any] 未进 OpenAPI 生成范围，继续手写对齐后端）；`settings/skills/page.tsx` 的 `deriveSkillGroups` 回退结构对齐（skill→name / fileCount→file_count），`platformGroups` 优先用 `manifest.skills`（兜底 deriveSkillGroups），表格「说明」列渲染 `g.description || 通用文案`（原所有技能写死同一句废话）；`page.test.tsx` 加 description 渲染测试（mock manifest 带 skills），7 passed + tsc 0 错。

- ql-20260803-003-cb34 | 添加工作区对话框对「复用已存在工作区」显式提示：`useNotify` 补 `warning` 键（antd message.warning）；`workspace-scan-dialog.tsx` 创建成功读 `ws.creation_notice` 非空即弹 warning（复用/激活/复活文案来自后端），否则 `success("工作区已创建")`，杜绝静默 201「创建成功却看不到/绑定没生效」困惑。

- ql-20260804-001-4a3e | borrow 前端清债（change 2026-07-25-daemon-borrow-for-business 收尾）：workspace-binding.ts 本地兜底类型 MemberBindingWithShared（intersection）与 SharedDaemonView（interface）切回 OpenAPI 生成类型——api-types.ts 已含 MemberBindingView.shared 与 SharedDaemonView，无需 gen:types，消费方 import 名不变零改动；borrowed-solution-files.tsx 删过时「后端无按 owner_type list 端点」注释（容器层 panel 已调 listFiles）；frontend/src/components/workspace/shared-daemon-manager.tsx:180 适配生成类型 optional（daemon_id ?? null 归一化匹配本地 shortId(string|null)）。tsc 0 错。

- ql-20260807-004-e5bf | opencode 供应商预设：初版建的 `config/opencodeProviderPresets.ts`（8 家 opencode agent 供应商数据）方向有误——cc-switch 调研确认 opencode 在 cc-switch 是 agent 模式（与 Claude Code 并列，写 ~/.config/opencode/）非供应商，平台当前也 claude-only。该数据模块及其测试已删除；改在 `config/llmProviderPresets.ts` 聚合站组加 **OpenCode Go** 预设（opencode.ai 官方 API，base_url https://opencode.ai/zen/go，模型 deepseek-v4-flash，照 cc-switch claudeProviderPresets 同名条目剔 affiliate，commit ea966414）。

- ql-20260809-003-56db | 多代理审计 8 个低风险单点修复（frontend 部分 2 处）：lib/daemon.ts 删生产 SSE onmessage 的 console.log("[SSE-raw]", ...)（泄漏前 150 字 + 性能噪声）；components/daemon/machine-card.tsx 升级按钮 disabled 加 || upgrading + title 提示「升级中…」（防双击双发自更新，props.upgrading 父组件 runtimes/page.tsx 已传）。machine-card vitest 9 passed + eslint 0 error。

- change 2026-08-09-security-credentials-hygiene | 安全凭据卫生（前端部分）：桌面 `(auth)/login/page.tsx` 与移动 `m/login/page.tsx` 删 localStorage 明文密码缓存（"记住我"原 setItem 含 password，改只缓存 `{account,remember}`）+ 删 admin/admin123 默认回填（无缓存时账号密码均空）+ 旧格式缓存一次性改写清洗已落盘明文（cached.password!==undefined 时重写）+ 复选框文案"记住密码"→"记住登录名"。tsc --noEmit 零错、next lint 两登录页零新增问题。

- change 2026-08-08-llm-provider-openai-format | llm-provider 表单加 API 格式下拉（openai 隐藏认证字段/角色映射 + 完整 URL 粘贴提示）；列表加格式徽标 + litellm_registered 状态；预设聚合站组加 opencode_zen_openai；api-types.ts 从后端 OpenAPI 重新生成（api_format/litellm_registered 字段同步）；Wave2 set-default openai 守护移除（FR-11 开放 set-default）。

- ql-20260810-003-3aa6 | 修复 ImportModuleModal（实施阶段导入弹窗）确认导入按钮 flaky 竞态：selectedRowKeys 默认全选原用 useEffect 异步初始化（与 handleUpload 的 setStep(2) 不同渲染周期），步骤2首帧 selectedRowKeys=[] → validCount=0 → 确认按钮 disabled={validCount===0} 偶发被测试/极快点击点到 disabled 致 importModulesCommit 调 0 次（全量并发跑触发，单跑独占 CPU 不触发）。改为 handleUpload 成功分支与 setStep(2) 同批 setSelectedRowKeys + 切 sheet Checkbox onChange 同步重置，删该 useEffect。行为完全一致仅消首帧竞态，全量连跑 4 次 134/1346 全过。

- change 2026-08-11-change-detail-layout-rework | 变更中心详情页展示结构整体重做（左主右辅两栏）：page.tsx 1119→484 行瘦身为编排层，拆 8 个展示组件——CollapsibleCard（次线折叠基件）/ ChangeFilesCard / ChangeSessionsCard / ChangeReviewHistoryCard（normalizeReviewHistory 读真实 stages.review_history，兼容 gate/rerun 双形状）/ ChangeStageHeader（5 阶段宏观进度）/ ChangeTaskBoardCard（任务摘要）/ ChangeStageActions（收口 gate 面板+推进横幅+验证门禁+触发智能体+provider/model+team toggle）/ ChangeAgentRunLog（组合 AgentRunPanel+子步骤+gate 徽标+TeamProgress，传 onDispatch=undefined 消双入口 FR-05b）。布局 `grid lg:grid-cols-[minmax(0,1fr)_320px]` 左主（操作区+执行日志）右辅（文件/会话/审核历史/任务看板），移动端 <lg 单列折叠。删除僵尸区块（审批状态 approval_status + 审查记录旧实现 listReviews 读死表 change_reviews）+ 死代码（handleExecute / handleTransition）+ 未用状态（archiveGate）。R-06 team toggle role=switch + aria-label=用团队执行 DOM 契约迁入 ChangeStageActions 零破坏。40 组件测试 + 8 迁移测试（page-team-toggle 重写指向 ChangeStageActions，3136ms→80ms），frontend 全量 142 文件 / 1386 测试零回归，tsc 0 错。

- ql-20260811-002-a623 | 修复变更详情页次线侧栏两个宽内容卡在 320px 窄栏不可用（用户实测「会话调试展开都怼一起」「变更文件预览看不了」）：根因 md: 是视口断点非容器断点，桌面视口下即使塞 320px 侧栏折叠卡里也强制两栏/预览挤崩。ChangeFilesCard + ChangeSessionsCard 从 CollapsibleCard 内联折叠改为侧栏紧凑入口卡（标题+说明+打开按钮）→ 点击在宽 Dialog（max-w-6xl × 85vh，flex-col + overflow-hidden）里渲染完整 ChangeFileTree / ChangeSessionSection（黑盒复用不改内部，radix Portal 惰性 mount 关闭即卸载）。CollapsibleCard 沦死代码（两卡改 Dialog 后无引用），ql-002 删除被 quick `--files` 审计拦「超出 allowedFiles 的删除」（`--force-baseline`/`--allow-new` 均不解锁），留 ql-004 清理。2 卡测试重写验入口卡渲染+Dialog 惰性+点打开透传 props。vitest 全量 141 文件 1384 全过、tsc 0、eslint 0。

- ql-20260811-004-067d | 删 change-detail-layout-rework / ql-002 遗留死代码 CollapsibleCard（collapsible-card.tsx + __tests__/collapsible-card.test.tsx 2 文件）：ql-002 把变更文件/会话调试两卡改 Dialog 入口卡后该组件已无引用（grep 复核 src/ 零外部引用）。⚠️ SillySpec quick 流程【无法删除文件】——step1 --files 声明删除目标后「超出 allowedFiles」过，但「删除文件」是独立硬规则，--force-baseline/--allow-new 均不解锁，--done 三次 BLOCKED；经用户确认改直接 git rm + commit（绕过 quick）。tsc --noEmit 0 错（无断链）+ vitest 全量 141 文件 / 1384 测试零回归（较删前 142/1387 净 -1 文件 -3 测试）。详见 docs/sillyspec/quick-done-blocks-deletion-outside-files.md。

- change 2026-08-11-mcp-token-management-ui | McpToken 管理 UI（workspace 内签发/列表/吊销，纯前端零后端改动）：新增 `lib/mcp-tokens.ts`（API client 三函数 list/create/revoke，复用 api-types 现有 McpToken* 类型）+ `components/mcp-token-create-dialog.tsx`（双 phase 签发弹窗，scope 多选默认 read+dispatch，明文仅一次展示 + 复制 + 警示 + 连接信息）+ `app/(dashboard)/workspaces/[id]/mcp-tokens/page.tsx`（管理主页：PageHeader + 3 StatCard + SectionCard 表格 + EmptyState + GET 403 无权限空态 D-001@v1 + 吊销二次确认），`workspace-tabs.tsx` TABS +1「MCP 令牌」tab（紧邻「MCP」全可见）。消费既有 `POST/GET/DELETE /api/workspaces/{id}/mcp-tokens`（router.py，不新增后端）。3 测试文件 17 例 + 全量 vitest 1401/1401 + tsc 0。1:1 镜像 settings/api-keys 模式（手写 useState/apiFetch 非 react-query）。

- change 2026-08-15-change-step-visibility | 变更中心 step 级进度展示+智能轮询：新增 change-step-badge.tsx（列表徽章：三态 active 蓝/waiting 黄 chip「等待用户决策」/done 绿+迷你进度条+两级降级）与 detail/change-step-timeline.tsx（步骤时间线：七值色白名单+stage 分组+memo entry 级 diff，删除 sillyspec-step-progress.tsx，change-agent-run-log 内联 AgentStepProgress 保视觉）；列表/详情页数据层从裸 useEffect+useState 改 react-query useQuery（queryKey 全参/keepPreviousData/错误语义对齐）+函数式 refetchInterval（列表 30s/详情 10s，isTerminalChange archived||archive 停轮谓词可测，structuralSharing 默认开=内容不变跳过重渲染不乱跳，refetchIntervalInBackground 默认 false=后台暂停）。测试 badge 12+timeline 8+页面 25/10+run-log 7，全量 1582 passed+tsc 0。
- change 2026-08-16-change-owner-from-token | 变更中心责任人用户名+履历事件样式+明细不截断：列表 owner 列三态（owner_name 优先/UUID 前 8 位/—）；ChangeStepTimeline 支持 kind=event 条目（👤 emoji dot+紫色 chip 对齐原型色板，data-kind 锚点，step 分支零变化）；Phase 2.4 明细 line-clamp-2 移除改自然换行 break-words+max-h 滚动兜底（列表摘要截断保留）。测试 timeline 12+page 26（含长文本不 clamp 用例）。
- change 2026-08-16-change-center-quick-tab | 变更中心第三 tab「快速修复」：page.tsx TABS +quicklog（tabTotalsQuery 三并发拉计数 pill，changesQuery enabled: tab!=="quicklog" 隔离主 load，useSearchParams 支持 ?tab=quicklog|archive 初始 tab）；新 quicklog-table.tsx（筛选关键词回车/状态/负责人/显示空壳占位 + 6 列：4 态徽标/标题双行 ql_id+pushed ● 点/负责人 enrich/影响模块/关联变更 Link/时间 zh-CN + quicklogPollInterval 纯函数 in_progress|stale→30s 全终态停轮 + 空态分场景）+ quicklog-drawer.tsx（Drawer 详情：四段正文固定序+文件全角括注+关联变更链接+原始 md Switch 切换 raw_block 直出+降级占位+错误态列表项元信息不依赖详情）+ detail/quicklog-linked-card.tsx（变更详情页反向「关联的快速任务」区块：linked_change 筛选拉取，点击跳 ?tab=quicklog，失败静默隐藏）；lib/quicklog.ts API client（gen:types 契约）。测试 table 11+drawer 4+列表页 2 新用例+详情页 2 新用例，全量 1608 passed+tsc 0。
- ql-20260818-008-ec6d | 变更文件 Dialog 预览滚动修复（用户反馈「太长没滚动条、太宽不能左右滚动」）：根因是 Dialog(85vh)→section→grid→右列整条链无 min-h-0/flex 限高，flex-1 overflow-auto 失效，超长内容被 Dialog 的 overflow-hidden 直接裁切；横向 flex 子项缺 min-w-0 被宽内容撑开 + 源码 pre 用 whitespace-pre-wrap 软折行。改 change-file-tree.tsx（section 加 flex min-h-0 flex-1 flex-col；grid 加 min-h-0 flex-1 overflow-hidden + lg:grid-rows-[minmax(0,1fr)]；文件树列 lg 满高；右列改 flex flex-col；FilePreview 三分支统一 min-w-0、pre 改 whitespace-pre 不折行横向滚动、iframe h-full；路径 span 加 truncate）+ change-files-card.tsx（Dialog 内容容器加 flex flex-col + 注释同步）。针对性 vitest 9 用例过 + lint 仅预存 warning。
- ql-20260819-001-4d85 | explorer 文件树目录双击展开/收起：file-explorer.tsx 的 antd Tree 加 expandAction="doubleClick"（antd 6.4.4 透传 rc-tree 同名 prop），双击目录行切换展开/收起，与 switcher 箭头共用受控 onExpand + loadData 懒加载链路；单击仍只选中、叶子与 Ctrl/Shift 修饰键由 rc-tree 忽略。测试补双击展开/收起/叶子不触发 3 用例，17/17 passed + lint PASS + tsc 0。
- ql-20260821-012-22b0 | explorer 文件图标按开发语言细分（用户补充）：FILE_ICON_BY_EXT 把常用语言从 CODE_ICON 兜底拆出独立形状+配色——Java 咖啡橙/class 二进制锌灰/xml code-xml 橙/Vue 三角绿/JS 花括号黄/TS 花括号蓝/jsx·tsx React 原子天蓝·青/py f(x) 蓝（lucide FunctionSquare→类名 lucide-square-function）/go 六边形青/rb 宝石玫红/rs 齿轮石墨/php 花括号靛/swift 雨燕橙/kt 花括号紫罗兰/cs·vb·fs·csproj·sln 等 .NET 系积木紫（.net 本身非扩展名，按语言/项目文件映射）/c·indigo + cpp·深蓝/html 地球/css 调色板蓝/scss 系调色板粉/sh 系终端绿/sql 数据库靛；svelte/astro 补回兜底列表（拆表时差点漏）。测试组件 19/19（图标分型用例改 main.ts 断言 + 新增 8 语言互不相同用例）+ tsc 0 + lint 仅预存 warning。
- ql-20260821-008-fade | explorer 三项 UI 修复（用户反馈）：①长文件名换行根因——antd blockNode 把行钳在容器宽内，wrapper(block)+iconEle(inline-block)+标题(inline) 排不下时图标/大小掉第二行，且旧注释声称的「横向滚动看全名」实际不生效；修法 file-explorer.tsx 树容器加 6 条 `[&_…]!` 提权覆盖（treenode w-max+min-w-full、content-wrapper flex/items-center/whitespace-nowrap、indent-unit w-4 补 ! 修正——原 w-4 无 ! 被 antd css-in-js 压制实际 24px），行宽放开靠外层 overflow-auto 真横滚。②explorer/page.tsx 左栏 w-60(240px) → 新 TreePanelResizer 夹持把手（默认 320px、拖拽/键盘 ←→ ±16px 钳 200~640、双击复位、localStorage sillyhub-explorer-tree-width 记忆）。③文件图标按扩展名分型配色（FILE_ICON_BY_EXT：代码 FileCode2 琥珀/json FileJson2 绿/图片 FileImage 紫/视频 FileVideo 玫红/音频 FileAudio2 粉/压缩包 FileArchive 橙/表格 FileSpreadsheet 绿/pdf 红/office 文档蓝/.env FileLock），树与搜索面板共用 EntryIcon。测试：组件 +1（图标分型，坑：目录行第一个 svg 是 switcher 箭头须 .ant-tree-iconEle svg 取、lucide 类名数字缀尾 lucide-file-code2）+ 页面 +3（拖拽调宽/localStorage/钳制复位，坑：jsdom 无 PointerEvent，fireEvent.pointer* 带不上 clientX 须 createEvent+defineProperty 补），32/32 passed + tsc 0 + lint 仅预存 warning。
- ql-20260821-013-2c1a | 扫描文档页左侧结构树改文件树风格 + 抽 3 个共享组件：①新 components/ui/file-node-icon.tsx（FileNodeIcon + FILE_ICON_BY_EXT 整表迁出 file-explorer，语言/媒体分型图标单一源）；②新 components/ui/panel-resizer.tsx（usePanelWidth hook + PanelResizer 把手，从 explorer TreePanelResizer 泛化，storageKey/default/min/max 参数化，testId/ariaLabel 必填；修抽离时引入的 changeWidth 漏 clamp 回归）；③新 components/ui/tree-box.tsx（TreeBox + TREE_SINGLE_LINE_CLASS，antd Tree 单行覆盖 6 条 ! 提权类单一源）。explorer 侧 file-explorer.tsx（删本地图标表/容器改 TreeBox）与 explorer/page.tsx（改 usePanelWidth+PanelResizer，localStorage key 不变）同步复用，行为零变化。scan-docs/page.tsx 左树重写：手搓递归 TreeView（内联 SVG 灰图标/truncate 截断/固定 grid 280px）→ antd Tree 受控（DocTree：默认全展开可收起、点文件行 getScanDoc 拉详情、文件行徽标👤成员/🕘历史版本/doc_type 保留、目录 font-medium）+ FileNodeIcon + TreeBox + flex 分栏 PanelResizer（默认 280 钳 200~480，lg 固定宽走 CSS 变量 --tree-w 移动端全宽，把手仅 lg 显示）+ localStorage sillyhub-scan-docs-tree-width；顺手删预存未用 STALE_THRESHOLD_MS import。测试：新 scan-docs-page.test.tsx 3 用例（树渲染+图标+徽标/点行拉详情/拖拽记忆，坑：成员徽标 slice(0,8)="member-1"），explorer 2 文件 36 用例零回归，39/39 + tsc 0 + lint 0 告警。
- ql-20260821-015-4637 | 树点行展开 + 知识库页重构（用户指令三连）：①explorer 文件树与 scan-docs 文档树 expandAction doubleClick/默认→"click"——点目录行任意位置展开/收起（本指令覆盖 ql-20260819-001 双击决策，单击仍选中、叶子/修饰键 rc-tree 忽略），file-explorer 3 个双击用例改单击、删 dblClickNode 助手；②knowledge/page.tsx 重写（原「知识 & 日志」双 tab）：快速日志 tab 移除（变更中心保留完整入口）、列表 flat nav → buildKnowledgeTree 按 path 斜杠建树 + 共享三件套（FileNodeIcon/TreeBox/PanelResizer+usePanelWidth，默认 280 钳 200~480 localStorage sillyhub-knowledge-tree-width）、antd Tree expandAction=click 默认全展开、内容区 .md 从裸 pre → MarkdownPreview（复用 markdownRehypePlugins sanitize）非 md 仍 pre、页头改「知识库」回链工作空间；③workspace-tabs.tsx TABS 加 knowledge「知识库」排 explorer 文件之后，menu-permissions.ts 侧边栏 menuLabel「知识 & 日志」→「知识库」（emoji 图标不变）。测试：新 knowledge-page.test.tsx 4 用例（树渲染图标日期/点目录行收起展开+点行拉详情 md 渲染/拖拽记忆/WorkspaceTabs 顺序，坑：usePathname 需文件顶 vi.mock next/navigation、antd DataNode 从 antd/es/tree 导出），关联 5 文件 78/78 + tsc 0 + lint 仅预存 warning。
- ql-20260821-020-69d9 | 变更详情页阶段条兼容 archived 终态：后端读侧把 CLI 归档投影为 current_stage='archived'，前端 ChangeStageHeader 原只识别 'archive' 导致 return null；映射 archived→archive 渲染并保证与 ChangeStepTimeline focusStage 联动一致。改 change-stage-header.tsx + 测试 2 例，组件 14 测/时间线 19 测全绿，lint 无 error，tsc 0。
- ql-20260824-001-60df | 会话页收纳与刷新保持（用户反馈）：①SessionListPanel 工作区分组默认全折叠，仅选中会话所在组（刷新/深链 ?session= 恢复定位）与 defaultExpandedWorkspaceId（workspace/change 入口 FR-06 预展开）豁免；toggleGroup 改基于生效态翻转（默认折叠后首击不再误判）；选中变化 effect 兜底展开新所在组（继续最近会话/预会话创建路径，同一选中只触发一次不回弹用户手动折叠）。②origin=tool_report（本地 Agent）会话不进机器分桶，合并进组内末尾「本地 Agent」可折叠小节（默认收起、折叠态计数、选中在其中自动展开、R-05 筛选变化随 filterEpoch 重置）。③SessionsPortal 选中态 ↔ URL ?session= 双向同步（列表选中/继续最近/预会话创建成功 replace 写参 scroll:false；进预会话/删选中清参；同参去重；new=1 随首同步移除；replace 不产生历史记录）。改 session-list-panel.tsx/sessions-portal.tsx + 3 测试文件迁移（默认折叠下先展开组再点行，scope 用例补 defaultExpandedWorkspaceId 对齐门户接线）+ 新增 8 用例（折叠默认/恢复定位/小节合并折叠/URL 写清参去重）；顺手修 agent-log-card 集成用例（同因挂掉）与 🧾 标题存量债。前端全量 181 文件 2014/2014 绿 + tsc 0 + lint 仅 2 条预存 warning。
- ql-20260824-002-0d64 | 会话树展开状态 localStorage 记忆（用户反馈「前端缓存展开折叠信息」）：SESSION_TREE_EXPANSION_LS_KEY（sillyhub.sessions.tree.expansion）存 {openGroups, openToolSections}——记「展开例外」而非折叠集合，与默认全折叠自洽且跨 scope 不泄漏（workspace/change 入口看不到的组不进集合，全局页对未提及组仍默认收起；落盘读-改-写保留 scope 外记忆）。渲染期默认 = 记忆 ∪ 选中组 ∪ 入口预展开；仅用户 toggle 落盘（R-05 筛选重置/选中兜底展开不写，不覆盖手动选择）；「本地 Agent」小节展开记忆惰性初值（挂载纪元取当时 filterEpoch，筛选变化照常 epoch 丢弃）；无记录/坏 JSON/SSR 静默回默认。新增 5 用例（展开/收起记忆、记忆∪选中并集、小节记忆、坏数据容错），beforeEach 补 localStorage 清理隔离。前端全量 181 文件 2019/2019 绿 + tsc 0。
- ql-20260824-004-978e | 会话左栏树/状态及时更新（用户反馈「左侧树信息和状态信息没能及时更新」）：根因 = sessionsQuery 无 refetchInterval（仅窗口聚焦>15s 与四少数 invalidate 点：会话结束/重开/切配置/软删），页面停留期间轮数/状态点/新上报会话/远端结束均不动。修复：①sessionListPollInterval 纯函数（存在非终态会话→10s 近实时、全 ended/failed→30s 巡航）接 sessionsQuery 函数式 refetchInterval（先例 quicklogPollInterval；后台标签页不轮询、structuralSharing 内容不变不重渲染）；②session-panel page 模式 attach 流 onTurnCompleted 接 onSessionListRefresh（每轮完成即时 invalidate 列表，dialog 模式本就不传不受影响）。新增 3 用例（纯函数参数化、fake timers 轮询接线收敛、onTurnCompleted→invalidate 重拉）。前端全量 181 文件 2022/2022 绿 + tsc 0 + lint 仅存量 warning。
- ql-20260824-005-aa13 | 工作区↔PPM 项目绑定后页面回显（用户反馈「绑定后没正确回显要手动刷新」）：根因 = 详情页基本信息卡「关联项目」简要行（linkedProjectNames）只在整页 load() 赋值一次，「关联 PPM 项目」弹窗内 LinkedProjectsSection 绑定/解绑成功后只刷自己内部 state，无向上通知回调，Modal 关闭也不重拉（同页其它区块如 daemon switcher 均有 onChanged 联动，唯独此弹窗缺）。修复：LinkedProjectsSection 加 onChanged? prop（bind/unbind 成功且自身 reload 完成后调用，失败不通知）；详情页抽 refreshLinkedProjects（load 内联逻辑收敛，单拉 listLinkedProjects 不整页 load 避免闪烁），弹窗 onChanged 即时回显 + Modal onCancel 关闭兜底重拉。项目侧 LinkWorkspaceDialog 自身 reload 正常、项目表格无关联工作区列可回显，不动。测试：组件 +3（绑定成功/失败不通知/解绑成功 onChanged 契约）、详情页 +1（弹窗内绑定→基本信息行「未关联」消失且 tag 即时出现，mock 链路按调用次序编队）。前端全量 181 文件 2026/2026 绿 + tsc 0。
- change 2026-08-24-sessions-live-updates | 会话列表 SSE 变更信号 + 轮询兜底（design §3，task-05/06/07）：前端新增 `subscribeAgentSessionsEvents`（`frontend/src/lib/daemon.ts`，复用 `fetchSse` + 退避重连，断线重连补 `invalidateQueries`）；`SessionsPortal`（`frontend/src/components/sessions/sessions-portal.tsx`）挂载即订阅，收到信号/重连后失效 `queryKey: ["agentSessions"]` 前缀，三入口（全局 / workspace / change）共用自动生效；轮询兜底保留（ql-20260824-004 的 10s/30s 函数式 refetchInterval）。测试补 `sessions-portal.test.tsx` + `daemon.test.ts` + `sessions/page.test.tsx` mock；全量 181 文件 2034 passed + tsc 0 + lint 无新增告警。
- ql-20260824-017-a6ef | 会话面板技能装载内容误入对话正文（用户反馈）：Claude Code 装载技能时 Skill 工具 tool_result 只有「Launching skill: X」，SKILL.md 全文随后以 assistant 文本块注入（`[ASSISTANT] Base directory for this skill: …` 开头，DB run d01bd6d2 实证），classifySessionLog 归 reply 致整份技能说明直接刷进对话气泡。修复（`session-log-assembler.ts`）：classify 识别该前缀归新 kind=skill（仅匹配 [ASSISTANT] 前缀形态，裸文本/用户手打不误吞）；装配器 attachSkillInjection 把全文追加到同桶内最近 Skill 工具段 result（进度视图 Skill 工具卡折叠展开可见，endedAt 更新、状态不重判），同轮多次装载各挂最近不串段，子代理桶路由照常；桶内无 Skill 工具段（tool_call JSON 丢失/旧数据）退化文本段不丢内容。对话视图零改动自动不含（只渲染 text/file 段）。测试 +6（分类/挂载/多技能/子代理/兜底/历史路径一致），daemon+sessions 28 文件 403 绿 + tsc 0。遗留：run 详情页 agent-log-viewer 仍按 assistant 类展示该行（全量日志调试面，不隐藏任何行，语义可接受）。
- ql-20260824-018-7f80 | 会话「进度」视图 Write/Edit 工具卡展开补参数详情（用户反馈「很久之前日志展示能看到具体改动行」）：8-19 段模型改版后 ToolRowView 展开只渲染 tool_result（Write/Edit 仅一句成功消息），参数详情未从旧日志渲染器迁移。修复（`turn-segment-views.tsx`）：展开区上半部按工具名渲染参数详情——Write 内容预览（5 万字符截断+标注，复制按钮带完整原文，搬自 WriteToolPreview/ql-20260709-002 规则）、Edit 红「- 原文本」/绿「+ 新文本」对比（两侧 line-clamp-6，搬自 EditToolPreview），下方保留原 result（MarkdownText）；running 未配对 result 时同样可见预览；非 Write/Edit 工具 result-only 零变化；pre 配色改主题 token（border-border/bg-background/text-foreground）适配双主题。复用 agent-log/tool-renderers 导出的 CopyButton。测试 +5（Write 展开+复制、5 万截断、Edit 对比、running 占位、Read 零回归），daemon+sessions 408 绿 + tsc 0 + eslint 0。
- ql-20260824-019-03db | 会话「进度」视图工具卡展开区继续补齐 + Edit 行级 diff（用户反馈「还缺很多东西」「编辑的文件应该有行号+高亮变化而不是两个代码块」）：新建 `components/daemon/tool-args-detail.tsx`（展开区内容单一入口 ToolExpandBody，自 ql-018 内联组件扩展收编）——①Edit 改行级 diff：computeLineDiff（LCS dp 反向+前向回溯，>100 万单元格回退两块）+ DiffView（双侧行号列 + -/+ 标记 + bg-red-500/10 删 / bg-emerald-500/10 增行底，透明度色双主题安全），replace_all 徽章 + 复制新文本；②Write 内容预览保持（5 万截断+复制完整原文）；③Bash 完整命令 pre + 输出改纯文本 pre（不走 Markdown 防 #/* 误渲染）+ 复制输出 + 10 万字符前端兜底截断（ql-20260709-001 规则）；④Read 行范围标注（offset–limit）+ 复制内容（复制 result 全文，行头复制按钮对 Read 只有路径）；⑤Grep/Glob 参数行（path/glob/type）+ 命中 N 条（解析 result Found N match）；⑥Agent/Task Prompt 预览（2 万截断+复制完整原文）；⑦其余工具通用「参数」JSON pre（2 万截断+复制参数，MCP/Skill/WebSearch 等 args 可见性兜底）。turn-segment-views 删 ql-018 内联组件改一行接线，容器/行头零变化。测试：新 tool-args-detail.test.tsx（computeLineDiff 5 纯函数用例 + DiffView 1）+ turn-segment-views +8（Edit diff/replace_all/Grep/MCP JSON/Bash pre+复制/10 万截断/Read 行范围+复制/Agent Prompt/非 JSON 零回归），daemon+sessions 29 文件 422 绿 + tsc 0 + eslint 0。坑：noUncheckedIndexedAccess 下 Int32Array 索引也返回 number|undefined，dp 表需 as number 断言收窄；测试里「命中 N 条」数字在独立 span（跨节点 getByText 找不到整串）。
- change 2026-08-24-platform-session-feedback-fix（verify 返工后，分支未合 main）| 会话实时反馈消费 + 弹窗最小化：lib/daemon.ts SessionStreamEnvelope 四事件解析（plan_mode_entered/bash_status/bash_chunk/**agent_task_status——verify P1 返工补齐，含 AgentTaskStatusEvent/parseAgentTaskStatusEvent/onAgentTaskStatus 分发**）+ `submitPlanResponse` client；新组件 plan-approval-card.tsx（confirm/revise/cancel 三态、revise/cancel feedback 必填行内校验、ApiError 行内展示）、bash-progress-card.tsx（running spinner/退出码徽标/输出折叠/stdout-stderr 分区着色/is_final 停 spinner）与 **agent-task-card.tsx（返工新增：running 进度条钳 0-100+「后台任务进行中」徽标/终态徽标/message 行）**；session-panel page+dialog 双模式接线（planPending 按 runId 去重；agentTasks 按 task_id upsert 保留终态最近 6 条、会话结束清空、无活跃 turn 且有 running 任务提示「后台任务仍在运行，会话未结束」=FR-03 不提前标记完成）；ask-user-dialog-card/permission-approval-card/session-permission-panel 最小化为右下角浮动胶囊（受控不卸载保留已填内容、多卡累计角标、permission_resolved 同步递减、还原定点恢复）。测试 +36 用例（组件 24 + 返工补 SSE 分发 6 + agent-task-card 6——假 SSE 流喂真实 streamSession 断言四事件解析）。
- ql-20260824-020-6f01 | Edit 展开 diff 行号改文件内真实行号（用户反馈「行号不对——人家是文件内的行号，不是有多少行的行号」，定案方案 A 三端透传）：SDK `tool_use_result.structuredPatch` 链路落地——`AgentRunLogEntry`/`SessionStreamEnvelope` 类型加 `edit_patch`，`toAssemblerInput`（历史 REST）+ `toAssemblerLogInput`/`applyEnvelopeToTurn`（session-panel 两处 SSE 归一）加 `editPatch` 映射，`AssemblerLogInput`/`TurnSegment(tool)` 加字段，装配器 tool_result 配对分支与孤儿分支随 result 写入段 `editPatch`；`tool-args-detail` 新增 `parseStructuredPatch`（hunks `oldStart`/`newStart` 真实行号起计，`' '`/`'-'`/`'+'` 前缀分流 ctx/del/add，`'\'` 标记行不占行号，多 hunk 插双侧空号「…」分隔行，非法 JSON/缺 oldStart/lines 非数组/空串行/未知前缀 → null），`EditArgsDetail` 优先 patch 渲染（DiffView 复用，行号列即真实行号），无 patch/解析失败回退 `computeLineDiff` LCS 片段相对行号。测试：`parseStructuredPatch` 4 纯函数用例 + turn-segment-views 2（真实行号 55 渲染 / 非法 patch 回退相对行号 1）+ session-log-assembler 1（配对/孤儿/无 patch 三分支）；前端全量 183 文件 2063 绿 + tsc 0 + eslint 0 error。
- change 2026-08-24-session-team-mission-context | 会话团队任务上下文（task-12/13/14 前端部分）。**预会话派团队解禁（task-13/FR-05）**：session-panel preSession 实例（无 sessionId 不能挂 mission）也开「派团队」入口——弹层确认后 payload 暂存 state（`preTeamMission`，含主 agent 选择器落定的 `orchestrator_workspace_id`），随首句 `createSession` 上送 `team_mission` 块（失败保留、重试仍携带；确认后 objective 回填输入框）。**弹层探测徽标 + 主 agent 选择器（task-12/FR-04）**：TeamTriggerPopover preSession 实例——组件内 `probeWorkspaces`（`POST /api/workspaces/probe`，弹层机器状态/git 模式唯一数据源）渲染每个候选工作区的在线/离线与 git/direct/unknown 三态徽标；新增「主 agent（项目经理）工作区」选择器（scope 内选择或「当前会话」默认 null=E2 后端按 (锚点,创建者) binding 钉定）；非 preSession 实例零变化。**daemon.ts client 扩展**：`createSession` 入参透传 `team_mission`（有值才带，不带时请求体形态与既有逐字节一致）。**task-14 类型收敛**：gen:types 后 `SessionCreateTeamMission` 改别名生成版 `TeamMissionCreateBlock`、`SessionCreateRequest` 不再覆写 `team_mission`（直接继承生成字段，仅保留 manual_approval/ask_user_only 的 Omit 放宽）；弹层 `TeamTriggerPayload` 保留为构造侧精确视图（精确→宽松结构安全）。测试：team-trigger-popover + session-panel-pre-session 共 48 用例；前端全量 183 文件 2082 绿 + tsc 0 + eslint 0 error。
- ql-20260825-001-f85b | 会话相关代码审查修复（前端 9 项）：**P0 dialog 模式 SSE 建流卸载竞态**——session-panel establishStream 在 `await getAgentSessionLogs` 期间组件卸载后仍建流，连接永不 close 且内部退避循环 30s 封顶永久重连；加 disposedRef/epochRef/establishingRef 三 ref：卸载先置 disposed，await 返回后三条件自查（disposed/已有连接/epoch 不一致）任一命中不建流，同 session 并发复用 in-flight promise 防双连接；**BashProgressCard 跨命令污染**——同 runId 第二条 bash 命令沿用上一条输出 chunks（含 is_final，spinner 被提前终止），新增共用归约 applyBashStatusEvent（runId/command 变化或终态翻回 running 即重置 chunks），page/dialog 双模式统一委托，顺带清理恒 false 三元死代码；**chunks 无界增长**——appendBashChunk 封顶 600 条/约 256KB（UTF-8 字节估算），超限头部丢弃整条保底留最后一条且最后一条 is_final 不丢；**useDaemonMachines 双查询键**——session-config-bar `{}` 与 portal/list-panel `{limit:100}` 分叉成两个键各自 15s 轮询（每轮 machines+sessions 双请求×2），统一 `{limit:100}` 共享缓存；**SSE 信号去抖**——lib/utils 新增 debounceLeadingTrailing（leading 立即+400ms trailing 合并），sessions-portal refreshSessionLists 接入，批量广播/turn 完成双路径不再每帧 invalidate 全列表（limit 500 重拉风暴）；**agent_task_status 补 run_id 必填白名单**（缺 run_id 整帧丢弃，不再归一字符串 "undefined" 挂错 run）；turn-timeline 两处 toLocaleString 补 "zh-CN"；两处过时注释修正（invalidate 不覆盖 daemonMachines 旁路 / fetch-sse 无浏览器自动重连改 streamSession 退避+resync）。测试：新增 session-panel-bash-progress 9 + utils debounce 4 + dialog 竞态 2 + portal 去抖 1 + 白名单 1；前端全量 193 文件 2161 绿 + tsc 0。
- ql-20260825-002-3e67 | 会话优化第二轮（前端 4 项）：**装配器 O(n²) 消除**——session-log-assembler 热路径改「增量维护急切 plain 字段」：同源 partial 续接时 output 尾接增量（V8 rope O(1)）+processItems 数组引用复用（增量投影 cell 以 enumerable symbol 键随 {...turn} 展开流转），结构变更/撤回才全量重投影；seenLogIds 改共享可变 Set+单槽 applyResultMemo 双调幂等记忆（reactStrictMode 双 invoke setState updater 下裸共享 Set 会丢日志——首次 add 二次重查误判重复）；makeUniqueSegmentId 改索引 O(1)（撤回同步删 id）；消费方调研否决 getter 惰性方案（session-panel 16+ 处 {...turn} 展开会枚举求值或静默丢弃）。N=200 大文本 partial 全量投影 ≤2 次（legacyProjectionBuildsForTest 观测计数）+终值与全量重算逐字节对拍+StrictMode 双调幂等；既有 51 测试零修改通过；**resync REST 超时**——listSessionRuns/getAgentSessionLogs 增可选 signal，streamSession 增 resyncTimeoutMs（默认 10s，AbortSignal.timeout 缺失环境退化手动 controller），重连前 resync 挂起不再卡死退避循环（全局 apiFetch 默认不动）；**订阅盲窗补偿**——subscribeAgentSessionsEvents 新增 onConnected（每连接周期恰一次，onopen/首条消息先到者），sessions-portal 接 refreshSessionLists 复用 400ms 去抖单点补拉「快照→订阅」窗口内变更（与 onReconnected 同点天然合并）；**三处 setState after unmount 守卫**——refreshRunsMeta 移入建流 effect 共用 cancelled（顺带修会话切换旧快照迟到覆盖竞态）、useSessionTeamMissions 加 aliveRef、onSwitched 用 mountedRef。测试：新增 session-log-assembler-perf 7/daemon-session 超时 1/daemon.test onConnected 2；前端全量 194 文件 2171 绿 + tsc 0。
- ql-20260825-006-57c4 | 会话输入框支持 Ctrl+V 粘贴图片/文件为附件（用户需求）：session-input-bar textarea 加 onPaste——clipboardData.files 非空则 preventDefault（文件不落输入框）并复用 handleFiles 上传管线（与 📎 选文件完全等价：image/file 分类、chips 预览、onAttachmentsChange 父级回传、10 个上限）；空文件列表（纯文本粘贴）放行默认插入；attachmentsDisabled（codex 引擎）门控与 📎 同口径直接放行。📎 title 补「支持 Ctrl+V 直接粘贴」提示可发现性。测试 +4（图片粘贴 kind=image+chip+父级回传+事件取消、普通文件 kind=file、纯文本不拦截不上传、disabled 门控），测试模块级 mock @/lib/api/session-attachments（factory 须含 fetchAttachmentObjectUrl——attachment-chips 渲染链同源 import）；daemon+sessions 33 文件 494 绿 + tsc 0 + eslint 0 新告警（3 个预存 props 类型签名参数名告警前后一致）。
- ql-20260825-007 | dialog 弹窗（/runtimes）会话输入框接通附件管线（用户需求「消息队列输入框也支持粘贴附件」，接续 ql-006）：调研发现 ql-006 粘贴是 SessionInputBar 组件级能力，page 模式全链路已通，缺口在 dialog——不传附件 props、enqueue 硬编码 []、submitFollowup 两参调用，📎/粘贴能上传但发送静默丢弃（task-12 起就存在的洞，粘贴放大）；后端 inject_session/SessionInjectRequest 早已支持 attachment_ids，SessionCreateRequest（首句）无此字段（R3 契约）。修复（session-panel.tsx dialog 组件，镜像 page 管线）：附件状态三件套（pendingAttachments/clearAttachmentsRef/attachmentMetaRef）+门控派生（attachmentsDisabled = codex 引擎 || 无 sessionId 首句——首句 createSession 无附件入参，放开会「上传成功但发不出去」；attach 模式即有 sessionId 正常开放）+SessionInputBar 新 props attachmentsDisabledTitle（禁用原因文案，缺省「当前引擎不支持附件」，首句门控传「发送首条消息创建会话后可添加附件」防误导）+handleSend D-7 空文本豁免（附件非空看图说话）+enqueue 带 attachment_ids/displayPrompt 标记行（joinAttachmentMarkers，meta 入队登记投递侧拼行）+submitFollowup 附件参数透传 injectSession（无附件保持两参调用形态——既有测试按两参断言，带 undefined 第三参会被 toHaveBeenCalledWith 严格匹配拒）+投递成功/队列移除/新建会话三处清理 meta+既有 MessageQueueBar onRemove 补 meta 清理（排队条 dialog 原本就有渲染，仅缺附件传递）。测试新建 session-panel-dialog-attachments.test.tsx 5 用例（idle 首句门控 title、codex 门控、active 追问粘贴→injectSession 带 ids+占位轮标记行、running 排队 📎1→turn 完成投递、D-7 空文本+附件可发），先红后绿；daemon+sessions 34 文件 499 绿 + tsc 0 + eslint 0 error（2 个新告警为测试 FakeConn 接口类型签名参数名，与既有 dialog 测试同款模式）。
- **2026-08-25-runtimes-entry-unified-floating**（runtimes 会话操作统一走悬浮助手，ca3a83ad/5e348a6a）：①新增 FloatingLockedRuntime 接口 + lockedRuntime 状态 + openRuntimeSession/closeRuntimeLock action（floating-session.ts）；②SessionListPanel 新增 RuntimeScope(kind:'runtime',runtimeId) 类型 + runtime_id 端点过滤 + runtime scope groups 计算（canNew:false）；③FloatingSessionHost 抽屉加宽 960px、grid 320px+1fr、左栏替换为 SessionListPanel + 锁定 badge；④runtimes 页面会话按钮改调 openRuntimeSession（替代 RuntimeSessionDialog）+ ?session= 深链复用；⑤删除 RuntimeSessionDialog + 2 个配套测试（-1087 行）。186 测试绿 + tsc 0。
- ql-20260825-016-b100 | explorer 文件浏览器右栏浏览器原生预览（用户需求：html/pdf 等浏览器可原生预览的文件直接看渲染效果，交互定稿「默认预览态 + 点源码看源码」，不做悬浮按钮/弹窗）：file-preview.tsx 新增 BROWSER_PREVIEW_EXTENSIONS（pdf/html/htm；图片已内联预览、文本/代码走源码分支均不收）+ NativePreviewFrame（fetchDownload 鉴权取 Blob → 按扩展名重设 MIME 转 objectURL → iframe 原生渲染；下载端点多回 octet-stream，不重设 MIME 浏览器会当下载不渲染；html/htm sandbox="allow-scripts allow-popups" 不设 allow-same-origin——隔离工作区文件脚本防摸父页面 storage/DOM（change-file-tree.tsx 先例）；pdf 不加 sandbox 走浏览器内置查看器；卸载/切换 revoke 同 ImagePreview）；FilePreview 加 sourceMode 状态（默认 false=原生预览态，头部「源码⇄预览」切换与下载成右对齐操作组；html 源码=Prism markup 高亮、pdf 源码=binary 元信息卡；filePath 变化 effect 重置回默认预览态防模式跨文件残留），截断黄条条件补原生预览态排除。测试 +8（切换按钮仅预览扩展名可见、pdf 默认 iframe+MIME 重设+无 sandbox、html sandbox 隔离、源码切换 html/pdf 双分支+切回、切文件重置默认态、取数失败降级可切源码、objectURL 切换/卸载 revoke 生命周期），前端全量 194 文件 2195 绿 + tsc 0 + eslint 0。
- 2026-08-25-team-subsession-governance：团队任务块分身行点击入口——workers[].sub_session_id 非空的行主体可点击（键盘可达 + stopPropagation），onOpenWorkerSession(subSessionId) 上抛，session-panel 两渲染点以 WorkerSessionOverlay 浮层复用 SessionPanel（key=subSessionId remount）；存量 batch 行无字段零变化。api-types 已含 sub_session_id/first_run_id（gen:types）。
- change 2026-08-29-usage-by-provider-model（frontend 侧） | 会话配置条四块→两块（删机器/智能体展示块与 runtimeId/useDaemonMachines 等专属依赖，caller 同步）+ 供应商块升级供应商+模型级联（候选=model/default_fallback/role_mappings 去重保序+「默认」首项+快照兜底；provisional 专用回调防误写档案，preModelId 随首句携带）；runtime-card 用量区加调用次数 stat（by_provider 求和）+ 分组明细表（供应商 tag/模型/四维/调用+「未记录」灰阶）+ 计费口径 footnote（含子代理全口径，与套餐计费可能出入）；gen:types 同步。session-config-bar 21/page-usage 13/runtime-card 14/session-panel 56 用例绿 tsc 0。
- change 2026-08-29-daemon-platform-resilience（frontend 侧） | ①streamSession 处理器加可选 onStatusChange(reconnecting|reconnected|live, attempt?)（首连不触发，旧调用方不传不受影响）+连接对象可选 resync()（复用 syncGapFromDb 缺口同步不重建 SSE）。②session-panel 双模式（page+dialog 经 tapStreamHandlers 包装）共用 useStreamConnectionGuard：断线 warning 横幅（第 N 次尝试）/恢复 success 横幅 2s 消失（原型②③）+运行轮 90s 看门狗（getAgentSession+listSessionRuns 对账、30s×3 复核仍 running 且 SSE 断开显 accent 提示**不伪造终态**、终态走 resync()，原型④）。③agent-stream 收到任一成功事件 retryCount=0（修复 5 次耗尽永久停连）。④审批面板 SSE 无限退避重连（共享 RECONNECT_BACKOFF_MS）+重连成功补拉 dialogs 幂等合并。⑤suspended 会话展示四入口：列表「已挂起」徽标+statusLabel 未知兜底「未知状态」、helpers ACTIVE_SESSION_VIEW_STATUSES 纳入（局部类型过渡，OpenAPI status 无枚举不可替代）+禁用续聊专用 title、panel 双模式 info 横幅（24h 副行+抑制通用离线横幅）+输入禁用+挂起 15s 低频轮询（SSE 无会话级状态推送，翻转发现必需）+dialog attach 挂起不计 15s 超时防误报（原型⑤⑥）。api-types 经 gen:types 收口。测试：session-panel-connection 11+session-suspended-display 11+约 420 回归全绿 tsc 0。
- ql-20260829-007-3ec0 | 终态会话 SSE 无限重连修复：backend stream_session_logs 对 ended/failed 会话连上即发命名事件 event: done 并关流（连接时终态 race guard 与流中 session_ended 两场景），而 fetch-sse 命名事件只走 addEventListener、onmessage 收不到且 streamSession 从未注册 done 监听 → 关流触发 onerror 无限重连（终态会话打开面板反复打 runs/logs/stream，被 task-09 重连横幅显性化）。修复：streamSession 注册 addEventListener(done) 置 closed+close（与 session_ended 同语义）；event: error（Redis 故障）保持重连。回归 3 用例（连上即 done/流中 done/无 done 普通断流仍重连）+27 相关回归绿。
- change 2026-08-29-daemon-selfupdate-safety（frontend 侧） | MachineCard 三状态展示：pending_update 非空时横幅同一位置两种 reason 文案（server_command=warning「等待空闲后自动升级（每 30s 复查）」+版本对比副行/disk_change=info「检测到程序文件已变更」+来源副行），主题语义 token 零硬编码，折叠/展开两态均渲染（禁用原因需可见）；升级按钮 disabled 三条件扩展+title 优先级保既有；MachinePendingUpdate 手写类型（task-08 gen:types 收口）；15s 轮询自然刷新。8 新用例+74 消费方回归绿 tsc 0。
- change 2026-08-27-session-token-usage-fix（frontend 侧） | session-panel 环分子口径改逆序最新非 null ctxTokens；ctx-usage-bar usedTokens 可空+未知态；lib/daemon.ts envelope 扩展字段
- change 2026-09-04-conflict-resolve-entry（frontend 侧） | 变更中心「平台同步」处理区：lib/daemon.ts 新增 triggerMachineSillySpecResolve / triggerMachineSillySpecGhostCleanup 两触发函数（POST /api/daemon/machines/{id}/sillyspec-resolve|sillyspec-ghost-cleanup）+ api-types.ts 经 gen:types 再生成（两端点与 MachineSillySpecCommandResultRead 读模型）；新组件 changes/platform-sync-section.tsx——冲突裁决（keep_local/take_platform）与 ghost 一键清理（危险弹窗 okType danger）两区 + 回显「已下发·等待机器回报」+ sillyspec_command_result 匹配 action/change 即认定回报 + ECHO_TIMEOUT_MS 150s（执行 120s+一个心跳周期）无回报恢复可重试 + 旧版 daemon 静默忽略提示；权限走新 hook lib/use-machine-sync-action-access.ts（机器 owner 本人或平台管理员 canOperate，无权隐藏按钮）；桌面 `(dashboard)/workspaces/[id]/changes` 与移动 `m/workspaces/[id]/changes` 双端挂载；总览卡（workspace/changes-overview-card）ghost/冲突区 CLI 指引文案收口为跳转变更中心处理区的入口链接（操作单一入口，入口 Link 置折叠按钮外防 a 嵌 button 非法嵌套）。

<!-- MANUAL_NOTES_END -->

- change 2026-09-14-session-export（frontend 侧） | 会话列表导出双入口：session-list-panel.tsx 批量操作条「导出选中（N）」+ 行 hover Download 图标，均 antd Dropdown 两档菜单（导出对话（Markdown）/导出完整信息（JSON+附件），items 单一源防漂移）；onExportSessions 可选 prop 未传零渲染；exporting 独立 state；lib/daemon/session-export.ts 认证下载通道（POST+Bearer+401 单飞刷新重试+RFC5987 解析+blob 下载，SessionExportTier 切生成类型）；sessions-portal.tsx dynamic import 接线。__tests__ 追加 4 用例（105 passed 零回归）。
- ql quick-3c85b05e | 轮次时间三段显示：turn-timeline.tsx 完成轮时间行升级「开始 HH:MM:SS · 结束 HH:MM:SS · 历时 mm:ss」（新助手 formatTurnTimeSec/formatTurnDuration；无开始锚点回退单显结束时间）；turn-status-bar.tsx 运行中状态条补「开始 HH:MM:SS」（与走秒 ≥15s 门槛解耦，锚点存在即显示）——悬浮对话与门户会话共用 TurnTimeline/TurnStatusBar 内核一处生效两宿主。新增 __tests__/turn-time-display.test.tsx 5 用例（三段/两视图/旧数据回退/状态条开始时刻/无锚点容错），timeline 相关 38 用例回归绿。
- change 2026-09-15-subagent-three-pane-display | 会话子代理三分栏：右栏单槽位（文件预览⇄子代理详情按最后触发覆盖，portal subagentView 状态双向清零+会话切换清零，sessions-portal.tsx）；中栏子代理精简紧凑卡片（turn-segment-views.tsx SubagentBlockView 双模式：SubagentPanelContext 有值仅头部行状态点/名称/类型/徽标/时长+toggle 点击上抛，null 走原内联展开——dialog/悬浮宿主零回归）；右栏 SubagentDetailPanel（新 subagent-detail-panel.tsx，children 经 SegmentView 递归与主会话进度视图同构、无输入框、段失效自动关、宽度与文件预览同 LS 键 usePanelWidth）；对话视图过滤放宽（turn-timeline.tsx isConversationSegment 纳 tool 带 children/subagent_stub，排除 dispatch_worker）修「子代理内容不完整」主因；SessionPanel 3 可选 props（openSubagentId/onOpenSubagent/onSubagentPanelClose，session-panel/index+page，Provider 挂载条件=!mobile&&onOpenSubagent!=null）；subagent-catalog activeId 高亮+目录点击 page 模式直开右栏（旧双 rAF 定位留 dialog 回退）；头部派生抽公共纯函数 subagentHeaderOf。新增/扩充测试 6 文件，六套件 213/213+tsc 0+eslint 0/0。验收返工两修（design §13）：①面板「任务指令」块——装配器导出 dispatchPromptOfRaw 取派发 tool_call args.prompt 全文，SubagentDetailPanel 正文首块展示初始化提示词；②父归属优先跨轮路由修「结束后仍显示运行中」——upsertTurn 实时路径带 parent_tool_use_id 的 log 先全轮 DFS 找父段路由到派发轮（终态轮不翻回 running），logsToTurns 历史 regroupSubagentLogsByParent 重挂（tool_use_id→最终组登记，嵌套跟随，原组重置防双份），跨 run 终态信号不再丢弃、幽灵 stub 消除；返工新增测试 9 用例，八套件 252/252。

### 2026-09-07-agent-liveness-states（agent 会话活性状态推导）

- `components/agent-log/liveness-badge.tsx` 五态视觉单源（语义色阶+呼吸动画，badge/dot/悬浮详情）；`agent-liveness-overview-card.tsx` 工作台「Agent 状态总览」卡（D-004 第二层：分组计数+在等人组跳转，30s 轮询）；日志面板行尾徽章；通知铃 agent_blocked 类型。api-types 经 gen:types 同步。已知缺口：会话列表小灯/idle 小红点待 SessionsPortal 联表管道（见 verify-result NOTES）。
- 2026-09-16-logs-cursor-tiebreaker | 会话日志向上翻页复合游标：getAgentSessionLogs 增 opts.beforeId（仅与 before 同传）；session-panel-page 游标二元组化（historyCursorIdRef 与 ts 游标三写点同步——初始加载/翻页/换会话重置，请求透传 before_id，pageKey 加游标 id 前 8 位后缀并外提 setTurnState updater 消 ref 竞态，loadEarlierOnce 进度判定二元组——同 ts 不同 id=有进度跳转循环不误 break）。修单事务 ≥100 行同 timestamp 批次：重复拉页+React key 撞号+批内前段行不可达。测试：session-history-scroll 6/6（首翻复合游标/同 ts 双页 id 推进无 key 告警/换会话重置）+page.test 三处断言校准；api-types 经 gen:types 再生成（before_id 参数）。

- ql-20260917-006-1380 | 会话对话区隐藏上下文压缩续接摘要（用户要求）：CLI 自动 compaction 后把结构化摘要以 assistant 文本块注入新窗口（"This session is being continued from a previous conversation..."，线上会话 6e213eb3 两天 16 次），此前被 classifySessionLog 归 reply 渲染成大段回复气泡刷屏。修复：`session-log-assembler.ts` classify 识别该前缀归新 kind=compact（仅匹配 [ASSISTANT] 前缀形态，裸文本/用户手打不误吞，同 skill/API 错误行口径）；TurnSegment 增 compact/compact_status 两成员（compact_status 载 daemon 落库的 [COMPACT_STATUS] {"phase":compacting|success|failed} 协议行——压缩过程实时提示，前端用户明确要求「压缩过程中也能回显」）；装配器按序平铺独立过程段，兼容投影（segmentsToLegacy 无 case 自动跳过）output/processItems 不变——历史旧消费方（logsToTurns attach 等）零变化；回复内容级去重（ql-20260822-010 收窄）不含 compact 天然不误伤。渲染：`turn-segment-views.tsx` CompactSegmentView（进度视图折叠卡「上下文已重新压缩（N 字，摘要已注入新窗口）」，默认收起可展开，平移 PreambleSegmentView 语义）+ CompactStatusRowView（compacting 实时「上下文正在重新压缩…」旋转提示 / failed amber「上下文压缩失败」行 / success 渲染 null——成功后紧随 compact 摘要段已是完成标记，坏 JSON 容错按 compacting）+ CompactNoticeChip（对话视图一行短提示）；`turn-timeline.tsx` isConversationSegment 纳 compact（对话视图原位特判渲染短提示）、compact_status 仅运行中轮进对话流（live turn 过滤，终态轮不显示）、segmentTsOf 补两成员；`turn-status-bar.tsx` segmentTs 穷尽 switch 补两成员（非 running 候选取捕获 ts）。测试 +9（装配器 4：分类剥前缀/状态行/投影空/历史实时一致 + 视图 3：折叠交互/状态三态+坏 JSON/短提示 + daemon 归一化器 1：三事件+requesting 丢弃+zod + schema 枚举闭合 1）；frontend daemon 组件 67 文件 978 绿 + tsc 0 + eslint 0 error（3 warning 既有）；daemon typecheck 0 + 相关测试 35 绿 + 全量 4305 绿（1 failed 为 daemon-status-root-persistence 计时并发用例，单跑 9 绿，与本改动无关）。遗留：压缩完成行 compact_result=success 不落提示（由 compact 摘要段承担完成语义）；非 claude provider 无压缩帧来源。

- ql-20260917-008-48e5 | 会话执行中直接切换供应商/模型/思考档/档案（用户拍板：下一轮生效、重复切换覆盖）：原 running 全置灰逼用户干等轮结束（`canSwitch = !running && !ended`），但后端 inject 路由 queue_when_busy=True 早已受理忙轮切换（落排队表轮末派发）——前端封锁是展示层旧契约。修复（`session-config-bar.tsx`）：canSwitch 放宽为 `!ended`（运行中可点，受理即排队/暂存）；底部 🔒「本轮完成后解锁切换」改 ⏱「运行中切换将于本轮结束后生效」信息行（mobile 仍收）；四控件 title running 分支改「运行中可切换，将于本轮结束后生效」；切换成功 toast 分态——running「已排队切换 X → Y（本轮结束后生效…）」/idle 照旧；思考档 handler 消费响应新 `queued` 字段，queued=true 提示「已排队切换思考级别：X（本轮结束后生效）」+ 乐观缓存照写；providerOpenSignal running 不再吞（ended 才吞）。存量 5 用例按新契约改写（running 可点+下拉可开/title/插槽锚/信号分态/mobile 锚），新增 2 用例（running 切供应商排队提示/思考档 queued 提示）。sessions 8 文件 346 绿 + session-panel 3 文件 28 绿 + tsc 0 + eslint 0 error（8 warning 既有回调形参）；api-types/openapi 经 gen:types 同步（SessionThinkingLevelResponse.queued）。遗留：运行中切档后若会话被 end，暂存档随会话终态不再应用（fail-loud 日志）；provider_caps/provider-caps.ts 行尾重写假 M 未提交（.gitattributes 根治待办）。

### 2026-09-19-tool-report-session-replay（tool_report 会话按会话样式回放）

- 纯 tool_report 会话（origin=tool_report 且 turn_count===0）主体由 AgentLogSessionBody 元数据卡列表（已退役删除）换为 AgentReplayBody——TurnTimeline 真组件复用（「对话/全部」视图/轮导航白得），数据源 daemon 解析器矩阵对话化消息。
- 数据链：listAgentLogs → subagent_agent_ 前缀分主/子（最新主日志为正文、子代理+更早主日志进「工作会话（N）」折叠条）→ readAgentLogMessages 最近窗口+触顶 beforeSeq 前插 → buildReplayTurns（frontend/src/lib/agent-log-turns.ts 纯函数：turn_id/真人 user 切轮、sender=system_event→SessionProcessItem system_event 中性行、thinking/tool→processItems、usage 去重聚合/ctxTokens=末次 inputTokens、无 usage 三 null）→ TurnTimeline 11 props 哑值（errorMsg=null/sessionStatus=idle/回调 no-op）。
- 三态提示：daemon 离线 404 no_daemon / 格式 unsupported 或 409 二进制（cursor IDE 新文案）/ 文件缺失 404——中文提示行+元数据保留；total_usage 会话累计由 daemon 返回（前端不求和）。
- turn-timeline.tsx 最小扩展：SessionProcessItem 增 system_event kind（居中虚线胶囊中性行，仅回放路径产生；session-log-assembler 镜像类型 parity 同步+dialog-helpers 一行类型守卫为涟漪）。agent-log-card.tsx：AgentLogCard 顶部栏保留、fallbackNoteForError 增 409 二进制专属文案。
- 类型：AgentLogMessagesResponse 新可选字段经 gen:types 生成（sender/turn_id/model/duration_ms/usage/total_usage，老 daemon 缺省）。

### 2026-09-20-agent-log-session-replay（同需求独立重做——replay-redo 分支，与上节 2026-09-19 实现并行对照）

- 同基线（53c67e02a）独立实现：AgentLogReplayBody（frontend/src/components/daemon/agent-log-replay-body.tsx）+ 适配层 buildReplayTurns/isSubagentLog/selectMainLogs（frontend/src/lib/agent-log-replay.ts 纯函数）。与 2026-09-19 实现的关键差异：系统事件**零改 TurnTimeline**（复用 stderr processItems 首项，对话视图隐藏）而非新增 system_event kind；dialog 形态经既有 attach 轮询捕获 origin/turn_count 门控（保 R4「dialog 渲染路径零 react-query」）；首屏顺序翻页到最早窗口（上限 10 页）而非最新窗口触顶前插。
- token：daemon totals 直出汇总条 + 轮徽标（usage 全量口径 input 含缓存读写，claude-code 解析器归一）；无数据源「未知」不伪造。schema 字段名差异：本实现 totals/消息五字段（turn_id/model/is_meta/turn_end/usage）。
