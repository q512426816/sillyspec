---
schema_version: 1
doc_type: module-card
module_id: sillyhub-daemon
source_commit: ba87eec
author: qinyi
created_at: 2026-06-24T01:16:42
---
# 本地守护进程（sillyhub-daemon）

## 定位

运行在用户本机（或工作机）的 Node 守护进程，是平台"触达本地 Agent 运行时"的桥梁。它把本地 Claude Code / Codex 等子进程接入平台：受控启动会话、拦截工具调用做权限审批、把消息流回 backend、维持租约心跳。是 monorepo 中唯一与外部 Agent SDK 直接耦合的组件，依赖 backend 的 daemon/lease 接口，被 deploy 编排、被 frontend 通过 backend 间接驱动。

技术栈：Node.js ≥20、TypeScript 5.5、ESM（`"type":"module"`）、@anthropic-ai/claude-agent-sdk 0.3.181、commander（CLI）、ws（WebSocket）、pnpm 9.6、vitest、@vercel/ncc（打包）。产出可执行 `sillyhub-daemon`（bin 指向 `dist/cli.js`）。

## 契约摘要

- **CLI 入口**：`src/cli.ts` 的 `createProgram()` 用 commander 暴露子命令——start（startAction）、stop（stopAction）、status（statusAction）、logs（logsAction）等；进程管理经 PID 文件（getPidFile/readPid/writePid/removePid/isProcessAlive）与日志文件（getLogFile）。status 展示的 Runtime ID/Server URL：running 时经 `resolveRunningDaemonConfig(pid)` 从 runtime lock 反查实际 server 的 per-server 配置（ql-20260818-001），否则读 DEFAULT server 配置。
- **与 backend 通信**：`src/hub-client.ts` 的 `HubClient` 实现 daemon 注册（RegisterBody）、领租约（ClaimLease/StartLease）、心跳（Heartbeat/LeaseHeartbeat）、提交消息（SubmitMessages）、完成租约（CompleteLease）；认证 `HubClientAuth`，错误类型 `HubHttpError`。**模型错误回传**：`notifyRunResult` payload 携带可选 `error`（ModelError，is_error turn 失败时），backend 写入 AgentRun.error_detail（向后兼容，旧 daemon 不传则 None）。
- **消息协议**：`src/protocol.ts` 定义 WS 消息集合 `MSG` 与 payload（SessionInject、SessionControl、PermissionRequest/Response 等）。
- **Agent 接入**：`src/adapters/`（json-rpc/jsonl/ndjson/stream-json/text/protocol-adapter）适配多种 Agent 输出流；`src/interactive/`（claude-sdk-driver、session-manager（1965 行瘦 facade + `session-manager/` 13 模块包，2026-09-07-arch-large-file-split）、session-store-persistence、permission-resolver、input-queue）支撑交互式会话。
- **模型错误归类**：`src/model-error/`（types + classifier）把 claude turn 失败信号（is_error/resultText/api_retry.error/assistant stdout/stderr）按关键词归类成结构化 ModelError（8 类，429 quota 先于 rate）。stream-json adapter 在 result is_error=true 时产出并缓存；session-manager turn 收尾近源 classify 后随 notifyRunResult 回传（仅 claude 落地，D-001 扩展点）。
- **SESSION_RESUME 恢复双向确认**（2026-08-21-session-reopen-resume）：`_routeSessionResume` 在恢复成功后调 `confirmReconnected`、失败时（`restoreAndReconnect` 抛错）调 `markRecoveryFailed`，两路均 best-effort（失败仅 warn 不阻断）；hub-client 两方法新增可选 `opts: {leaseId?, runtimeId?}`——`leaseId` 显式透传 backend 做陈旧确认防护（lease 不匹配幂等跳过），`runtimeId` 从 SESSION_RESUME payload 显式供给（F1，不依赖仅 recover 链路填充的内存映射，`opts?.runtimeId ??` 映射回退）。旧 backend schema 忽略多余 `lease_id` 字段，向后兼容。**残留条目驱逐**（ql-20260823-006）：`restoreAndReconnect` 遇 `_store` 已有同 id 条目不再抛 `SessionAlreadyExistsError`（该行为曾把 reopen 打成死循环：backend 翻终态未通知 SESSION_END 的活僵尸 / `end()` 留下的终态条目都挡恢复），改为先静默驱逐（`_terminateSession` 全套清理 + `notifyBackend: false` 不回发终态，避免与 backend 的 reconnecting→active 推进竞态）再正常恢复。

- **MCP 三件套拉取与注入**（2026-08-26-workspace-mcp-edit）：`mcp-config.ts` 新增 `fetchMcpBundle(serverUrl, token, workspaceId?, logger)` → `McpBundle {platform, whitelist, workspace}`——拉 `GET /api/daemon/mcp/config?workspace_id=`（workspace 维度非 stdio 条目预净化跳过+warn 不抛错；回落链分层：整体失败→platform 走本地 `~/.sillyhub/daemon/mcp.json`+whitelist 空+workspace 空，仅缺 workspace 键→该维度回落空其余照用）；`daemon.ts` `_startInteractiveSession` 在 create 前 await 预取写入 `Map<sessionId, McpBundle>` 会话级缓存（失败写空 bundle+warn 绝不阻塞；onSessionEnd/create 失败 catch 清理）；`cli.ts` `mainAgentMcpConfigProvider`（同步签名不变）读缓存合并——`mergeMcpConfigs([...whitelist, DAEMON_MCP_SERVER_NAME, FILE_MCP_SERVER_NAME], platform, workspace, 内置双server)`（内置名必须并入白名单参数：configs[0] 自动白名单只覆盖 platform 位）、rejected 记 warn、merge 抛错防御 catch 回落仅内置；缓存 miss（quick-chat/daemon 重启 restore）回落空 bundle=现状行为（restore 重取是 D-007@v2 后续增强点，SESSION_RESUME payload 无 workspaceId）。
## 关键逻辑

- **Daemon 核心**：`src/daemon.ts` 的 `Daemon` 类 + `RecoveryCoordinator`（崩溃恢复）+ `InteractiveCredentialManager`（凭据管理）+ `translateSpecRoot`（spec 根映射）；`DaemonOptions` 控制运行参数。
- **本地能力**：agent-detector（探测已装 Agent）、terminal-launcher/observer（终端拉起与观察）、file-rpc（文件操作）、task-runner（1660 行瘦 facade + `task-runner/` 8 模块包，2026-09-07-arch-large-file-split）、spec-sync（规范同步）、spawn-env、credential、config、workspace、ws-client、runtime-handler（runtime.* 只读 RPC：read_progress spawn sillyspec progress dump + 直读 .runtime/，2026-08-19-runtime-live-daemon-read）。**进度总览采集器**（2026-09-02-changes-overview-card）：SillySpecManager.collectStatusOnce——execFile 数组形参 spawn `node <bin> progress show --json`（bin 解析 SILLYSPEC_BIN env → npm 全局布局候选[win32: node 同目录 + %APPDATA%
pm；posix: ../lib/node_modules]，ql-20260904-M4 补标准安装器布局）、cwd=claim 观察的 workspace 主仓根（规则 22 不进 worktree）、三态降级矩阵（①成功快照/②能力缺失上报 null/③瞬态失败保留旧值）、changes N=50 截断 + 32KB 预算超限降级纯计数；第五循环按 config.sillyspec_status_interval_sec（默认 60s，0=关）采集，心跳第 6 参透传（sillyspec 缺席时显式 undefined 占位防位置滑槽）。
- **冲突快照 RPC + ql_id 心跳补报**（2026-09-07-conflict-diff-compare）：daemon.ts `_registerSillySpecRpcHandler`（:5413，调用点挂 `_registerExplorerRpcHandler`（:5091）旁）注册只读 RPC `sillyspec_conflict_snapshot`——backend compare 编排经 ws_hub.send_rpc 请求/响应式实时拉取（explorer 先例，区别于一写即忘的裁决通道）。params `{change, kind}`（非字符串归一空串由 manager 入口拒 invalid_params），result `{change, kind, ql_id, conflict_created_at, local_updated_at, files[]|progress}`，RpcError 语义码原样上抛交 ws-client `_dispatchRpc` 回填 error.code（no_spec_root / invalid_params / conflict_record_missing / conflict_record_corrupt / progress_collect_failed / bin_not_found）。业务在 sillyspec-manager.ts `conflictSnapshot`（:996）：spec 根复用 `_statusCwd` 回调（claim 观察主仓根，无根 no_spec_root）；冲突记录按 kind 双前缀（spec-tree → `.runtime/spec-sync-conflict-<change>.json`、progress → `.runtime/sync-conflict-<change>.json`，readSillySpecConflictRecord :1523，缺失/损坏不回退空快照）；spec-tree 分支逐路径读 `.sillyspec/<path>`——realpath 落点必须在根内（isRealPathWithinRoot :1598，file-rpc explorer 同款校验双保险；越界拒读不置 missing 且元信息不泄漏）+ 三道截断护栏（单文件 256KB :158 / 路径 300 :161 / 聚合 4MB :168，常量集中文件头）+ 非 utf8 二进制嗅探（NUL / 严格 fatal 解码）+ 信噪比排序（changes/<change>/ 优先、archive 沉底，specPathSignalRank :1586；聚合帽按排序先到先得，溢出路径仅元信息 truncated）；progress 分支跑全局 `progress show --json` 后自行从 envelope `data.changes[]` 过滤该 change 条目（CLI --json 分支忽略 --change）；ql_id 对 quick-* 名 best-effort 读 `.runtime/quick-sessions/<change>/guard.json` 的 quicklogId（readQuickSessionQuicklogId :1557，读不到/非 quick 回 null）。心跳侧 collectStatusOnce（:608）在纯函数摘要（buildSillySpecStatusSummary 不落 fs）返回后调 `_attachPendingConflictQlIds`（:1168）补 pending_conflicts[].ql_id（best-effort，单条失败仅缺省该条不阻断整拍心跳）。测试 tests/sillyspec-conflict-snapshot.test.ts。
- **构建发布**：`tsc` 出 `dist/`，`scripts/build-bundle.sh`（bundle 脚本）用 ncc 打成单文件便于分发；engines 锁 Node ≥20。

## 注意事项
- **pi 扩展禁止在 session_start 等启动期事件里同步 await dialog**（ql-20260911-027-13b6 实测）：pi 0.81.1 RPC 模式的 stdin 读取器（rpc-mode.js 末尾 `attachJsonlLineReader`）在启动序列完成后才挂接——session_start handler 里 `await ctx.ui.select(...)` 会永远卡住启动，stdin 全冻（get_state 都不响应），表现为「进程活着烧 CPU、零日志」。dialog 只能在 turn 进行中发起（工具 execute 内 / 延时回调），此时读取器已挂接、extension_ui_response 立即回流（端到端已实测：request 冒出 → 回 value → 同轮拿到 answer）。vendored ask-user 扩展即按此约束实现（仅工具 execute 路径调 dialog）。
- 写守卫 session 级 overlay（2026-08-28-daemon-agent-share / D-011）：`_judgeWriteViaPolicyEngine`
  在 `_borrowSandboxRoots` 之后增加 `_sessionOverlayRoots` 交集收紧——claim payload 的
  `effectiveAllowedRoots` 非空时写路径须同时落于 session roots 与 PolicyCache（机器级），
  语义只收紧、无字段会话零变化；fallback 块复用同一 helper（overlayRoots ?? providerRoots）。
  服务方：backend 平台共享智能体会话（writable_dir 注入 effective_allowed_roots）。
  单测 tests/interactive/session-manager-write-guard.test.ts（三态/fail-closed/Bash 提取器组合）。


- claude-agent-sdk 跨平台二进制用 pnpm overrides 统一钉版 0.3.181，升级需全平台验证。
- daemon 与 backend 的 session/permission 消息契约双向耦合，改动必须 backend daemon 模块 + frontend runtime-session 组件同步。
- 守护进程生命周期（PID/日志/恢复）跨平台差异大，Windows/macOS/Linux 都要验证。
- **dialog 审批不超时**：`PermissionResolver.register` 对 dialog 请求（`dialogKind` 存在，如 AskUserQuestion）不启 5min 兜底定时器，永久等待用户决策（与 backend `permission_service.py`/`protocol.py` 的 dialog 不超时语义对齐）；超时不再 deny 放行 "Proceed with recommended option"。普通工具审批（无 dialogKind）仍 5min 兜底；signal abort + abortAll 收尾两者都保留。
- **idle 自动回收默认禁用（D-001@v1）**：`session-manager.ts` 的 `_idleTimer` 默认不启动（`DEFAULT_IDLE_TIMEOUT_SEC=0`），session 不再因假性空闲被误杀。env `SESSION_IDLE_TIMEOUT_SEC>0` 可恢复旧行为（逃生口，用于极端运维）。session 终态收敛靠：backend 完成驱动 end + 用户手动 end（FR-05）+ interrupt（FR-04），不靠 idle 超时。
- **完成驱动 end（D-002@v1）**：scan run（`change_id=None` + `spec_strategy=platform-managed`）与 stage run（`change_id` 非空）的 lease 完成时，backend `complete_lease` 收尾链末尾主动调 `end_session`（经 facade 委托 + FR-05 `session_end` → daemon `SessionManager.end()` → claude 进程退出），区别于用户手动 end。多轮对话（非 platform-managed）不自动 end，留给用户手动。end 失败 try/except warn 不阻塞 lease 完成。
- **spec 树终态回灌 + change-write 分支（D-002/D-004@v1，2026-06-26-daemon-client-spec-sync-fix）**：scan run 终态（`onTurnResult` 收尾，`notifyRunResult` 后）+ onSessionEnd 兜底经 `syncSpecTreeIfNeeded(ctx, client)` 回灌（ctx null/undefined→no-op，失败仅 warn R-03）；`packSpecDir` push 路径**含** `.runtime`（非对称：pull 路径 backend `build_bundle` 仍排除）；`task-runner` `kind=change-write` 轻量分支轮询 `pending-change-writes`→claim→本地写 `~/.sillyhub/daemon/specs/<wsId>/changes/<key>/`→complete 回执→触发 syncSpecTreeIfNeeded，**不启 agent**。`hub-client` 加 pending/claim/complete change-write 方法。
- **install.sh .cmd wrapper 必须 CRLF + 纯 ASCII REM**：`scripts/install.sh` 生成 Windows `sillyhub-daemon.cmd` 时，heredoc 默认 LF 换行，需 `awk 'BEGIN{ORS="\r\n"} {sub(/\r$/,""); print}'` 转 CRLF；REM 注释一律英文。LF + UTF-8 中文 REM 在中文 Windows cmd.exe（GBK 代码页）下会让 REM 行解析错位、注释被当命令执行（打印 `'X' 不是内部或外部命令` 噪音，daemon 本身不受影响）。bash wrapper（无扩展名）UTF-8+LF 对 bash 无害，不受此约束。
- **install.ps1 分发响应必须显式 charset=utf-8**：`backend/app/modules/daemon/dist_router.py` 的 `GET /daemon/install.ps1` 用 `Response(media_type="application/x-powershell; charset=utf-8")`。starlette 对非 `text/*` 的 media_type **不自动补** charset（只有 `text/*` 如 `/install.sh` 的 `text/x-shellscript` 才自动补）；缺失 charset 则 Windows PowerShell `irm | iex` 按 latin1 逐字节解码 UTF-8 body，脚本中文（Write-Info/Write-Ok 输出）在执行 `[Console]::OutputEncoding=UTF8`+`chcp 65001`（install.ps1 源码第 36-38 行）之前即已损坏成 mojibake（如 `å®è£`）——控制台编码救不回读取阶段已坏的字符串字面量。回归锚点：`backend/tests/test_daemon_dist.py::test_install_ps1` 精确断言 `content-type == "application/x-powershell; charset=utf-8"`。
- **WS keepalive 防 NAT idle 断连**：`ws-client.ts` 的 `WsClient` 每 30s 主动 ping + 10s pong 超时 terminate（npm `ws` 库默认不发 ping，与 Python `websockets` 默认 20s ping 不同；TS 移植漏配 → 连接经 docker NAT 静止时被中间层掐断，长 RPC 如 import `get_spec_bundle` 打包 ~16s 撞断连窗口报 `HTTP_504_DAEMON_RUNTIME_OFFLINE` mid-rpc）。pong 超时走既有 close→reconnect 路径；所有 keepalive 定时器 unref，进程退出不阻塞。
- **WS 事件按 socket 身份守卫 + 假活看门狗（2026-08-27 网络切换永久假连事故）**：`connect()` 注册的 open/message/close/error/pong 事件与 pong 超时/握手超时定时器必须比对 `this._ws === ws`——旧 socket 迟到的 close 无守卫时会把新 socket 从 `this._ws` 抹成 null，新连接 open 后 `_startKeepalive` 因 `_ws=null` **静默跳过**，keepalive 丢失后网络切换的黑洞连接无 ping/pong 检测，状态卡 Connected、`connect()` 幂等保护令重连永不发生（HTTP 心跳与会话 HTTP 兜底照常=「在线」假象，唯独 backend→daemon RPC 全 502，git-log 报「守护进程离线」）。兜底：`daemon._wsLoop` 每秒 `_reapStaleWsClient`——isConnected 且 `lastMessageAt`（**pong 也计入**，传输级新鲜度）陈旧 ≥ `WS_STALE_REAP_MS`(120s) 或 `connectedAt` 锚点陈旧 → `_closeWsClient` 强制重建；双 null fail-open 交给 keepalive 主判据。

## 人工备注
<!-- MANUAL_NOTES_START -->
- **change 2026-08-22-team-session-unify**（会话内团队操作 daemon 侧）：①**注入谓词（D-002@v2）**：`cli.ts` isMainAgentSession 谓词矩阵——`provider==='claude'` 且 lease stage ∈ {空,'orchestrator'} 才注入团队 5 个 MCP 工具；分身派发 stage 常量化 `'mission_worker'`（backend execution.py 固定）不注入（防 worker 递归派发/converge 干扰 CC-12）；存量 external 主控 stage='orchestrator' 照常注入。②**MCP 会话上下文（B1）**：`buildDaemonMcpServerConfig` 增 env 参数注入 `MCP_SESSION_ID`（session-manager create 传 state.id；spike-01 PASS 确认 env 必须放 `mcpServers[*].env`，顶层 options.env 无效）；`mcp-server.ts` 5 工具参数可选化（mission_id/workspace_id/run_id 全可选，backend 按 X-Session-Id 解析活跃 mission，显式参数仅越权校验锚）+ 工具描述重写为「能力说明书」（如何拆解/仅用户明确要求时派团队/何时 converge/预算提示）；`hub-client.ts` MCP 端点请求统一携带 `X-Session-Id` header。`src/api-types.ts` 随 backend OpenAPI 重新生成（含 `/api/sessions/{session_id}/missions/*` 治理路由族与 TeamMission* DTO）。
- 2026-08-20-runtime-readpoint-repo-first | runtime.* 读点「仓库优先、缓存回退」：`runtime-handler.ts` 新增 `pickRuntimeSpecDir(workspaceId, rootPath?)` 三道校验（元字符黑名单 `ROOT_PATH_METACHAR_RE`（design §6 字符集，shell:true 命令串注入防线）→ `assertWithinAllowedRoots`（复用 file-rpc 导入，roots 由 daemon.ts 类字段构造点注入 `() => this._effectiveAllowedRoots()`，对齐 HostFsHandler 范式）→ `<root>/.sillyspec/.runtime` 存在性（pathExists 可注入）），全过读成员本机仓库 `<root>/.sillyspec`，任一不过 console.warn 回退 `~/.sillyhub/daemon/specs/<wsId>/`；workspace_id UUID 白名单 forbidden 仍 fail-loud（在回退 catch 外）。`daemon.ts` 四 handler 经导出 `normalizeRootPathParam` 归一透传（非字符串/空串→undefined）。动机：platform-managed 下 spec bundle 排除 .runtime，agent 驱动执行流数据真源在成员仓库，旧读点恒空。测试 35 passed（六类读点+normalize+注册器接线）。
- **2026-08-19-cross-workspace-team-mission**（跨工作区团队执行 + 项目维度会话）：daemon 侧 MCP 透传层对齐。`src/mcp-server.ts` 的 `dispatch_worker` inputSchema 新增可选 `target_workspace_id`（string）并透传 backend；`src/hub-client.ts` 的 `dispatchWorker` 同步加可选参（undefined 时不写 body）；`src/api-types.ts` 随 backend OpenAPI 重新生成。daemon 不感知 project 维度业务，仅负责透传字段，与 backend `agent/mcp_tools.py` 契约一致。vitest 通过，tsc exit 0。
- **ql-20260812-007-d086**（preflight 超时杀进程树）：修 Windows 上 daemon 启动卡死——preflight.ts 的 runCmd/runCmdBoolean 用 execSync+timeout，Windows 上 timeout 杀不掉 npm.cmd wrapper 的孙 node.exe（npm view sillyspec version 国内间歇慢时卡住），spawnSync 永等→daemon CPU 空转、心跳停（CPU profile 97.7% ntdll）。改为 runWithTreeKill（spawn 异步+Promise+超时杀进程树：Windows taskkill /T /F，Linux/macOS 进程组 kill），runCmd/runCmdBoolean/runSillySpecCheck/installSillySpec 改 async，runPreflight await。超时后杀整树不阻塞 daemon。测试 mock 从 execSync 改 spawn（17 passed）。
- **2026-08-12-init-provision-local-yaml**（init 写 local.yaml）：`handleInitLease`（spec-sync.ts）新增第4步 `writeLocalYaml`——在 writeDaemonState+pullSpecBundle 之后，读 ctx.platformConfig.local_yaml（backend claim 时注入的 shpsync_+shmcp_ 明文）+ serverOrigin，调新建的 `local-yaml-writer.ts`（TS 重写 sillyspec sync.js 顶层段替换算法：findTopLevelSectionRange/replaceTopLevelSection）写成员本地 .sillyspec/local.yaml：**platform 段无条件覆盖**（url=serverOrigin+token）、**mcp 段有才留**（D-004，已存在不动）。失败 try/catch 返 ok:false→_finish(false) lease failed（D-003 严格契约，对齐 writeDaemonState 逐步 catch 模型不抛错）。task-runner `_runInitLease` 透传 local_yaml + serverOrigin（**serverOrigin=config.server_url 唯一源，不用 payload.server_origin**，D-002 本机可达地址）。ESM import 带 .js（./local-yaml-writer.js）。
- **2026-08-13-platform-managed-file-sync**（spec 增量同步）：`postSpecSync` 不再整树 tar 全量——改文件级增量 diff（本地清单缓存移出 specDir `~/.sillyhub/daemon/manifests/{ws}.json`，首同步走旧 tar，增量 404/失败回退旧 tar，conflict 抛 SpecPushConflict 人工拍板）。**本地清单缓存与 specDir 分离后 pull 的 rm -rf specDir 不清缓存**；增量路径排除 `.runtime(有点)/runtime(无点)/worktrees`。hub-client `postSpecSyncIncremental` URL 必须 `/api` 前缀（spec_workspace router 挂 prefix=/api，用 REST_PREFIX=/api/daemon 会恒 404 回退——QA P0 已修）。
- **2026-08-15-init-trigger-sillyspec-init**（init lease 触发 sillyspec init）：`handleInitLease`（spec-sync.ts）编序 5→6 步——pullSpecBundle 之后、postSpecSync 之前插入 `runSillyspecInit`（硬失败 abort，D-003）：spawn `sillyspec init --dir <rootPath> --spec-dir <specCacheRoot> --workspace-id <wsId> --no-skills --tool <逗号列表>`（shell:true + 60s 超时杀树 + spawnFn 依赖注入），落 .sillyspec-platform.json 平台指针 + CLAUDE.md 等指令注入 + spec 骨架。**spawn 前 3s 版本门控**（MIN_SILLYSPEC_VERSION_FOR_INIT=3.26.8，查询/解析失败 fail-safe 拦截，错误前缀 sillyspec_init_cli_too_old 带中文升级指引，不依赖 daemon 重启）。tools 来源：cli.ts 构造 TaskRunner 前 AgentDetector 独立探测映射 SILLYSPEC_VALID_TOOLS（6 值同名交集），空/失败兜底 ['claude']。**上传排除三处统一**：UPLOAD_EXCLUDE_TOP_BASE（含 projects）由 computeIncrementalOps/buildFullManifest/packSpecDir 共用 + isUploadExcludedPath 过滤缓存残留行防 delete op 误删（pull extractTar 不排除）。init 失败 error 前缀 sillyspec_init_failed 透传 stats.init_error。CLI 侧配套（sillyspec 仓 3.26.8）：--no-skills 开关、--tool 逗号/重复多值、平台模式整体跳过项目内 .sillyspec 清理（保 local.yaml 手调段）。
- **ql-20260820-007**（spec 策略两路径静默失效修复）：①`handleInitLease` 时序重排 pull → writeDaemonState（原状态文件先写会在缓存根占位 `.runtime/` 普通目录，阻塞 pullSpecBundle 的 repo-native `ensureSpecJunction` 普通目录守卫 + repo-mirrored cacheEmpty 首拷判定 → 两策略静默降级 platform-managed；且 pull 的 rm -rf 会删掉先写的状态文件）；repo-native 时状态文件经 junction 落源项目 `.sillyspec/.runtime`（gitignored，三策略读写路径 resolveSpecDir(ws)/.runtime 统一）。②task-runner batch agent 路径 `pullSpecBundle` 补传 `strategy: ctx.specStrategy, rootPath: ctx.rootPath`（原只传 existingSpecRoot → 永远 platform-managed pull，rm -rf 拆除 interactive scan 已建 junction，repo-native 永久退化）。③`bumpLocalSpecVersion` 状态文件缺失/损坏时完整重建 2 字段（原「缺失跳过不创建」在 pull rm -rf 后保鲜永久失效，每次任务全量 pull）。副作用契约变更：pull 失败返回 `daemonState: null`（状态文件后置未写）。回归：test_init_lease 新 7 用例（init 策略分支/状态文件幸存/bump 重建/batch 透传）+ test_spec_version_refresh 2 用例改新契约；daemon 全量 2453 绿 + tsc 干净。
## 变更索引
- ql-20260918-012-ac1b | host_fs 第 11 方法 git_remote（工作区 Git 地址识别）：host-fs-handler 新增 gitRemote({root}) → {remote_url, error}——`git remote -v` 取首个 (fetch) 行 URL（无 origin 但有其它 remote 同样识别首个），无 remote → error="no_remote" 空态、非 git 目录/超时 → stderr 文案（不抛，仅 root 越界走 RpcError forbidden，对齐 git_rev_parse 语义）；daemon.ts 注册 host_fs.git_remote（params 归一与既有方法同款）。backend probe 端点据此识别回填 workspace.repo_url；旧 backend 不调用零影响，旧 daemon 不识别该 RPC 由 backend 侧 `_via_rpc_or_degrade`（DaemonRpcRemoteError）归 None。测试：host-fs-handler.test +5（GR1~GR5：fetch 行解析/非 origin remote/空输出 no_remote/非 git 目录/越界 forbidden）54 passed；typecheck 0。
- 2026-09-12-provider-file-tx | 供应商切换文件层事务性（24h 审查 M-7 修复）：①`src/atomic-write.ts` writeFileAtomic（tmp+fsync+rename 顶替，Windows=MoveFileEx REPLACE_EXISTING，失败清 tmp）——codex 两文件+pi 三文件+宿主镜像拷贝六写盘点全部原子化（rollout 拷贝按划界不纳入）；②ForReload 落 `.sillyhub-managed` 生效标记（分支一后置 best-effort；分支四标记先行——标记失败跳过镜像含删除动作，R-03 双失败从根消除）；③reload 事务性三改造（引擎门 provider 维度值比较判据、resume 守卫前移=缺 key 零文件写入、catch 以旧形态重跑 ForReload+空返回删标记）；④restore null+codex 探测三态化（标记在=managed / 无标记有 auth|config=legacy 兼容补落标记 / 皆无=零动作——迁移钩子只建目录恒落第三态，F3 假阳性消除）。测试：九套件 189 用例全绿+typecheck 0。
- ql-20260911-027-13b6 | vendored ask-user 扩展（pi AskUser 弹窗缺失的发起端补全，会话 d4c29d95 实证：pi 会话模型只能文字罗列问题、session_dialog_requests 零行——2026-09-09-askuser-pi-cursor 只桥接了 extension_ui_request 的接收半，但 pi 本体无任何模型可调提问工具、daemon 也只挂 subagent 扩展，弹窗链路无发起端）：①新增自研扩展 `vendor/pi-extensions/ask-user/index.ts`——注册模型可调 `AskUserQuestion` 工具（有 options 走 ctx.ui.select、无 options 走 ctx.ui.input；**不传 timeout** 对齐平台 dialog 永久等待语义；hasUI=false/取消/空答均降级明确文本告知模型），工具名对齐 claude 内置名 → daemon/backend 双端 tool-kind 分类器零改动归 ask、backend scan 提示词「必须调用 AskUserQuestion」对 pi 直接成立；②pi-rpc-driver 抽共用 resolveVendoredExtensionPath + piVendoredAskUserExtensionPath（降级开关 SILLYHUB_PI_ASK_USER_EXTENSION=off，语义同 subagent）+ spawn 追加第二个 `--extension`（pi CLI 支持多次）；③vendor README 补自研件行。测试：pi-rpc-driver 78 passed（+5：路径解析三态/双扩展 spawn 参数面含顺序断言）；tsc 0；本机真实 pi CLI 装载冒烟过（对照实验：坏扩展会报 ParseError，方法有效）。
- ql-20260911-024-9098 | 平台命令结果回显提速第一级（用户反馈「执行很慢，基本都要十秒以上」——实测命令本体 0.2-0.7s，大头是结果回显两级 15s 定时器）：daemon.ts `_runSillySpecCommand` 忙拒落槽与执行完成（finally 统一出口）均立即补发一次心跳——新私有方法 `_nudgeHeartbeatAfterCommandResult(action, phase)`（debug 日志 sillyspec_command_heartbeat_nudge）`void _sendHeartbeatOnce()` fire-and-forget，结果不再等下一个 15s 心跳节拍。安全性：_sendHeartbeatOnce 全路径 catch 不 reject（不阻塞 WS 接收）；未注册 runtime 时其内部 return false 静默跳过；与 15s 循环短暂重叠无害（心跳是无状态全量上报，backend 字段级 last-write-wins）。第二级（前端下发后 5s×3 加速轮询）见 frontend.md 同 ql 条目。测试：sillyspec-platform-command.test.ts +3（WS 下发完成即补发且第 7 参携带新结果/忙拒路径同补发+放行后首条完成再补发共两次/未注册 runtime nudge 静默 no-op）共 41 绿；近邻 daemon-heartbeat-sillyspec + sillyspec-conflict-snapshot 49 绿；tsc 0。
- ql-20260911-003-355a（24h 审查 P2 修复·daemon 三件）| ① fileDiff 256KB 截断代理对安全化——新增 `truncateUtf16Safe(str, maxChars)`（sillyspec-manager.ts 导出）：String.slice 恰切在代理对中间时尾字符为 lone surrogate，backend Python 侧 ensure_ascii=False UTF-8 编码抛 UnicodeEncodeError → HTTP 500（星面字符 diff 边界约半概率）；截在高代理项（U+D800-DBFF）时丢弃该项。② zcode SQLite 读取内存预算——readZcodeSqliteMessages 遍历行累计 data code unit 超 `DEFAULT_MAX_CONTENT_BYTES`（20MB，与文件路径 lstat 预检同源常量；opts.maxContentUnits 注入测试）返回 too_large，不再全量物化后才切 200 段窗口。③ zcode 分派存在性门——host-fs-handler zcode 先库后文件分派前先 `lstat(abs)`：上报 rollout 文件必须真实存在才进库读取器（log_path 可经 platform_sync 自登记任意路径；文件缺失回落文件流程最终 not_found）。④ pi/cursor 受控 reload 基线回收——`_reloadSession` 成功后经新可选 `SessionManagerDeps.onSessionReloaded` 通知 daemon 删 `_modelUsageBaselineBySession[sessionId]`（快照 per-handle 归零后残留基线致恢复首轮差分少记）。测试：sillyspec-file-diff +1（代理对边界）/read-zcode-sqlite +1（预算）/zcode-sqlite-dispatch ZD1/ZD3/ZD6 改写/session-manager-config-switch CFG-1 +reload 回调断言，定向 99 passed；tsc 0。
- ql-20260911-001-c0be | 对账表 RPC sillyspec_scope_audit（结果卡 daemon 半）：SillySpecManager.auditTable(change, workspaceId)——spawn 本机 CLI 跑 scope-audit --change --json 表模式（信封实测：full-flow 行 {path,additions,deletions,kind,planned,verdict}，quick 行 {…,declared,attribution}，totals/baseAnchor/degradedReason/excluded.foreignDeclared），投影 SillySpecAuditTable（锚点 hash 短化 7 位、语义锚原样；rows 500 行护栏截断置 truncated）；执行器抽 _runScopeAuditJson 共享（fileDiff 与 auditTable 防双实现：能力门/stderr/超时/JSON 契约校验一处维护，返回型 Record & {ok:boolean} 免调用方重复窄化）。daemon.ts 注册 sillyspec_scope_audit（params 归一同 conflict_snapshot）。测试：sillyspec-file-diff.test +3（full-flow 投影+spawn 无 --file/quick attribution+语义锚+degraded/500 行截断+能力门复用），12 passed；typecheck 0。
- ql-20260910-017-2006 | 单文件变化比对 RPC sillyspec_file_diff（变更中心点击文件看 diff 的 daemon 半）：SillySpecManager.fileDiff(change, file, workspaceId)——_resolveWorkspaceRoot 取根 + _execSillySpecCli 复用执行器 spawn 本机 CLI 跑 `scope-audit --change <c> --file <f> --json`（锚点解析与对账行数同源，daemon 零自研 git/锚点逻辑），stdout JSON 信封投影 snake_case（SillySpecFileDiff：ok/mode/base_ref/anchor_label/diff/note/truncated），diff 256KB 截断护栏（SILLYSPEC_FILE_DIFF_MAX_CHARS）。能力门：exit 非零且 stdout+stderr 含「未知命令/unknown command」→ RpcError sillyspec_capability_missing（旧 sillyspec 升级引导，前端弹窗分型文案）；其余非零/非 JSON → scope_audit_failed；超时 → scope_audit_timeout。配套：SillySpecProgressOutcome 增可选 stderr（execFile 回调原本丢弃——CLI 用法/错误横幅走 console.error，能力检测需要；progress show 链路不消费零影响）+ runProgressJsonDefault 捕获；daemon.ts _registerSillySpecRpcHandler 注册 sillyspec_file_diff（params 归一同 conflict_snapshot）。测试：sillyspec-file-diff.test.ts 9 用例（信封投影+spawn 数组形参断言/untracked note 透传/能力门/入口断言/超时/非 JSON/截断护栏/RPC 注册归一透传）；回归 heartbeat/conflict-snapshot/manager 四套件 122 passed；tsc 0。
- ql-20260910-014-6c29 | 心跳 sillyspec_status.changes[] 补可选 ql_id（变更中心 scope-audit 命令代入的 daemon 半）：SillySpecStatusChangeItem 增可选 ql_id 字段（quick 会话映射的 QUICKLOG 编号）；collectStatusOnce 后处理新增 _attachChangeQlIds——对 quick-* 名变更条 best-effort 读 .runtime/quick-sessions/<name>/guard.json 的 quicklogId（复用 readQuickSessionQuicklogId，镜像 _attachPendingConflictQlIds 先例：单条意外仅缺省该条不阻断整拍心跳快照）。普通变更/读不到为 null 或缺省，旧 daemon 不上报无影响（backend 宽松可选字段）。前端快速修复抽屉据此把 ql_id 反查成 quick-<8hex> 会话名代入 scope-audit --change 命令。测试：daemon-heartbeat-sillyspec +1（guard 命中补入/guard 缺失 null/非 quick 条不带/六字段投影不受补报影响，真实 fs 临时 spec 根 + DI 假 runProgressJson），两套件 50 passed；tsc 0。
- ql-20260910-007-ce62 | codex AskUser 解锁：spawn 参数内置 -c features.default_mode_request_user_input=true（app-server 子命令前）——request_user_input 工具 codex 官方默认仅 Plan 模式可用，app-server 无以 plan 建线程（ThreadStartParams 无 mode；enablement/set 对 underDevelopment 特性不生效，0.147/0.154 双版本实测），官方解锁开关即该 features 键（GitHub openai/codex#24750/#29104）。生产案：阿里云 codex 会话 118406c3 模型明言「工具仅计划模式可用」改文字作答。旧 codex 未知 -c 键非 strict 仅告警不失败（实测）向前兼容。本机 ~/.codex/config.toml 已同步加该键（存量 codex 进程重启后生效）。测试：codex 驱动两套件 77 passed（+1 spawn 参数断言，3 处旧 args 断言适配）。
- change 2026-09-09-askuser-pi-cursor | AskUser 通道 pi 接入+caps dialog 能力位：①pi-rpc-driver extension_ui_request 提问类四方法（select/confirm/input/editor）从自动 cancelled 改桥接——归一化 questions[]（confirm 合成是/否、input/editor 合成占位选项）→ sessionPermission.requestUserDialog 上抛（dialog_kind=pi_extension_ui）→ Map<rpcRequestId,replyFn> 挂起表 → denormalizePiDialogReply 回流（select→value/confirm→confirmed，不可解析 fail-closed cancelled）→ _cancelAllPendingDialogs 三锚点兜底（_close 直调+_rejectAllPending 汇点）；权限类/未知方法零桥接红线保持；dialog 永久等待不超时对齐。②driver-factory sessionPermission 注入扩 pi 分支+PiStartOptions.sessionPermission? 槽位。③providers caps 增 dialog:'native'|'marker'|'none' string 枚举键（打破全 boolean 旧约定）——claude/codex/pi=native、cursor=marker 初值（spike no-go 联动改 none）；pi permission_dialog 翻 true。测试：pi-rpc-driver 73 用例（+22 四态：上抛/回流/中止兜底/权限类拒绝）。task-08（marker prompt 注入）挂起——cursor spike 因免费额度 INCONCLUSIVE（2/2 有效运行合规），额度恢复补测 go 后另卡执行。
- ql-20260909-016-fa10 | init lease 写盘跳过补 warn + 心跳工作区键 UUID 守卫（docs/sillyspec/init-lease-silent-no-local-yaml.md ② + daemon-heartbeat-workspace-key-no-uuid-guard.md ①②，2026-09-09 junction 切换 422 实证）：①task-runner _runInitLease 跳过 writeLocalYaml 时 console.warn('task_runner: init_lease_local_yaml_skipped', leaseId, 原因枚举)——local_yaml_missing / local_yaml_not_object / token_empty_or_invalid，跳过语义零变化，泄漏凭据不再整链静默；②daemon.ts 增 WORKSPACE_ID_RE/isWorkspaceIdShape 单源守卫，三处接线——_collectSpecCacheEntries 扫 specs 目录名非 UUID（备份/临时目录）跳过不进心跳 + warn 一次（Set 去重防每跳刷屏；实证：junction 备份目录名 rename 留在 specs 根即毒化整心跳 422）、_noteSillySpecStatusRoot claim 学习键非 UUID（ws-b1~b4 测试残留）拒绝登记 + warn 一次、_restoreSillySpecStatusRoots 恢复时过滤存量污染键 + warn 带被拒键清单。测试：test_init_lease +1（缺失 warn 一枚举 + 全凭据不 warn 且 writeLocalYaml 被调正问）、daemon-status-root-persistence +3（三守卫各一），含新文件共 36 passed；回归 daemon-heartbeat-sillyspec/test_spec_version_refresh/runtime-handler/daemon-heartbeat-pending 101 passed；tsc 0。心跳 422 loc 提升未做（守卫落地后 422 不再发生，文档建议保留）。
- ql-20260909-004-e5f9 | 修复 24h 审查第二批三项：①cursor chatId 双轨分叉（M3）——resume 失效时 cursor 静默开新 chat，活会话 handle.chatId 随帧更新但持久层 state.agentSessionId 被 write-once 守卫锁旧值，daemon 重启恢复即回旧 chat 上下文分叉；修：events.ts session_started 分支加 provider 限定跟随（state.provider==='cursor' 时允许主流新 sid 覆盖 + console.info 记换轨，其它 provider 维持 D-003@v1 write-once 不动，claude 子代理 init 双重守卫保持）——选 provider 限定而非全局放宽，因 write-once 是显式设计决策且被既有测试锁定。②cursor-driver 帧采纳 session_id 只验非空 string——补 UUID_RE 校验（对齐 _tryCreateChat 先例），畸形 sid 不进 handle.chatId、不拼下一轮 --resume。③tailer 预读失败残留旧 range 缓存——catch 只不写入，上一轮旧字节被 readCachedRange 当本轮新增量重复喂 tail（注释宣称的 fail_open unknown 实际不成立）；修：DefaultFs 加 forgetRange，catch 内清掉残留条目走真 fail_open。测试：session-manager +3（cursor 跟随/claude write-once 保持/子代理不覆盖，直调 dispatchStatusEvent）、cursor-driver +1（畸形 sid 不采纳不拼 --resume，createChatTimeoutMs 调小防慢）、tailer +1（猴补 readRangeAsync 模拟读失败间隙断言 fail_open）；interactive+liveness 全量 64 文件 902 passed，tsc 0。
- ql-20260909-002-3667 | 修复 24h 只读审查四项高/中危缺陷：①liveness tailer R2 endedPaths 误杀 pi 复用路径——登记前提「日志路径按会话唯一」对 pi 不成立（session.jsonl 按 cwd 固定跨会话复用、registry 无 pi deriver 走 L0 mtime 判 ended），idle>15min 即 ended 后同 cwd 的 pi 会话被 add() 永久拒绝直到 daemon 重启；修：Set→Map<path,endedAt>，add 命中登记时经可注入复活探针 statSizeSync（默认 node:fs statSync）比 mtime>endedAt 判复活放行（mtime 未动的 registry-sync 死路径重推维持拒绝，防震荡语义不变）。②spec pull 整树交换前二次回灌——D-008 检查原只在 pull 起点做一次，getSpecBundle 下载+解包 ~40s 窗口内会话新写 spec 文件在交换时被覆盖（SWR 后台 revalidate 恰在会话启动成功后 fire，正落窗口）；修：pushUnsyncedIfDirty 抽 helper 两处复用（pull_start / swapExtractedDir 前 pre_swap），二次回灌失败 abort 交换保留本地（刻意不落 in-place 解包兜底）；真实 postSpecSync 增量 diff 零 ops 不发网络请求（幂等无谓开销为零）。③CursorDriver 轮次收敛后旧 stdout 'data' 监听未摘除/流未 destroy——排空宽限超时收敛时迟到字节会经 framer 外发记到下一轮 runId 名下（错轮归因），孙进程持管道写端时 child/framer 随轮滞留；修：detachStreams（removeListener+destroy，'error' no-op 监听刻意保留防 EventEmitter 无监听抛）在 interrupt/error/exit 三收敛路径统一执行，interrupt 分支漏 .unref() 的 2s 定时器补齐。④sillyspec-manager 工作区摘要 map 随目标集裁剪——_statusSummariesByWs 原只有 set/②整体 clear，daemon 侧 LRU（上限 8）淘汰的 wsId 摘要永久滞留（心跳 map 整包直发：淘汰工作区脏数据+随历史 ws 数无界增长）；修：collectStatusOnce 多目标循环后移除非目标集槽位（目标内 ws ③缺席保留旧值语义不变）；另 daemon 心跳第 10 参补采集关闭门控（interval=0 不再发空 map 覆盖 backend 已存值，对齐第 6/9 参口径）——④两文件属未提交的总览工作区级化特性同批（随其提交）。测试：tailer +2（探针复活/默认 statSync 真实 utimes 复活）、pull_before_push +2（窗口内新写入二次回灌/失败 abort 保本地）+3 断言随行为（checker×2、postSpecSync 幂等仍 1 次）、cursor-driver +1（收敛后迟到帧不外发+流销毁）、sillyspec-manager +1（LRU 淘汰裁剪）、daemon-heartbeat +2（第 10 参装配/关闭门控）；定向 19 文件 269 passed，tsc 0。
- ql-20260907-004-dea5 | 修 Windows 会话/批量任务弹黑框（用户实测 pi 会话弹命令窗口）：daemon 以无自有控制台形态运行（IDE 直跑 / VBS 隐藏自启拉起）时，三个拉 agent 的 spawn 点——task-runner.ts 批量、interactive/pi-rpc-driver.ts 会话、interactive/codex-app-server-driver.ts 同款——均未设 windowsHide → Windows 为控制台子进程（node 跑 pi/codex/claude CLI）新开可见命令窗口挂整个会话。修：三处 spawn options 补 `windowsHide: true`（libuv 传 CREATE_NO_WINDOW；stdio 管道不受影响，非 Windows 无操作），对齐仓内既有约定（preflight respawn/taskkill、runtime-handler runSillyspecCmd、autostart schtasks、host-fs-handler taskkill 均已带）。claude 会话经官方 SDK 内部 spawn 不在三处内，后续观察是否需要 SDK 侧处理。测试：pi-rpc-driver/codex-app-server-driver 各 +1 用例断言 windowsHide（objectContaining）、task-runner 主流程用例补 toMatchObject；vitest 定向 3 文件 154 passed，tsc 0。
- ql-20260906-004-62c6 | 修复审计 #9：turn 在途 close() 后 consume 协程永久挂起——pi 与 codex 两 driver 统一在 _close 释放轮次等待者。机制：_terminateSession → handle.close() 置 closing=true 后杀进程，被杀进程不会再发收敛帧（pi agent_settled / codex turn/completed），exit handler 又因 closing 早退不兜底 → consume 挂在 settleWaiter / await currentTurnPromise 永不返回（每次会话关闭泄漏一个协程+闭包：normalizer/segments/stderr 缓冲，finally 清理全被跳过；codex 为既有平台模式、pi 复制时引入同款+waitAgentSettled 多一处挂起点）。修（两 driver 对称）：①consume 把释放器挂 handle 内部槽（沿 _ctx cast 惯例：pi `_releaseSettledWaiters` / codex `_finishTurnOnClose`=finishTurn({kind:'cancelled'})），finally 清槽防陈旧引用；②_close 置 closing 后调用释放器；③codex 主循环在 `await currentTurnPromise!` 之后补 `if (h.closing || finalized) break;` 守卫（pi waiter 后本就有同款）——close 释放的 cancelled 轮不上报假 result，终态归 _terminateSession（守卫同时覆盖「turn 恰好完成后、上报前 close」的窄竞态，跳过上报无害——reportResult 本有 finalized 幂等）。测试：两 driver 各 +1 回归（turn/started 后不喂完成帧、handle.close()、Promise.race 3s 超时兜底断言 consume 返回且 results/errors 双空——修复前必挂超时），pi-rpc-driver + codex-app-server-driver(+approval) 3 套件 114 passed，tsc 0。
- ql-20260906-003-a611 | 修复审计 #10（daemon 侧半）：vendored pi 扩展树分发链——install.sh 与 preflight 自更新此前只搬 sillyhub-daemon.js/mcp-server.js 两文件，vendor/ 永不到 `~/.sillyhub/daemon/bin/`，piVendoredSubagentExtensionPath 两候选落空 → pi --extension 静默跳过。修：①install.sh fetch_latest 提取 latest.json vendorFiles（`grep -o '"vendor/[^"]*"'` 纯 shell 无 jq 依赖），download_bundle 逐文件下载（vendor/ 前缀 + `..`/反斜杠子串白名单，tmp+mv 原子替换，单文件失败 warn 不中止）；②install.ps1 Get-LatestManifest 存 `$script:VENDOR_FILES`（Invoke-RestMethod 原生 JSON），Download-Bundle 逐文件下载（同白名单 + Join-Path 分隔符转换 + tmp+Move-Item）；③preflight.ts 新增导出 updateVendorBundles（LatestInfo.vendorFiles 可选键，fetchLatest 过滤 string 形态；URL 推导同 mcp-server.js「主 bundle URL 同目录+rel」；无 BUILD_ID 校验 [.ts/.md 非 JS bundle]、tmp+rename+mkdir 目录链、isSafeVendorRelPath 白名单 [`..` 子串级，与 shell/ps1 三端口径一致——段级判等挡不住 `pi..evil` 形态]、单文件失败 warn 继续），runDaemonSelfUpdate 在 updateMcpServerBundle 后调用，旧服务器无 vendorFiles 键 no-op 兼容。测试：preflight-download-replace +5（嵌套目录落盘无 .tmp 残留/白名单四态拒绝不 fetch/空清单与 undefined no-op/自定义 URL 形状 debug 跳过/单文件 404 继续其余），51 passed + tsc 0；install.sh bash -n 过、install.ps1 PowerShell AST parse 过（两脚本无自动化 harness，逻辑对齐 preflight 已测口径）。配套 backend 侧见 backend.md 同 ql 条目。
- ql-20260906-002-f6b7 | 修复审计 R1：immediateAck 冲刷失败后无自驱动重试的误杀残余窗口——ql-20260904-022 引入的立即冲刷失败仅 warn 留桶，唯二后续触发点（心跳补拉要求 pending_controls>0，而该计数只统计 pending 行、WS 送达即 delivered 的行不计入；WS 重连对账）在「ack POST 单次瞬时失败 + WS 保持连接 + 10 分钟内无新 pending 指令」组合下都不会发生，session_inject 行按 delivered-未-ack 过期 → backend GC 联动判死等用户回答的活轮（正是 ql-022 要修的事故模式的窄残口）。修：control-dispatcher 新增 `_immediateFlushWithRetry`——immediateAck 冲刷失败后短退避（CONTROL_ACK_RETRY_DELAY_MS=5000，Options.ackRetryDelayMs 可注入）**单次**自驱动重试；按 runtime key 定时器去重（退避窗内多失败合并一次）、unref 不阻进程退出、fire 即清位不常驻；二次失败留桶交还既有补拉/重连兜底（不无限重试不引入周期任务）。**与补拉批量语义零耦合**：pullAndConsume 批尾的 _flushAcks 保持 await 直调、失败不挂重试；重试只重刷单 runtime 桶，与趟批冲刷并发时 backend ack 幂等（重发 no-op）无害。类头「不做」段收窄为「不做**指令**重投调度」（ack 回执链路收口不属指令重投）。测试：control-dispatcher 23/23（新增 4 用例：退避重试成功出队/只重试一次封堵无限重试/退避窗内定时器去重/成功不挂定时器；pendingAckCount 并入 waitFor 避 ackControls 入口 push 与出队微任务的竞态）+ resilience-scenarios 27/27 + tsc 0。
- ql-20260905-001-fc24 | 修复昨日只读审计 4 项 daemon 侧高置信缺陷：①**pull 后 manifest 回填真实版本**（审计 #1 修正轮，修正 ql-20260904-019 注释宣称的「同内容豁免/全量回退降级链无新增失败模式」——同内容豁免只覆盖无改动、SpecPushConflict 明确 throw 不回退，pull 后首次真实改动必 conflict）：pullSpecBundle 落地后读顶层 PLATFORM-BUNDLE.json 的 manifest_versions（backend ql-20260905-001 新增第五键）传 buildFullManifest 回填真实 base_version；旧服务器 bundle 无该键退化 version=0 兼容。PLATFORM-BUNDLE.json 加入 UPLOAD_PRUNE_NAMES_BASE（不进 manifest/不上传，消除 pull 解包残留随全量 tar 回推服务器的既有污染）。②**pi-events segments Map 键并入消息序号**（审计 #3）：原键 `ci<contentIndex>` 跨消息撞键（真实 fixture 两消息均 ci0）→ 第二条消息起 partial 被遗留 flushedLen 压制或错位截取；改 `m<msgSeq>ci<idx>` + message_end 清当前消息段 + turn_end 全清兜底（Map 不随会话膨胀）。③**MIN_VERSIONS 补 pi: [0,81,0]**（审计 #5）：PROVIDER_SPECS.pi.minVersion 此前无消费点、表缺条目 → checkMinVersion 恒 null 版本门禁从未生效；version.test.ts 加「声明 minVersion 的 provider 必有同值表条目」守护防再犯。④**install.ps1 Defender 排除收窄**（审计 #4）：整个 ~/.sillyhub（含 workspaces 任意代码执行区+daemon 本体）→ 仅 ~/.sillyhub/daemon/specs（写放大真正源头），先建目录保证排除路径有效。测试：spec-pull-swap +2（真实版本回填+元数据不进清单 / 旧 bundle 兼容）、pi-events +1（跨消息重号+turn_end 清账）、version.test 守护用例，3 文件 72 passed，tsc 0。
- ql-20260904-024-e59b | init-lease 测试适配 ql-20260904-019 manifest 回写新契约（daemon-ci 红修复，纯测试债）：ql-019 起 pullSpecBundle 成功即按落地树重建 manifest 缓存 → handleInitLease 第 4 步 postSpecSync 在「init 无新增文件」时增量 diff 恒零 ops 按契约跳过不发请求（postSpecSyncImpl ops.length===0 早退，sillyhub-daemon/src/spec-sync.ts:728），test_init_lease 两例仍锁「init 后必 post」旧前提（前提来自「清空 manifests → 首同步必全量 post」，pull 回写后不再成立）。适配：ws-init-ok 改锁「无改动跳过 post」；ws-init-order（时序 C 例）与 postfail 例的 spawn mock 镜像 sillyspec init 落骨架真实行为（init 步往 specDir 写骨架文件），post 经增量 ops>0 + mock client 无 postSpecSyncIncremental 回退旧 tar 真实触发——「init→post→localYaml」时序覆盖与 R-03 软失败路径不再空转。生产代码零改动。测试：test_init_lease 28/28 绿、tsc 0。
- ql-20260904-022-ab52 | WS 送达控制指令 ack 无冲刷触发点修复（GC 误杀等用户回答的活轮，事故会话 e148364e/run ca7ec9b8：设计确认 AskUserQuestion 卡挂 10min 后 run 被判死，点选报 no active run to approve）：ack 冲刷原本只在 pullAndConsume 发生，而触发条件（心跳 pending_controls>0 / 重连对账）对 WS 送达即 delivered 的指令永不成立——backend pending_controls 只统计 pending 行——daemon 活着消费后 ack 永远留队，10 分钟后 GC 按 delivered-未-ack 联动判死 run（interactive_inject_send_failed「消息指令已送达执行端但 10 分钟内未被执行（无回执）」），等 AskUserQuestion 用户回答超 10min 必中。修：control-dispatcher consume() 新增 immediateAck 选项（入桶后 fire-and-forget 冲刷该 runtime 桶；POST 失败 ids 留桶由既有补拉/重连兜底；runtimeId 缺省的 UNKNOWN 桶无 ack 端点可定位，维持入队等捎带）；daemon.ts _dispatchControl（WS 六类控制消息统一入口）传 immediateAck: true；补拉路径不传、批尾单次冲刷批量语义不变。效果：10min GC 窗口与「等用户回答」时间天然解耦。测试：control-dispatcher 新增 4 用例（立即冲刷/冲刷失败留队补拉兜底/UNKNOWN 桶不冲刷等捎带/补拉批量单次 ack 不变）19/19 绿；近邻 kind-dispatch/resume-route/interactive-bridge/ws-client-session-control/inject-drop-report/heartbeat-pending/disk-probe-pending/integration resilience-scenarios+worker-resume 共 10 套件 146/146 绿；tsc 0。UNKNOWN 桶残余（permission_response 三处下发点 payload 无 runtime_id）已由 ql-20260904-023-0bea 收口（backend payload 补键，见 backend.md 同条目）。
- ql-20260904-019-17cc | 修 pullSpecBundle 成功后不回写本地 manifest 缓存（ql-20260904-016 遗留缺口，E2E 实证会话 0871e917/b3fa8993）：pull 整树覆盖本地后 manifests/{ws}.json 仍是上次 push 时旧态——下次「版本文件丢失/mtime 信号」触发 push_before_pull 时 diff（旧 manifest ↔ pull 后本地）产出全量假 ops，撞服务器 base_version 乐观锁判 conflict → SpecPushConflict abort pull（会话卡旧缓存）。修：pull 落地（交换或原地兜底）后用落地树 buildFullManifest 重建 manifest 缓存（version=0 对齐 full-tar 回退既有语义 Q7/R-07：本地==服务器 → diff 恒零 ops；pull 后首次真实改动走同内容豁免 no-op+new_versions 对齐或既有全量回退降级链，无新增失败模式），顺带消除 pull 后任务末尾的无谓整树回传 no-op（30s 级）。测试：spec-pull-swap +3（manifest 重建一致性/事故回归「版本文件丢失→push 零 ops 不 POST→pull 成功」/真实改动不被吞）；task-09 四用例按「pull 落地即镜像→无改动不回传」新契约改两轮 lease 形态（一轮 pull+bump、注入改动、二轮同版本跳过 pull 触发回传），16 用例全绿。残余边界（如实）：「版本文件丢失 + manifest 双坏」叠加仍会卡 pull（概率极低，R-03 容错会话照常），manifest 权威对齐需 daemon 可访问的 per-file 清单端点（现仅 shpsync_ CLI token），留待后续。
- ql-20260904-016-7cab | 会话首响性能优化（daemon 侧，排查会话 f0f76381 首响 46.5s）：**extractTar 两段式 + 16 并行写**（原逐文件 await 串行写，Windows 含杀软扫描 ~8ms/文件 ×3563 ≈ 28s）——先纯内存解析+校验全部 entry（坏 tar 在任何写盘前抛出）再 worker pool 并行落盘；**pullSpecBundle 改 tmp 目录解包 + 原子交换**——解到同级 {wsId}.tmp-{pid}-{ts}，旧目录 rename 让位（瞬时）、tmp rename 顶上、旧目录后台异步 rm（3563 文件删除移出关键路径），解包失败旧缓存完好（原 rm 先行会打残缓存），swap 失败重试后回退原地解包（旧语义），specDir 为 junction 残留只删链接不碰目标，启动时清扫 >10min 的 tmp/trash 残留。**getSpecBundle 超时 30s→120s**（SPEC_BUNDLE_TIMEOUT_MS，36MB 全树 tar 冷拉取实测 15-30s+，30s 恒假失败；配合后端 gzip+缓存，见 backend.md 同 ql 条目）。**SpecPushBeforePullError message 带内因**（原只报 wrapper，conflict 被误读为超时）。**install.ps1 加 Defender 排除步骤**（~/.sillyhub 加入排除，权限不足自动 UAC 提权一次、120s 应答超时不阻塞安装、无 Defender 静默跳过）。测试：spec-pull-swap.test.ts 新 8 用例（首拉就位/覆盖镜像/坏 tar 保缓存/trash 后台清理/junction 目标不误删/60 文件并行一致性/空 tar/404 容错），task-09-spec-pull-push 16 + spec-sync 既有套件零回归。E2E 实测：缓存命中 pull ~1.5s（原 30s 超时失败）、冷预热一次性 ~41s 后全量命中。
- ql-20260904-017-28be | 会话创建凭证持久化闭合（daemon 重启恢复丢供应商致 SDK 裸起 "Not logged in"，会话 2f08b5da 实证）：claim 下发的 provider_config 原本只进 spawn env（内存，env 字段明确禁止落盘），SessionManager.state.providerConfig 唯一赋值点是切换供应商（reloadWithProvider）——首次创建的会话凭证从不进 sessions.json（实证：18 会话 record 全无 providerConfig 键）。daemon 一重启，恢复链（restore 读 record.providerConfig → buildSpawnEnv 第 0 层）拿不到凭证，claude 隔离 CLAUDE_CONFIG_DIR 又无登录态 → SDK 直接 "Not logged in · Please run /login"（0 次 API 请求秒失败，被误读为远端 401）。修复三处：types.ts CreateSessionInput 加 providerConfig 可选字段；session-manager _createInternal 建 state 时条件展开记录（null/undefined 不写键，与切换链语义一致，既有 snapshotPersistable 落盘 + restore 读回链自动生效）；daemon.ts _startInteractiveSession create 调用透传 execPayload.provider_config。测试：config-switch 新增 PERSIST-0/PERSIST-0b（create 带凭证落盘 / 不带不落键）29 用例绿 + 近邻回归 41 用例绿 + tsc 0。运维自救（存量坏会话，新代码未部署前）：会话底部「切换供应商」切「不指定」再切回目标供应商（provider_config_changed → reloadWithProvider → state 落盘 + SDK 带凭证重启），或结束会话重开。
- ql-20260827-004-c49b | 修网络切换后 WS 永久假连（wp 机器切换网络 → git-log 全 502「守护进程离线」但心跳/会话正常，2026-08-27 事故）：ws-client `connect()` 事件与 pong/握手超时定时器加 socket 身份守卫（旧 socket 迟到 close 曾把新 socket 抹出 `this._ws` → keepalive 静默丢失 → 状态卡 Connected 永不重连）+ `_handlePong` 计入 `lastMessageAt`（传输级新鲜度）+ 新增 `connectedAt` 锚点；daemon `_wsLoop` 加 `_reapStaleWsClient` 假活看门狗（isConnected 且新鲜度陈旧 ≥ `WS_STALE_REAP_MS`=120s → 关闭重建，双 null fail-open）。测试：ws-client.test.ts +2（迟到 close 回归/pong 新鲜度）、daemon-ws-stale-reap.test.ts 新 4 用例，相关 87 用例全绿 + tsc 0。
- change 2026-08-24-platform-session-feedback-fix（verify 返工后，分支未合 main）| 会话反馈事件上报（出站）+ plan 决策接收（入站，verify P0 返工 89649656）：hub-client 新增 4 方法 notifyPlanModeEntered/notifyBashStatus/notifyBashChunk/notifyAgentTaskStatus（X-API-Key 鉴权 + URL encode + HubHttpError 透传）；interactive/types.ts 新增 `SessionEventForBackend` 联合 + `SessionManagerDeps.onSessionEvent` 可选回调（未注入零影响）；session-manager 在 turn 消息流识别 Bash/Enter+ExitPlanMode/Task+Agent tool_use（`_runningBashCommands` Map 配对 tool_result）fire-and-forget 上报，异常吞掉不阻塞 turn；cli.ts 桥接回调→hub-client（camel→snake 映射）。**plan 决策回传闭环（返工补齐）**：protocol.ts `MSG.PLAN_RESPONSE='daemon:plan_response'`+`PlanResponsePayload`（与 backend 逐字对齐）、daemon.ts `_handleWsMessage` case→`_routePlanResponse`（字段校验+非阻塞分发）、session-manager `resolvePlanResponse`（stale run_id 拒收防 currentRunId 回拨、终态拒收、confirm/revise/cancel 三态文案经 inject 注入 InputQueue 排队下一 turn）。**仍存边界（如实）**：agent_task_status 只发 running 无终态；bash_chunk 为 tool_result 一次性 is_final（非执行中分片）。测试 tests/session-plan-bash-events.test.ts 31 用例 + tests/plan-response-delivery.test.ts 12 用例。
- change 2026-08-24-session-team-mission-context | 会话团队任务上下文（task-11 + task-14 daemon 部分；inject 简报/create 预建均在 backend daemon 模块侧落地，本仓不涉及）。**mission_status 第 6 常驻工具（task-11/FR-02/D-005@v1）**：mcp-server.ts 注册 `mission_status`（与 dispatch_worker/get_worker_result/list_workers/converge_mission/report_progress 并列，只读随时可调）——入参 workspace_id/mission_id 全可选（缺省从 X-Session-Id 会话上下文定位，显式传参仅作越权校验锚，同 task-10 审查 B1 模式）；hub-client.ts 新增 `getMissionStatus`（GET `_missionActionPath(action='status')`，ws/mid 缺省时 session-scoped 形态 `/api/missions/status`；无活跃 mission 时平台 200 `active=false`+hint 优雅返回不走 404，handler 透传不报错）；工具 description 详述返回内容（scope_workspaces 每项含 daemon_name/daemon_online/git_mode 三态）与使用时机（派前查 scope 机器状态、无任务先查再派、与 list_workers 互补）。**task-14**：gen:types 重生成 src/api-types.ts（TeamMissionCreateBlock/MissionStatusResponse/probe 端点类型进产物），daemon 侧无本地类型需收敛。测试：hub-client +4（session-scoped/显式形态/active=false/header 断言）+ mcp-server 扩展；daemon 全量 152 文件 2656 passed + tsc 0。
- ql-20260823-006-80c8 | 修僵尸会话 reopen 死循环（2026-08-23 bdec91a4 事故，4 次 reopen 全撞 SESSION_ALREADY_EXISTS）：`restoreAndReconnect` 对 `_store` 同 id 残留条目（backend 翻终态未通知 SESSION_END 的活僵尸 / `end()` 不删 store 的终态条目）先静默驱逐再恢复——`_terminateSession` 加 `opts.notifyBackend`（false 时跳过 onSessionEnd 回发，防与 reconnecting→active 竞态误翻 failed）；daemon.ts 路由注释同步。session-recovery.test.ts +2 用例（活僵尸驱逐 / 终态残留驱逐）。
- ql-20260820-007 | handleInitLease 时序重排（pull→writeDaemonState，防 .runtime 占位阻塞 repo-native junction/repo-mirrored 首拷）+ batch pullSpecBundle 补传 strategy/rootPath（防 rm -rf 拆 junction）+ bumpLocalSpecVersion 缺失重建（保鲜不失效）；pull 失败 daemonState=null 契约变更；test_init_lease +7 / test_spec_version_refresh 2 用例改契约，全量 2453 绿。
- 2026-08-15-init-trigger-sillyspec-init | handleInitLease 5→6 步（pull 后插 runSillyspecInit：spawn sillyspec init 平台模式+--no-skills+60s 超时杀树+3s 版本门控 MIN 3.26.8 fail-safe）+ cli.ts 构造前 AgentDetector 探测注入 tools（兜底 claude）+ UPLOAD_EXCLUDE_TOP_BASE 三处排除统一（projects 不上传+缓存残留防 delete op 误删）+ test_init_lease 改写四组新用例（22 绿）+ run-sillyspec-init.test.ts 新（11 绿）。
- ql-20260626-001-4a8e | 放宽 complete 事件 result body 截断 slice(3000)→slice(50000)（task-runner.ts `_eventToMessages` complete 分支），避免 daemon 侧砍断 agent 最终总结（backend 侧 content 已同步放宽到 50000）。
- 2026-06-26-daemon-client-spec-sync-fix | syncSpecTreeIfNeeded 抽离 + scan 终态回灌（FR-05）+ packSpecDir 含 .runtime（FR-06）+ task-runner kind=change-write 分支 + hub-client change-write 方法（FR-08/10）。
- 2026-08-19-runtime-live-daemon-read | `_registerRuntimeRpcHandler` 注册 `runtime.*` 四只读方法（read_progress/read_user_inputs/list_artifacts/read_artifact，design §6.1 契约）+ `runtime-handler.ts` 业务层：workspace_id UUID 正则白名单（shell 注入防线，spawn+shell:true 替代 execFile——Windows npm .cmd shim ENOENT）+ filename 预检 + containment resolve + 1MB/30s 上限；RpcError 复用 ws-client 导出类（`_dispatchRpc` instanceof 判定）。25 用例。
- ql-20260702-001-f3c7 | install.sh 生成 .cmd wrapper 改 CRLF（awk）+ REM 英文化（修中文 Windows cmd.exe 下 LF+UTF-8 中文 REM 致注释被当命令执行的噪音报错；daemon 本身正常）。
- ql-20260703-001-643f | agent-detector normalizeProvider（claude_code/claude-code→claude，其余原样）+ daemon _startInteractiveSession:2338 / reopen:2144 归一化（替换粗暴三元 === 'codex'?'codex':'claude'，防 opencode/cursor/openclaw 误归 claude）+ :2355 early-return 加 notifyRunResult 回传标 run failed（治本：provider 错配/CLI 缺失时不再 lease 永远 claimed/run 永远 pending 无声卡死，主动回传 backend 标 failed）。
- ql-20260705-004-5e2f | ws-client.ts 加 WS keepalive（30s ping + 10s pong 超时 terminate）：修 import get_spec_bundle RPC 撞 docker NAT idle 断连窗口报 HTTP_504_DAEMON_RUNTIME_OFFLINE mid-rpc（npm ws 库默认不发 ping，Python→TS 移植漏配）+ 顺手修 4 个 daemon_local_id URL query 测试断言（daemon-entity-binding 遗留债）。

- ql-20260709-001-7e3a | tool_result 命令输出截断 3000→100000（task-runner.ts `_eventToMessages` tool_result 分支，新增常量 TOOL_RESULT_PREVIEW_MAX）+ 超长追加中文标注，与 backend interactive 路径对齐（batch 路径，原 1928 result summary 已 50000、本次补普通命令输出的 3000 漏改）。

- ql-20260709-002-1b8c | MAX_OUTPUT 1万→5万(run最终输出) + thinking 2000→2万 + [TOOL_USE] 命令行 2000→2万（task-runner.ts），A 类日志截断放宽（B 类 MAX_STDERR_FORWARD/ECHO_MAX_LEN 防刷屏保留）。

- 2026-07-25-daemon-borrow-for-business | 借用任务沙箱只读隔离（候选 B，D-007@v2）：daemon.ts 检测 lease rootPath 的 `borrow-sandbox:<slug>` marker → `_getBorrowWorkspaceManager`（lazy，`<workspace_dir>/borrow-sandboxes`，非借用部署零回归）prepareWorkspace(slug=borrow-<actor>-<run>) 作独立 cwd + `registerBorrowSandbox`；session-manager.ts 写守卫（`_judgeWriteViaPolicyEngine`）对借用 session **跳过 lender runtime 缓存**，按 sessionId 隔离只允许写沙箱 root（`isPathUnderAnyRoot`），沙箱外/UNC 路径 deny（R-02 借用继承 lender 写权限堵住）。沙箱创建失败 fail-open 退化 workspace_dir 不阻塞读源码主流程。借用产出走 submit_lease_messages 回传不落沙箱。

- ql-20260809-003-56db | 多代理审计 8 个低风险单点修复（daemon 部分 1 处）：interactive/session-manager.ts 删 3 处 [reload-diag] 调试 console.log（:2602/:2723/:2891，含配套 eslint-disable no-console 注释）——生产路径残留调试日志，删后 reload 诊断逻辑（reloadProvider 机制）不变。session-manager-reload-provider vitest 11 passed + tsc 0 错。

- change 2026-08-08-llm-provider-openai-format | ProviderConfig 加 4 optional 字段（api_format/litellm_base_url/litellm_model_name/litellm_auth_token，零回归）；credential-injector.toEnv 加 openai_chat 早返回分支（ANTHROPIC_BASE_URL/AUTH_TOKEN/MODEL 指向 LiteLLM 网关 + gap-D 4 档位 ANTHROPIC_DEFAULT_{HAIKU/SONNET/OPUS/FABLE}_MODEL 全映射 litellm_model_name 防 claude 副通道请求无 deployment 失败，不注入上游 key D-003/NFR-01）；anthropic 分支逐字不变（NFR-02 零回归，REGISTRY/getInjector 未动）。

- ql-20260813-003-f61b | install.ps1 分发响应补 charset=utf-8（dist_router media_type=application/x-powershell 非 text/* ，starlette 不自动补 charset，PowerShell irm 按 latin1 解码 UTF-8 body 致中文 mojibake；对照 install.sh text/x-shellscript 自动补不乱码）+ test_daemon_dist 精确断言回归锚点 + gen:types 同步 openapi/api-types。

- change 2026-08-13-platform-managed-file-sync | spec 推送从**整树 tar 全量覆盖**改为**文件级增量 diff**（服务器权威清单 spec_file_manifest + base_version 乐观锁）：spec-sync.ts `postSpecSync` 读本地清单缓存 `~/.sillyhub/daemon/manifests/{ws}.json`（移出 specDir，pull rm 不清）→ walk+hash 与缓存 diff 只发变化 op（add/update/delete/rename 同 hash 不重传），首同步/404/失败回退旧 tar，conflict=true 抛 SpecPushConflict（人工拍板 NFR-02）；hub-client.ts 新增 `postSpecSyncIncremental`（JSON POST `/api/workspaces/{ws}/spec-workspace/sync-incremental`，**注意 /api 前缀**非 REST_PREFIX(/api/daemon)——QA 揪出 P0 URL 已修+回归锚定）；`.runtime(有点)/runtime(无点)/worktrees` 增量排除（D-006）。spec-sync-incremental.test.ts 12 用例。

- ql-20260825-001-f85b | 会话相关代码审查修复（daemon 8 项）：**P0 终态会话内存只增不减**——_terminateSession 收尾链不删 _store 条目（含凭证 env/subagentDepth/inputQueue 永久驻留），新增 _scheduleTerminalCleanup：终态后 10 分钟 setTimeout（unref）删 _store+_pendingInjectCount，条目被 restore/create 重建（状态非终态）则跳过 + 重建侧主动 _cancelTerminalCleanup，stop() 清全部定时器（结束后短期可查语义保留）；**P0 并发 _reloadSession 竞态→孤儿 Query 僵尸进程**——两次快速切换（reloadWithProvider/reloadWithConfig 共用 _reloadSession 无串行化）时后完成者覆盖 state.query，被挤掉的中间句柄永不 close、consume 协程 isAuthoritative 恒 false 挂起、背后 claude 子进程无人 kill 沉默烧 token；新增 _reloadChains per-session promise 链串行化（state 快照移入串行段，last-wins 顺序执行），两处 fire-and-forget 补 catch 日志；**resetForResubscribe 丢弃 waiter**——Codex 热切换时 _pending resolver 被置 null，旧 consume 永久挂起致 finally（stdoutLogStream.end/rl.close/h.close）不执行 fd 泄漏，改 resolve(null) 关闭哨兵（next() null→done 契约）让旧迭代器干净退出；**pendingServerRequests 只 push 不删**——新增 _markServerRequestResponded 统一 3 个应答出口（respond 闭包/fail-closed/未知 method）按 id 删 handle 数组+adapter.pendingMap；**同步复制 transcript 阻塞事件循环**——claude-transcript-dir 三个迁移/探测函数改 async + node:fs/promises（原 copyFileSync 数十 MB jsonl 期间全 daemon 心跳/节流停摆），调用点补 await；**回调异常杀会话**——claude-sdk-driver consume 的 onResult/onMessage 每次调用独立 try/catch（记日志继续迭代，原单条转发失败即整会话 fail+杀进程），codex 侧 void onResult 补 catch 防 unhandled rejection；create 失败补删 _sessionBudgetTokens/_overBudgetSessions；daemon.ts _interactiveFlatSeq 新增归属表 onSessionEnd 按 session 回收（原只增不减）。测试：新增 session-manager-terminal-cleanup 6/reload-serial 3 + input-queue 4/claude-sdk-driver 3/bridge 2/budget 1，codex 两文件断言「应答后清空」（原断言泄漏行为）；全套件 156 文件 2726 过 tsc 0。
- ql-20260825-002-3e67 | 会话优化第二轮（daemon 4 项）：**sessions.json 非原子窗口收口**——load() 目标缺失或空文件时 _recoverFromTmp（扫同目录 .tmp-* 按 mtime 新→旧取首个完整解析者 rename 落位，损坏忽略不炸），save() 成功后 _cleanupStaleTmp 清超 1h 过期 tmp（save 队列串行+时长阈值不误删在写 tmp），解析抽纯函数 parseContent 供 load/恢复共用；**子代理桶无界增长**——_onResult 在 _checkBudgetCutoff 后调 _shrinkSubagentBuffers：turn 收尾清非 main 桶+subagentDepth.clear()（SDK 契约下 turn result 前本轮子代理已结束，无需解析 tool_result），删前把子桶 sessionInput/OutputTokens 折算进 main（per-call 增量求和语义成立，预算跨桶聚合不漏计；cache_* 为 replace 快照不求和），late timer fire 由 _flushPartial 桶缺失早退兜底；**inject 附件下载超时+错误转译**——_downloadAttachmentWithTimeout（Promise.race+60s unref 定时器，race 输家 settle 被吞）包两处下载，超时抛新 SessionAttachmentTimeoutError（types.ts，带会话/附件 id）由既有单附件 catch 降级「下载失败」不中断 turn；inputQueue.push 包 catch，SessionQueueClosedError 转译 SessionNotActiveError 不再泄漏队列内部错误给 WS 调用方；**终态通知按会话串行**——_notifyChains per-session promise 链（对齐 _reloadChains 模式，永不 reject 链尾+自愈摘除）包 onTurnResult 与 onSessionEnd：同会话 end 通知必 await 在飞 turn result 之后（backend 恒 result→end），空链时同步直调保时序（既有测试零改），只串这一对终态通知。测试：新增 session-store-persistence 恢复 6/subagent-shrink 5/inject-attachment 6/terminal-notify-order 5；全套件 159 文件 2748 过 9 跳过 + tsc 0。
- ql-20260829-001-ace1 | batch token 统计翻倍修复：stream-json.ts extractResultStats 的 input/output 旧语义是 `result.usage + accumulated` 求和，但 Claude CLI 的 result.usage 本身就是**整个 run 的官方累计**、accumulated 是 message_start/message_delta/assistant 自算的**同一份账**（batch 必开 --include-partial-messages，accumulated 恒有值）——同源相加致 AgentRun.input/output_tokens 终值精确 2 倍（实测模拟事件流 1447→2894 / 130→260；run 进行中实时值正确，complete stats 经 complete_lease 无条件覆盖后在结束时跳成 2 倍）。task-16 当时已发现并修了 cache 两维（result 优先回落），input/output 漏改。修复：input/output 对齐 cache 同款「result.usage 优先，缺失才回落 accumulated」。同步修正 4 个钉住求和语义的既有断言（stream-json/stats-passthrough case1&case3/cache-passthrough/task-runner-budget——后者 result.usage 数据改成官方累计语义 110/70 保持 budget 触发意图）+ 新增 case1b 真实事件流翻倍回归。测试：stream-json/stats-passthrough/cache-passthrough/task-runner-budget 88 + task-runner 72 全绿，tsc 0。
- ql-20260829-002-255f | interactive 终态 token 口径修复：SDK 文档（sdk.d.ts SDKResultSuccess）钉死 result.usage 是 **MAIN AGENT LOOP ONLY**（不含 Task 子代理/sidechain/compaction）且 per-turn，正确口径字段是 modelUsage（Record<string,ModelUsage> camelCase，含全部 query-pipeline 调用，官方注释 "The correct field for token/cost accounting"）——daemon.ts onTurnResult 终态 payload 四维原先只取 resultMeta.usage，会话大量派 Task 子代理时系统性低估。修复：新增 _aggregateModelUsage（跨模型求和，非有限数防御），四维优先 modelUsage 聚合、缺失/空回落 result.usage（老 CLI/Codex 兼容，D-001 行为不变）；total_cost_usd 本身已含全 pipeline 不动。测试：daemon-interactive-bridge 新增聚合优先/空回落/非法条目 3 用例，26+89（关联 7 文件）全绿 tsc 0。配套数据清理（本地 DB，非代码）：2026-08-27 23:07:58（ctx_tokens 首现=task-02 新 daemon 生效点）之前 run 的六 token 字段置 NULL（先备份 agent_runs_token_backup_20260829 表 514 行）——该批 input 累计 1860 万占总量 90%+ 全为会话级累计重复计数污染，清理后干净数据 input 14.6 万 vs cache_read 55.8 万比例恢复正常。**真实调用实测（2026-08-29 补）**：① batch——真实 GLM 端点跑 claude CLI 2.1.216 stream-json（--include-partial-messages，2 turn 工具任务）产出 69 行真实事件流喂修复后 adapter，四维精确 == CLI 官方 result.usage（input=34009 不翻倍；同流可复算翻倍机制：Σ message_delta.usage=34009 与 result 同源，旧求和代码会得 68018）；② interactive——真实 SDK query 派 Task 子代理（glm-4.7），result.usage（主循环 only）input 仅 407 而 modelUsage（全 pipeline 含子代理）=49273/output 264/cache_read 38784（低估 100 倍实锤），真实 result fixture 喂修复后 onTurnResult payload 四维 == modelUsage 聚合值。
- change 2026-08-29-usage-by-provider-model（daemon 侧） | 终态上报扩展：daemon.ts 新增 _modelUsageRows（SDK modelUsage 逐模型拆行 camel→snake + api_requests 按消耗占比四舍五入分摊/残差补最大行，Σ守恒）+ assistant 消息计数 Map<runId>（含子代理，终态读删+onSessionEnd 兜底）→ payload.model_usage[]/api_requests（modelUsage 缺失两字段不写，老 CLI 兼容）；batch 侧 stream-json 新增 messageStartCount getter（resetAccumulator 同生命周期）+ task-runner attachBatchModelStats 五出口单点组装（model=ProviderConfig.model??"unknown"，ndjson 等无 getter 不加字段）+ hub-client completeLease statsExtras 条件注入。bridge 32/stats 12 用例绿。前置：ql-20260829-002 的 modelUsage 聚合（修复主循环 only 低估）。
- change 2026-08-29-daemon-platform-resilience（daemon 侧） | ①WS 重连改指数退避 [1,2,4,8,16,30]s+±20% jitter（jitterFn 可注入），收到任何消息（含 pong/非法 JSON）重置档位，替换固定 5s RECONNECT_INTERVAL_MS；register 周期重试（心跳循环检测空注册表，15s 起步翻倍封顶 60s 重放启动探测快照）+heartbeat 401/403 清注册表接管重注册（FATAL 日志）。②新模块 control-dispatcher.ts——控制指令统一 (kind,payload) 消费入口（六 kind 复用 _routeSessionControl/_routePermissionResponse/_routeProviderConfigChanged 零复制）+LRU 256 command_id 滑动去重窗+批量 ack（成功/业务失败均回执、ack 网络失败留队、WS 无 runtime 回执未知桶下趟捎带）；_reconcileAfterReconnect 四步统一对账（立即心跳→drain outbox→补拉控制指令→补拉 pending leases+change-writes，幂等防重入），onConnected 裸 drain 并入；心跳响应 pending_controls>0 触发控制指令步。③outbox 扩展（D-007 形态）——entry 加 kind（messages 缺省/run_result/session_end）+pending_token 字段，文件命名 runId→dedupId 泛化（旧 runId 文件 load 兼容），drain 按 kind 三路由（SubmitClient 扩展 notifyRunResult/notifySessionEnd 两可选方法）；终态 retryTerminal 用尽落箱重放；422 入箱+ClaimTokenRefresher（读 SESSION_INJECT 刷新的 SessionState）重放（A3 getAgentSession 的等价实现）；PERMISSION_REQUEST WS 不通走 HTTP 上行（submitPermissionRequest，claimToken 空窗维持 fail-closed）。④stop() 在 markOffline 前调 suspend-batch 挂起会话（失败降级走 600s sweep）；_recoverOneSession 网络失败保留记录入重试队列（30s×2 封顶 5min+onConnected 立即重试+7 天超龄清理+合并落盘 _mergedPersistableSnapshot 防 flush 冲掉保留记录）。hub-client 加 getPendingControls/ackControls/suspendSessions/submitPermissionRequest；protocol 加六 kind 常量与 HeartbeatResponse.pending_controls。测试：ws-client 退避断言重写+control-dispatcher 15+resilience 64+daemon-stop-suspend 9+recovery 补断言+integration/resilience-scenarios 4，全套件回归绿 tsc 0。
- ql-20260829-003-feee | 过渡类型收口：protocol.ts PendingControlCommand 与 hub-client.ts SuspendBatchBody/SuspendBatchResponse 由手写 interface 改为 api-types 生成物类型别名（components['schemas']['ControlCommandItem'/'SessionSuspendBatchRequest'/'SuspendBatchResponse']，逐字段同构、生成物 payload 可选可空由 dispatcher 既有 ?? 兜底吸收），消费方零改动纯零行为；tsc 0 错+相关回归 92 用例绿。HeartbeatResponse 为刻意宽松视图（extends Record）不在收口面。
- change 2026-08-29-daemon-selfupdate-safety（daemon 侧） | SELF_UPDATE 安全层：①忙判定查询口 hasRunningTurn（仅 status==='running'，D-001）/hasActiveLease（_controllers 非空）。②磁盘旁路探测 startDiskProbe（读 bundle 文件正则提取 BUILD_ID 与内存比对——**不用 --version**（输出 semver 不同源，Grill 实跑证伪）；差异含降级→注入式 onDiskChange 回调；读失败/正则不中/空值/dev/0 间隔五类≠变化仅 debug；self_reload_check_interval_sec 默认 600 可配 0 关）。③tryUpdate 单入口编排器：所有权 _updateBusy 同步占位（忙推迟即释放可再入/交接排定持有到退出）/30s 复查定时器（单实例不叠/离开推迟三路径统一清/stop 兜底）/server_command 链 stop 前终检（无 await 窗口）/disk_change 直启不经 runDaemonSelfUpdate（操作者换文件即意图，multica trySelfReload 同款）/preflight 伴生 fetchLatestBuildId 不动布尔签名。④pending-update.json 原子写/清除/启动清矛盾残留+status 展示。⑤心跳第 4 可选参 pending_update（undefined 键不含；剥 since，起点权威在 backend）。测试：18+17+27+7+4 集成+config 键列表连带债兜底，回归全绿 tsc 0。
- change 2026-08-29-batch-session-inherit（daemon 侧） | ①CreateSessionInput 加 resume 可选字段（interactive/types.ts）+daemon.ts _startInteractiveSession create 调用传归一化 execPayload.resumeSessionId（既有 :6120 归一化不新建第二套；undefined 零行为变化）。②SessionManager.create 拆 wrapper+_createInternal 补 input.resume→spec.resume 透传（激活既有 spec.resume→driverOpts.resume 链）+RESUME_DAMAGE_PATTERNS 三模式正则（session not found/no conversation/unable to resume）命中→清 pendingFirstPrompt timer（防首句双提交 ql-20260825-002 回归）→同参 fresh 重建一次+resume_downgraded 日志（含原 resume id）；一次不循环再失败走普通 create 失败路径。pendingFirstPrompt 10s fallback 未动（首轮驱动机制照旧）。3+10 用例+24 近邻+92 回归+tsc 0。
- change 2026-08-27-session-token-usage-fix（daemon 侧） | session-manager usage 计数语义修复：轮级计数器新增、pendingUsage 轮级化（不再跨轮累计）、新指标 ctx_tokens、cache 语义注释修正；口径=本轮计费量 vs ctx 瞬时量（main 桶限定）
- ql-20260830-011-99a8 + ql-20260830-012-d892 | CI 环境依赖测试修复 + 自更新校验目录可注入（补 2026-08-30-daemon-self-heal 的 D-009 stop 前主拦截语义入卡）：_tryUpdate 两处 validateBundleOnDisk 的校验目录由硬编码 DAEMON_BIN_DIR（~/.sillyhub/daemon/bin）改为 dirname(_selfUpdateBundlePath)——生产默认值 dirname 后同值行为不变，测试经 DaemonOptions.selfUpdateBundlePath 注入临时目录即隔离（D-006 同款可注入口径）；集成测试 selfupdate-scenarios 两大 describe（task-08 四路径 + task-06 恢复互斥）beforeEach 预置 ≥MIN_BUNDLE_BYTES(64KB) 且含 BUILD_ID 的假 bundle 模拟「下载已落盘」盘态（download 被 mock 不真实写盘，而校验走真实链；不预置则 CI 无 HOME 部署必挂——951ad8be 在作者本机因有真实部署 bundle 而全绿的根因）。同批清偿 backend 15 个测试文件 19 处夹具非法 location 值（'change' 15 处 + 'local' 4 处，27c05447 CHECK 三值对齐遗留，统一改 'active'；首轮漏 'local' 值二轮补修）。daemon 相关 3 文件 50 用例 + tsc 0；backend 15 文件 217 用例 + ruff 0。

- ql-20260831-002-f683 | 轮末 pendingUsage 补发（ctx_tokens 短轮丢失根因修复）：_onResult 在清零轮级计数器**之前**、onTurnResult 通知之前调 _flushTerminalUsage——逐桶检查 pendingUsage 未 flush（与 flushedUsage 不等）即补发 usage-only 消息（content 空串+顶层 usage，backend submit_messages「无 content 的 message 跳过日志只提取 usage」既定分支），发后同步 flushedUsage 去重。缺口实证：result 与最后一次 message_delta 常落同一 500ms flush 窗口，轮边界 pendingUsage=null 清零把整轮最后一次 usage（含 ctx_tokens，close 终态不覆盖 ctx）静默丢弃——agent_runs.ctx_tokens 大量交替 NULL（短/快速轮必丢，前端环分子取最新非 null 滞后）。子桶同补发（input/output，仍无 ctx 键，D-006 口径不变）。新增 session-manager-terminal-usage.test.ts 4 用例（快速轮补发+消息先于 result 顺序断言/同值不重复/子桶无 ctx 键/无 usage 零补发），turn-usage 7 + usage-cache 2 回归绿 tsc 0。
- ql-20260831-009-c751 | 会话用量虚增修复（快照差分化）：SDK `modelUsage`/`total_cost_usd` 在 streaming-input 会话是**跨轮累计快照**（sdk.d.ts 明示 "cumulative across turns — read the latest result rather than summing"），而 daemon 一条 interactive 会话=一条长生命周期流式 query（claude-sdk-driver 订阅跨 turn input AsyncIterable），onTurnResult 原先把每轮快照当本轮增量上报、backend 按 run 求和 → 多轮会话用量虚增。生产实证（阿里云 574793c6，3 轮）：缓存读显示 338.7 万 vs 末轮快照 128.3 万（2.64 倍），输入 11.9 万 vs 4.2 万，输出 3.8 万 vs 1.4 万；铁证=三轮四维全单调递增 + 轮 2 快照差值(13571-10474=3097)与该轮 result.usage output 逐字相等。修复：daemon.ts 新增 `_deltaModelUsage`（按模型逐维 `max(0, 当前-基线)` 差分 + 复位检测——任一有效维回落基线即判 SDK 计数清零重来（resume/中途 /clear/crash），该模型按全量上报基线归零，不重不漏）+ `_modelUsageBaselineBySession`（Map<sessionId, 基线>，onSessionEnd 统一回收）；onTurnResult 四维/model_usage 行/total_cost_usd 全走差分（成本独立有效性、浮点截 6 位防尾差），无效快照（老 CLI/Codex）基线不动回落 result.usage 旧路径。机器用量/会话用量条等 backend 聚合（agent_run_model_usage 求和）随源头修复自动归真；历史已存数据仍虚增（daemon 侧无法重算，未做迁移）。测试：daemon-interactive-bridge 新增 7 用例（生产形态差分回归/多模型独立推进/复位三连/老 CLI 回落+成本独立/浮点截断/onSessionEnd 回收/并发会话隔离），文件 39 全绿 tsc 0。
- ql-20260831-017-a021 | SESSION_INJECT 早到竞态必死修复（分离式等待替代 3×100ms 短重试）：backend 等 daemon ready 仅 8s 即超时 fallback 发首条 SESSION_INJECT（session/service.py wait timeout=8），而 daemon create 全链（skills 拷贝/MCP bundle 预取/claude spawn 前置）Windows 实机 ~31s（本地会话 52893639 实证：inject 到达与 store 写入差 23s）——原 _routeSessionControl 的 3×100ms not_found 重试耗尽即丢弃，叠加当日部署的 ql-20260831-005「丢弃即报 run failed」把慢启动竞态变成会话必死（error_code=interactive_interrupted，output「daemon 丢弃消息指令：daemon 本地无该会话状态…」）。修复三层：①常量区新增 DEFAULT_INJECT_WAIT_SESSION_MS=60s（≈实测 2.5x 余量，**非原计划 12s——12s 救不了 23s 实测缺口**）+ INJECT_WAIT_POLL_MS=100ms + injectWaitSessionMs()（env SILLYHUB_INJECT_WAIT_SESSION_MS 可调、逐次读取便于测试覆写）；②_routeSessionControl 的 INJECT not_found 分支分流 _awaitSessionThenRoute 后即刻 return（handler 秒回，不阻塞 WS/补拉分发批——pullAndConsume 逐条 await，60s 同步等待会卡同批 permission_response 等延迟敏感指令）；③新增 _awaitSessionThenRoute：轮询至窗口上限，会话出现→重入 _routeSessionControl 走完整消费链（lease 校验/claim_token 刷新/附件），超时才 _reportInjectDropped（005 上报语义保留、仅延后到确认真等不到，reason 带「等待 Nms 会话仍未创建」）；重入包 try/catch 防分离调用绕过 dispatcher handler_error catch 的 unhandled rejection。INTERRUPT/END not_found 维持纯 warn（良性终态收敛）。测试：daemon-inject-drop-report.test.ts A-F 适配（beforeEach 压窗 150ms 控时）+ 新增 G（会话 200ms 晚到→正常 inject 零上报，日志 inject_wait_session_appeared waited_ms=215）7/7 绿；近邻 kind-dispatch/resume-route/control-dispatcher/interactive-bridge/ws-client-session-control 5 套件 94/94 绿；tsc 0。未跑 daemon 全量（仓库规约留给 CI）。
- quick 2026-09-01 风险审查修（六项） | ①SILLYHUB_DAEMON_DIR 隔离补三个漏项：sessions.json（session-store-persistence 改 `defaultSessionFilePath()` 懒函数，无参构造构造时求值）/ audit-failed.jsonl（audit-sink `defaultFailoverPath()`）/ codex 交互日志目录（codex-app-server-driver 抽出 `codexInteractiveLogDir()` 导出）——原均直拼 homedir()，隔离实例会读写真实主目录（sessions.json 被整文件覆写=跨实例清档）；②`_awaitSessionThenRoute` 轮询加停机感知：每拍检查 `!this._running` 即中止（不报 run failed——未处理原因是 daemon 退出而非会话未建，由 backend 控制指令 GC 兜底；修 stop() 后挂起轮询链把事件循环钉住最长 60s、拖慢 systemd/launchd 重启接管）。daemon-state-dir-isolation 新增 3 断言 + daemon-inject-drop-report 新增 H（中途置 _running=false→零上报+不等满窗；buildDaemon 统一置 _running=true 模拟运行态前提）17/17 绿、近邻 persistence/audit-sink/codex 107/107 绿、tsc 0。
<!-- MANUAL_NOTES_END -->
- change 2026-09-02-changes-overview-card | progress 总览采集器：sillyspec-manager 三态采集（execFile 数组形参/主仓根锚定/N=50+32KB 降级）+ daemon 第五循环（interval 默认 60s）+ 心跳第 6 参透传（undefined 占位）。config+heartbeat+manager 108 绿。
- ql-20260904-026-ad9b | register 鉴权/归属失败终端中文提示 + 后台进程 console tee 落 daemon.log（事故驱动：换账号 key 复用机器身份被 403 ownership mismatch 拒绝时 daemon 静默重试零提示，且自更新 respawn（stdio=ignore）/VBS 隐藏自启的 console 输出凭空丢失，用户误判「启动不了」）：①daemon.ts 新增 `buildRegisterFailureHint`（HubHttpError 403-ownership 单列文案含「改回原账号 key / 平台改绑机器归属」两条出路；401 提示重签 key；其它 HTTP 状态通用一行；网络错误不打提示防刷屏）+ `_registerFailStreak` 计数节流（首错立即、每 `REGISTER_HINT_EVERY_N_FAILURES=5` 次重发，60s 退避下约 5 分钟一条；成功清零并 console.log「注册已恢复」）；hint 同时进 `daemon_register_failed` 事件 kv。②cli.ts 新增 `attachConsoleToLogFile`（console 四方法 tee 到 daemon.log，append 逐行异步、失败静默防递归、幂等防双重包装；导出供单测），startAction 在 `!process.stdout.isTTY` 时挂接——respawn 与 VBS 隐藏自启两场景均为非 TTY，前台终端行为不变。测试 daemon-register-error-hint.test.ts 6 用例（403 首提示/节流静默+第 6 次重发/恢复提示+计数清零/401 文案/网络错无提示/tee 落文件+幂等，tee 用例照 cli.test.ts 范式动态 import 防 main() 副作用）6/6 绿；近邻 multi-runtime/heartbeat-sillyspec/daemon/cli/preflight/autostart 6 套件 208 passed 8 skipped；tsc 0。
- ql-20260904-027-25f6 | 修三件部署遗留：①运行中自更新补服务器轮询——原触发源只有平台 WS `daemon:self_update` 指令与磁盘旁路探测（读本机 bundle 比 BUILD_ID）两路，无任何服务器轮询，部署新 bundle 后运行中的 daemon 永不自发现（实事故：部署后等 11 分钟零触发，重启靠启动预检才更上）；新增 `startServerVersionProbe`（复用 `self_reload_check_interval_sec` 间隔与 startDiskProbe 同款守卫：0/非法不建定时器、dev 构建跳过、unref、幂等重建、round catch 收敛），每轮 `fetchLatestBuildId` 拉 latest.json 与内存 BUILD_ID 严格不等即回调（含降级，防降级门在 runDaemonSelfUpdate 链内），start() 接线 `_tryUpdate('server_poll', latest)` 进既有升级链，`serverVersionProbeActive` getter 供断言；reason 联合类型三处扩 `'server_poll'`（`_tryUpdate`/`_deferUpdate`/pending 语义——行为与 server_command 同走下载链）。测试 daemon-server-version-probe.test.ts 5 用例（差异每轮一触发/同值不触发/拉取失败静默/interval=0 不建/dev 跳过）。②③见 backend.md 同 ql 条目（runtime 归属对齐 + entrypoint chown 守卫）。
- change 2026-09-04-conflict-resolve-entry（daemon 侧） | 机器级直连指令执行（不走 control-dispatcher）：daemon.ts `_handleWsMessage` 新增两条直连 case（`MSG.SILLYSPEC_RESOLVE`/`MSG.SILLYSPEC_GHOST_CLEANUP`）+ 升级 in-flight 串行忙拒；sillyspec-manager.ts 新增 SillySpecCommandExecutor 四方法——runResolve（`sillyspec platform resolve --change <名> <strategy flag>`，keep_local/take_platform）/ runGhostCleanup（两步 doctor --cleanup-ghosts --confirm → platform sync，上行终态+墓碑供平台侧收敛，doctor 失败不继续）/ isUpgradeInFlight / recordCommandResult，execFile 数组形参每步独立计超时（config 新键 `sillyspec_command_timeout_sec` 默认 120s）；`_lastCommandResult` latest-wins + 10min 终态窗惰性过期（getCommandResult 每次判定，心跳仅在窗口内携带该键，过期停发键不发显式 null——backend 两态语义置 NULL 清除，不依赖 daemon 排定时器）。
- change 2026-09-07-conflict-diff-compare（daemon 侧） | 冲突对比弹窗 daemon 侧（task-02）：①新 RPC `sillyspec_conflict_snapshot`（daemon.ts `_registerSillySpecRpcHandler` :5413）→ sillyspec-manager `conflictSnapshot`（:996）——冲突记录 kind 双前缀（spec-sync-conflict- / sync-conflict-）、realpath 防逃逸（落点必须在根内，越界拒读）、三道截断护栏（单文件 256KB :158 / 路径 300 :161 / 聚合 4MB :168）、信噪比排序（本变更目录优先 archive 沉底）、二进制嗅探、progress 全局 envelope 过滤 data.changes[]、ql_id 读 guard.json quicklogId；②collectStatusOnce（:608）心跳补报 `_attachPendingConflictQlIds`（:1168）对 quick-* 冲突条补 ql_id（单条失败仅缺省不阻断心跳）。测试 sillyspec-conflict-snapshot.test.ts。backend 侧见 backend.md 同 change 条目。

### 2026-09-07-agent-liveness-states（agent 会话活性状态推导）

- `src/agent-log/liveness/` 新子层：五态模型（working/blocked/idle/ended/unknown）+ format→deriver 注册表（zcode/codex/claude 三推导器，永不产 blocked——R-01）；tailer 周期 10s offset 差量读（轮转 reset/消失超窗 ended 回收/watch≤16/4MB 预算/R-02 fail-open）；自发现三源（spawn 记录/sessions.json 恢复/窗口重扫）+ 两档定位（claude·pi 直算、codex·zcode 窄扫标记匹配 fail-closed）。
- `hub-client.ts` 增 liveness 上报：Bearer shpsync 直推 `POST /api/agent-logs/states`（daemon 各 workspace root local.yaml platform token）；`buildAgentLogStatePushGroups` 落 D-012 优先级（第一方 PERMISSION_REQUEST pending 覆写 blocked）。
- `daemon.ts`：`_livenessLoop` 独立 `_fire`（60s 发现刷新 + tailer 10s tick；abortableSleep 信号感知——裸 setTimeout 会拖死 stop）。

## 文件结构更新（2026-09-07-arch-large-file-split）

两个 god 文件拆为「瘦 facade + 同名模块包」（机械搬移 + facade re-export，原路径导入零变化——Node ESM 无目录导入，facade 文件保留；daemon tsc --noEmit 零错误 + 定向 958 用例全绿）：

- `src/interactive/session-manager.ts`：5438 行 → **1965 行瘦 facade** + `src/interactive/session-manager/` **13 模块**（task-02，commit 2d6bcfa72）——background-tasks(440) / driver-factory(366) / events(536) / helpers(31) / index(19) / lifecycle(381) / notify-chain(58) / permission(646) / persistence(428) / turn-control(486) / types(757) / usage(225) / write-guard(338)，全部 ≤800（max types 757）。
- `src/task-runner.ts`：3426 行（D-010 merge 后 3430，含 windowsHide 移植进 spawn-stream）→ **1660 行瘦 facade** + `src/task-runner/` **8 模块**（task-03，commit 9913bb26a）——change-write(108) / file-mcp(157) / index(56) / payload(44) / render(186) / runner-types(239) / skill-prompt(337) / spawn-stream(712)，全部 ≤800。
- 新增 `src/payload-utils.ts`（112 行，lease payload 鸭子读取器统一，轻重构白名单①）与 `src/event-wire.ts`（381 行，事件上行平行转换收敛，白名单②）——task-04，commit 4672ab25f。
- 在途排除文件（daemon.ts / hub-client.ts / config.ts / protocol.ts / sillyspec-manager.ts）本变更零改动（D-001；protocol.ts 存在 D-010 merge 自动合并残留的重复接口块 +48 行，合回 main 前复位 main 版——详见 change 的 verify-summary.md）。

### 2026-09-20-agent-log-session-replay（解析器矩阵独立重做，replay-redo 分支）

- sillyhub-daemon/src/agent-log/：parse-zcode-model-io.ts 扩 AgentLogUsage（snake_case 四键直通）+ 消息级 turn_id/model/usage/is_meta/turn_end + totalUsage（窗口前求和）；新增 parse-claude-code-jsonl.ts（usage 全量口径归一 input+cache_read+cache_creation，isMeta→is_meta 位）与 parse-cursor-agent-transcript.ts（{role,message} 行+turn_ended 标记段，token 恒缺省）；read-zcode-sqlite.ts 透传（实证 db.sqlite model_usage 表 assistant_message_id 1:1、turn_usage 反查、SUM 会话级 totalUsage）；registry.ts 注册 claude-code-jsonl/cursor-agent-transcript 两 key。真实数据四探针：zcode rollout 142 段 557 万 token / sqlite 同会话 892 段 4384 万 / claude-code 746 段 6240 万 / cursor 47 段。
