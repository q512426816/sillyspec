---
author: qinyi
created_at: 2026-09-20 13:14:36
generated_by: sillyspec-fourpiece-init
---
# 提案书（Proposal）

## 动机
知识库已解决「能看到」（zone/写入/蒸馏全链），但知识资产的价值看不见：命中遥测困在各端本机（.runtime 不同步）、决策/FR 索引裸文件展示埋没结构化价值、手册/INDEX 不可导航。用户要求运营口径（命中率/使用率）而非纯次数（开发量混叠）。

## 关键问题
1. hits 数据 2597 条困本机，平台侧零可见（.runtime 在同步排除集）。
2. 多用户单工作区汇聚无机制（用户亲提关切）。
3. decisions/fr/手册/INDEX 四类形态无统一人类友好渲染，fr 无独立 zone。
4. 纯次数指标意义不大，需剥离开发量的健康度指标。

## 变更范围
daemon hits 豁免增量上行（hash 幂等去重+daemon 归属）；backend knowledge_hits 表+接收/聚合端点（覆盖率/死条目/密度/生效速度四指标+使用率榜全量）；前端运营仪表盘+全 zone 统一条目渲染器（三形态）+fr zone+条目/文件使用徽标。

## 不在范围内（显式清单）
- 不做人工浏览计数（口径=agent inject）
- 不改 sillyspec CLI（上行走 daemon 豁免）
- 不做按人视图（留数据层）
- 不做日历热力图（用户亲答删除）
- 不做决策条目级热度（第一期文件级）
- 不做自动清理动作（死条目仅清单引导）

## 成功标准（可验证）
- 本仓 hits（2597 条）部署后上行落库零重复（重报全量计数不变实证）
- 知识库页四指标卡出数（覆盖率可复算）；使用率榜全量按次/天排序
- decisions/fr/手册/INDEX 点开均为卡片流（状态徽标/取代链/互跳/防复潮横幅/导航可点），原文 tab 可切回
- fr 出现在「需求规则」独立 zone
- 老 daemon（未升级）环境：页面「暂无使用数据」态，既有功能零回归
